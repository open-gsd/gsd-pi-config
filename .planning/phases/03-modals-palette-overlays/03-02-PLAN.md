---
phase: 03-modals-palette-overlays
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/components/ShareModal.tsx
  - src/components/ImportPreferencesModal.tsx
  - src/components/LoadPresetModal.tsx
  - src/pages/GalleryPage.tsx
  - src/pages/GalleryPage.source.test.ts
autonomous: true
requirements:
  - OVL-01
must_haves:
  truths:
    - "Share modal mounts controlled shadcn Dialog with X + ESC + backdrop dismiss (D-01, D-04)"
    - "Share always shows visible redaction warning naming key/token/secret/password (D-17, D-20)"
    - "Share review is scrollable mono pre of exact clipboard bytes; primary Copy to clipboard with Copied! feedback (D-18, D-19)"
    - "Gallery preview reuses ShareModal with title Preview preset (D-07)"
    - "Import preferences uses Dialog + Button language; browse outline; primary Import into editor/workspace; handlers and native pick hooks unchanged (D-05, D-06, D-23)"
    - "Load preset uses Dialog + Input search + linear rows; From file and gallery fetch handlers unchanged"
    - "No uiClasses btn/btnPrimary/modalPanel on these restyled overlays (WEB-06)"
    - "No hand-rolled window Escape listeners on these Dialog shells (D-13); no early-only unmount that skips controlled open (D-14)"
  artifacts:
    - path: src/components/ShareModal.tsx
      provides: "Dialog Share + optional title for preview"
      contains: "Dialog"
    - path: src/components/ImportPreferencesModal.tsx
      provides: "Dialog Import preferences"
    - path: src/components/LoadPresetModal.tsx
      provides: "Dialog Load preset"
    - path: src/pages/GalleryPage.tsx
      provides: "Preview preset title on ShareModal"
      contains: "Preview preset"
  key_links:
    - from: ShareModal copy()
      to: content prop bytes
      via: "clipboard writeText + execCommand fallback unchanged"
    - from: GalleryPage ShareModal
      to: title Preview preset
      via: "optional title prop (D-07)"
    - from: Import/Load handlers
      to: onImport / onLoaded / pick hooks / importPresetDialog
      via: "behavior-stable domain paths (D-00e)"
---

<objective>
Restyle **Share**, **Gallery preview**, **Import preferences**, and **Load preset** onto controlled shadcn Dialog + Phase 2 Button/Input language while preserving all domain handlers and security UX (per D-01, D-04–D-07, D-17–D-20, OVL-01).

Purpose: First user-visible overlay slice — product modals match Mist Sky without changing import/share/load behavior.

Output: Four surfaces on Dialog; Gallery preview title; no uiClasses button language on these files.
</objective>

## Phase Goal

**As a** web user of GSD Pi Config, **I want** product modals and the ⌘K palette on shadcn Dialog/Command with solid focus and exclusive open, **so that** overlays match Mist Sky chrome without changing share, import, load, submit, or field-jump behavior.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-modals-palette-overlays/03-01-SUMMARY.md
@.planning/phases/03-modals-palette-overlays/03-CONTEXT.md
@.planning/phases/03-modals-palette-overlays/03-UI-SPEC.md
@.planning/phases/03-modals-palette-overlays/03-RESEARCH.md
@.planning/phases/03-modals-palette-overlays/03-PATTERNS.md
@src/components/ui/dialog.tsx
@src/components/ui/button.tsx
@src/components/ui/input.tsx
@src/components/ShareModal.tsx
@src/components/ImportPreferencesModal.tsx
@src/components/LoadPresetModal.tsx
@src/pages/GalleryPage.tsx
@src/pages/GalleryPage.source.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ShareModal Dialog + Gallery Preview title (D-01, D-07, D-17–D-19)</name>
  <files>src/components/ShareModal.tsx, src/pages/GalleryPage.tsx, src/pages/GalleryPage.source.test.ts</files>
  <read_first>
    - src/components/ShareModal.tsx (full — copy handler, redaction copy, Escape, uiClasses)
    - src/pages/GalleryPage.tsx (ShareModal mount + preview content path)
    - src/pages/GalleryPage.source.test.ts
    - src/components/ui/dialog.tsx exports (Dialog, DialogContent, DialogHeader, DialogFooter, DialogTitle, DialogDescription)
    - .planning/phases/03-modals-palette-overlays/03-UI-SPEC.md Share + secret-scan UX + Copywriting
    - .planning/phases/03-modals-palette-overlays/03-PATTERNS.md ShareModal + Gallery preview sections
  </read_first>
  <behavior>
    - ShareModal imports Dialog primitives from @/components/ui/dialog and Button from @/components/ui/button
    - Optional title prop defaults to Share preset; Gallery passes Preview preset
    - Description still names key, token, secret, password (all four) as always-visible copy — not tooltip-only
    - Primary CTA Copy to clipboard / Copied!; Cancel outline; mono pre shows content
    - No btn, btnPrimary, or modalPanel imports from uiClasses
    - No window keydown Escape listener (Dialog owns dismiss)
    - Controlled open via Dialog open + onOpenChange mapping to onClose; prefer mounted Dialog over early return-null-only pattern for focus restore (D-14)
  </behavior>
  <action>
    Restyle Share + wire Gallery preview title (per D-01, D-04, D-07, D-17–D-19, D-00e).

    1. ShareModal: map existing props { open, content, onClose } to controlled Dialog (open + onOpenChange → onClose when false). Add optional title?: string defaulting to Share preset. Keep copy() clipboard writeText + textarea execCommand fallback and ~2s Copied! state exactly.
    2. Anatomy per UI-SPEC: max-w-2xl, max-h-[80vh], rounded-none, header/body/footer pads (px-5 py-4 / footer py-3), showCloseButton or ghost Close with aria-label Close (40×40). DialogDescription = redaction review copy naming key/token/secret/password. Body = scrollable mono pre of content (exact bytes). Footer: outline Cancel + default Copy to clipboard.
    3. Remove hand-rolled fixed inset-0 scrim, modalPanel, btn/btnPrimary, and component-level window Escape listener (D-13). Do not use AlertDialog (D-02).
    4. GalleryPage: pass title Preview preset on ShareModal; keep buildShareablePreset preview content path unchanged.
    5. Extend GalleryPage.source.test.ts to assert Preview preset (or title prop) appears with ShareModal mount.
    6. Do not edit preferencesCore, SubmitPresetModal, Palette, ConfigApp exclusivity, or Import/Load this task.

    Using shared Share shell for preview per D-07 (planner-locked title API).
  </action>
  <verify>
    <automated>npx vitest run src/pages/GalleryPage.source.test.ts && npx vitest run src/lib/preferencesCore.test.ts && ! grep -E 'from ["\'].*uiClasses["\']' src/components/ShareModal.tsx && grep -q 'Dialog' src/components/ShareModal.tsx && grep -q 'Preview preset' src/pages/GalleryPage.tsx && grep -Eiq 'key|token|secret|password' src/components/ShareModal.tsx</automated>
  </verify>
  <done>Share + Gallery preview on Dialog with redaction UX and Preview title; handlers stable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ImportPreferences + LoadPreset Dialog restyle (D-05, D-06, D-23)</name>
  <files>src/components/ImportPreferencesModal.tsx, src/components/LoadPresetModal.tsx</files>
  <read_first>
    - src/components/ImportPreferencesModal.tsx (full — pickFile, native picks, handleClose reset, busy, errors)
    - src/components/LoadPresetModal.tsx (full — fetch index, filter, row load, From file, gallery link)
    - src/components/ShareModal.tsx (Task 1 Dialog pattern to mirror)
    - .planning/phases/03-modals-palette-overlays/03-UI-SPEC.md Import + Load inventory + copywriting
    - .planning/phases/03-modals-palette-overlays/03-PATTERNS.md Import + Load sections
  </read_first>
  <behavior>
    - Both modals use Dialog from @/components/ui/dialog and Button from @/components/ui/button; Load uses Input from @/components/ui/input for search
    - Import keeps onImport, variant web/desktop labels, pickPreferencesFile/pickModelsFile/pickSettingsFile, readPreferencesFromFile/readJsonConfigFromFile, busy Importing…, role=alert errors, primary disabled until prefs present
    - Load keeps fetchPresetIndex/fetchPresetMarkdown/loadPreferencesFromText/backend.importPresetDialog/onLoaded, Search presets… placeholder, From file…, Browse full gallery (web)
    - No uiClasses btn/btnPrimary/modalPanel; no hand-rolled window Escape; linear rows (no active scale press theater); left-edge primary + soft wash for focus/active load rows where applicable
    - Import max-w-md; Load max-w-lg max-h-[85vh]
  </behavior>
  <action>
    Restyle Import + Load product modals only (per D-05, D-06, D-23, D-00e, OVL-01).

    1. ImportPreferencesModal: controlled Dialog shell max-w-md; DialogTitle Import preferences; body copy per UI-SPEC web vs desktop; file rows with required/optional markers (required uses primary text); Browse… outline Button; primary default Import into editor (web) / Import into workspace (desktop); Cancel outline; Importing… busy label; errors role=alert soft destructive. Keep handleClose → reset then onClose. Keep native pick hooks and browser pickFile unchanged (D-23). Drop fixed scrim, Escape listener, uiClasses button/modalPanel symbols.
    2. LoadPresetModal: controlled Dialog max-w-lg max-h-[85vh]; title Load preset; description per UI-SPEC; search Input placeholder Search presets… with autoFocus; From file… outline; list rows linear rounded-none min-h-10, quiet tags, Loading… row state, empty catalog vs filtered copy per UI-SPEC; footer Cancel + Browse full gallery link (web) or Community presets meta (desktop). Keep catalog fetch/load/importPresetDialog/onLoaded logic. Prefer left accent + soft wash over scale transforms on rows.
    3. Do not nest Dialog for dirty confirms (host window.confirm stays). Do not touch Submit, Palette, ConfigApp, preferencesCore, or Tauri native dialog implementations.
    4. Run targeted greps + npm test after restyle.

    Behavior-stable handlers per D-00e; Tauri pickers alone per D-23.
  </action>
  <verify>
    <automated>npx vitest run src/lib/preferencesCore.test.ts && npm test && grep -q 'Dialog' src/components/ImportPreferencesModal.tsx && grep -q 'Dialog' src/components/LoadPresetModal.tsx && ! grep -E 'btnPrimary|modalPanel' src/components/ImportPreferencesModal.tsx src/components/LoadPresetModal.tsx && ! grep -n 'addEventListener\(["'\'']keydown' src/components/ImportPreferencesModal.tsx src/components/LoadPresetModal.tsx src/components/ShareModal.tsx</automated>
  </verify>
  <done>Import + Load on Dialog with Button/Input language; domain handlers and native picks preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Share review → clipboard | User-initiated copy of shareable content; must stay redacted upstream |
| Import file pick → prefs state | Untrusted file bytes parsed by existing readers |
| Load gallery fetch → prefs | Remote preset markdown → loadPreferencesFromText |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Information Disclosure | ShareModal redaction UX | high | mitigate | Keep always-visible key/token/secret/password warning + mono review before copy (D-17–D-19); never tooltip-only |
| T-03-05 | Tampering | Share/Import mono/pre content | medium | mitigate | React text children in pre only — no dangerouslySetInnerHTML |
| T-03-06 | Information Disclosure | preferencesCore | high | mitigate | Do not edit redact/buildShareablePreset; preferencesCore.test remains green |
| T-03-07 | Elevation | Import/Load handlers | low | accept | Presentation only; native pick signatures unchanged (D-23) |
</threat_model>

<verification>
- Share/Import/Load source use Dialog + Button; no uiClasses button language
- Gallery Preview preset title wired
- Redaction keywords present on Share
- preferencesCore tests green; npm test green
</verification>

<success_criteria>
- OVL-01 satisfied for Share, Preview, Import, Load
- D-17–D-19 share security UX preserved
- D-04 dismiss paths via Dialog primitive
- No registry/handler redesign
</success_criteria>

<output>
Create `.planning/phases/03-modals-palette-overlays/03-02-SUMMARY.md` when done
</output>
