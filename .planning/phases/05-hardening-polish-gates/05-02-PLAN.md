---
phase: 05-hardening-polish-gates
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/components/sections/agentSettingsEditors.tsx
  - src/components/sections/HooksSection.tsx
  - src/components/sections/WorkspaceSection.tsx
  - src/components/sections/SkillsSection.tsx
  - src/components/sections/McpSection.tsx
  - src/components/sections/RoutingSection.tsx
  - src/components/sections/ExperimentalSection.tsx
  - src/components/sections/AgentSettingsSection.tsx
  - src/components/sections/ModelsSection.tsx
  - src/components/sections/ParallelSection.tsx
  - src/components/sections/VerificationSection.tsx
  - src/components/sections/ContextSection.tsx
  - src/components/sections/UokSection.tsx
  - src/ConfigApp.tsx
autonomous: true
requirements:
  - ISO-05
  - ISO-04
must_haves:
  truths:
    - "Web-visible preference sections migrate residual gsd-* color utilities to semantic tokens (foreground/muted/primary/border/card/destructive) per D-04"
    - "Ad-hoc text-[9–11px] on restyled residual web chrome becomes text-xs (12px) label/meta scale"
    - "Dense residual controls that are real CTAs upgrade toward ≥40px / Button when touched; prefer a11y over compact cosmetics (D-10, D-12)"
    - "ConfigApp web-rendered toolbar/banners already Button — verified zero live className={btn…} on isWeb branches; desktop uiClasses imports retained (D-03)"
    - "SkillsLibrarySection and AgentsLibrarySection left on uiClasses (desktop-only WEB_HIDDEN) — not web purge success criteria (D-03)"
    - "No WEB_HIDDEN_SECTIONS / route / IA changes (ISO-05); FormControls web semantic path not regressed"
  artifacts:
    - path: src/components/sections/agentSettingsEditors.tsx
      provides: "High-density token + control cohesion for agent settings editors"
    - path: src/components/sections/HooksSection.tsx
      provides: "Semantic token residual sweep"
    - path: src/ConfigApp.tsx
      provides: "Verified web Button path; desktop btn branches preserved"
  key_links:
    - from: section residual classNames
      to: "semantic Tailwind tokens"
      via: "UI-SPEC before/after token map"
    - from: ConfigApp isWeb branches
      to: "@/components/ui/button"
      via: "Phase 4 shell — verify only this plan"
    - from: WEB_HIDDEN_SECTIONS
      to: "skills-library + agents-library only"
      via: "ISO-05 freeze — do not edit sectionConfig"
---

<objective>
Sweep remaining **web-visible section chrome** off residual gsd-\* color utilities onto Mist Sky semantic tokens, upgrade dense residual controls for a11y when touched, and **verify ConfigApp web path** has no live uiClasses button application while desktop branches keep uiClasses (per D-03, D-04, D-10–D-12, ISO-05, ISO-04).

Purpose: Full residual color cohesion so the product surface speaks one token system before deleting web button-bridge CSS.

Output: Token-migrated web-visible sections; ConfigApp verify-only confirmation; libraries untouched.
</objective>

## Phase Goal

**As a** web user of GSD Pi Config, **I want to** finish the redesign with cohesive Mist Sky residual chrome, stable domain behavior, and accessible controls, **so that** the milestone closes without product or IA changes.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-hardening-polish-gates/05-01-SUMMARY.md
@.planning/phases/05-hardening-polish-gates/05-CONTEXT.md
@.planning/phases/05-hardening-polish-gates/05-UI-SPEC.md
@.planning/phases/05-hardening-polish-gates/05-RESEARCH.md
@.planning/phases/05-hardening-polish-gates/05-PATTERNS.md
@src/components/FormControls.tsx
@src/ConfigApp.tsx
@src/lib/sectionConfig.ts
@src/components/sections/agentSettingsEditors.tsx
@src/components/sections/HooksSection.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: High-density web section token + control sweep (P1)</name>
  <files>src/components/sections/agentSettingsEditors.tsx, src/components/sections/HooksSection.tsx, src/components/sections/WorkspaceSection.tsx, src/components/sections/SkillsSection.tsx, src/components/sections/McpSection.tsx, src/components/sections/RoutingSection.tsx</files>
  <read_first>
    - src/components/sections/agentSettingsEditors.tsx, HooksSection.tsx, WorkspaceSection.tsx, SkillsSection.tsx, McpSection.tsx, RoutingSection.tsx (gsd-* density)
    - src/components/FormControls.tsx web ModelChain Button/label pattern (analog)
    - .planning/phases/05-hardening-polish-gates/05-UI-SPEC.md token map + type cleanup + radius
    - .planning/phases/05-hardening-polish-gates/05-PATTERNS.md agentSettingsEditors + section sweep
    - .planning/phases/05-hardening-polish-gates/05-RESEARCH.md residual density table + Pitfall 3 shared-file desktop
  </read_first>
  <behavior>
    - Listed P1 files no longer use text-gsd-*/bg-gsd-*/border-gsd-* utilities on residual chrome (or only if an unavoidable desktop-only branch remains explicitly platform-gated)
    - text-[9px]/[10px]/[11px] on restyled residual chrome replaced with text-xs
    - Raw small CTA chips that are real actions become Button size sm when the file already needs control restyle; prefer a11y (D-12)
    - No import of uiClasses btn language introduced
    - Domain onChange paths, validators, and preference field paths unchanged
  </behavior>
  <action>
    Migrate residual presentation on high-density web-visible sections (per D-04, D-10–D-12, ISO-05).

    1. For each listed file, replace gsd-* color utilities with semantic tokens using the UI-SPEC map (text-foreground, text-muted-foreground, text-primary, bg-background, bg-card, border-border, soft destructive, primary soft washes). Success badges: soft success wash — not Mist Sky primary fill, not logo cyan.
    2. Replace ad-hoc text-[9–11px] with text-xs (12px) label/meta roles. Residual cards/panels touched for color: rounded-none.
    3. agentSettingsEditors: if small reorder/remove/add controls are native buttons with compact padding, upgrade to Button ghost/outline size sm (≥40px) following FormControls ModelChain analog — presentation only; preserve reorder/resync semantics.
    4. Shared-file pitfall (RESEARCH): prefer semantic classes acceptable on desktop glance; if a change would force full Mist Sky desktop chrome breakage, branch with isWebPlatform() so web purges and desktop may keep legacy utilities. Web path must not retain residual gsd-* color chrome after this task.
    5. Do not edit SkillsLibrarySection or AgentsLibrarySection. Do not change WEB_HIDDEN_SECTIONS, routes, fields.ts registry semantics, or preferencesCore. Do not regress FormControls web adapters.
    6. Remove active:scale press theater on residual web controls if present.

    ClassName/JSX surgical only — no product copy rewrite beyond type-scale cleanup of existing strings if required for Checking… title-case when already touching that string (UI-SPEC).
  </action>
  <verify>
    <automated>npx vitest run src/lib/phase04.forms.test.ts src/lib/preferencesCore.test.ts && for f in src/components/sections/agentSettingsEditors.tsx src/components/sections/HooksSection.tsx src/components/sections/WorkspaceSection.tsx src/components/sections/SkillsSection.tsx src/components/sections/McpSection.tsx src/components/sections/RoutingSection.tsx; do echo "== $f"; rg -n "text-gsd-|bg-gsd-|border-gsd-" "$f" || true; rg -n "text-\\[9px\\]|text-\\[10px\\]|text-\\[11px\\]" "$f" || true; done; rg -n "text-gsd-|bg-gsd-|border-gsd-" src/components/sections/agentSettingsEditors.tsx src/components/sections/HooksSection.tsx src/components/sections/WorkspaceSection.tsx src/components/sections/SkillsSection.tsx src/components/sections/McpSection.tsx src/components/sections/RoutingSection.tsx ; test $? -ne 0</automated>
  </verify>
  <done>P1 web-visible sections use semantic tokens and 12px type scale; domain wiring intact; libraries untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: P2 section sweep + ConfigApp web path verify (desktop uiClasses kept)</name>
  <files>src/components/sections/ExperimentalSection.tsx, src/components/sections/AgentSettingsSection.tsx, src/components/sections/ModelsSection.tsx, src/components/sections/ParallelSection.tsx, src/components/sections/VerificationSection.tsx, src/components/sections/ContextSection.tsx, src/components/sections/UokSection.tsx, src/ConfigApp.tsx</files>
  <read_first>
    - Listed P2 *Section.tsx files for residual gsd-* utilities
    - src/ConfigApp.tsx isWeb toolbar/banner branches and uiClasses import (~34, ~750–1065)
    - src/lib/sectionConfig.ts WEB_HIDDEN_SECTIONS (freeze — do not edit)
    - .planning/phases/05-hardening-polish-gates/05-UI-SPEC.md ConfigApp row + platform table
    - .planning/phases/05-hardening-polish-gates/05-PATTERNS.md ConfigApp verify-only rules
  </read_first>
  <behavior>
    - P2 web-visible sections free of residual gsd-* color utilities (same token map as Task 1)
    - ConfigApp still imports uiClasses for desktop branches only
    - Every isWeb toolbar/action path uses Button (or non-btn semantic classes); no isWeb branch applies className={btn} / btnPrimary / btnSegment*
    - Desktop !isWeb paths still use btn/btnPrimary/btnSegment* as today (D-03)
    - gsd-field-focus class add/remove for palette jump remains
    - sectionConfig WEB_HIDDEN still only skills-library and agents-library
  </behavior>
  <action>
    Finish residual color cohesion and lock ConfigApp platform split (per D-03, D-04, ISO-05).

    1. Sweep remaining low-density web-visible sections (Experimental, AgentSettings shell, Models, Parallel, Verification, Context, Uok, and any other web-visible *Section.tsx still matching gsd-* color utilities except Skills/Agents libraries). Same token map as Task 1; presentation only.
    2. ConfigApp verify (not a redesign): confirm web toolbar Import/Share/Download/Discard/Save/Dismiss already use Button under isWeb. Grep for className={btn…} / btnPrimary under isWeb true paths — there must be zero. Leave desktop scope segments, browse project, update install banner, and non-web buttons on uiClasses.
    3. Do not “simplify” by removing desktop uiClasses import or desktop branches. Do not forbid ConfigApp btn imports in future residual tests (RESEARCH open Q2 resolution: ConfigApp allowed).
    4. Keep palette field-jump: classList add/remove of the existing field-focus helper class on data-field-path elements (S9). Do not delete that behavior.
    5. Do not edit sectionConfig.ts, App.web routes, preferencesCore, backends, or desktop CSS. Do not restyle SkillsLibrarySection/AgentsLibrarySection.

    If any web-visible residual still imports uiClasses btn symbols after Plan 01+02, purge it here (D-01 completion).
  </action>
  <verify>
    <automated>npx vitest run src/lib/phase04.forms.test.ts src/lib/preferencesCore.test.ts && rg -n "isWeb \\? \\(" src/ConfigApp.tsx | head -5 && rg -n "from ['\"].*uiClasses" src/ConfigApp.tsx && rg -n "WEB_HIDDEN_SECTIONS|skills-library|agents-library" src/lib/sectionConfig.ts && rg -n "gsd-field-focus" src/ConfigApp.tsx && rg -n "text-gsd-|bg-gsd-|border-gsd-" src/components/sections/ExperimentalSection.tsx src/components/sections/AgentSettingsSection.tsx src/components/sections/ModelsSection.tsx src/components/sections/ParallelSection.tsx src/components/sections/VerificationSection.tsx src/components/sections/ContextSection.tsx src/components/sections/UokSection.tsx ; test $? -ne 0 && node -e "
const fs=require('fs');
const s=fs.readFileSync('src/ConfigApp.tsx','utf8');
// crude: ensure file still has desktop btn usage AND Button import for web
if(!/from ['\\\"].*uiClasses['\\\"]/.test(s)) { console.error('missing desktop uiClasses import'); process.exit(1);} 
if(!/from ['\\\"]@\\/components\\/ui\\/button['\\\"]/.test(s) && !/from ['\\\"].*ui\\/button['\\\"]/.test(s)) { console.error('missing Button import'); process.exit(1);} 
console.log('ConfigApp dual presentation markers present');
"</automated>
  </verify>
  <done>P2 sections token-migrated; ConfigApp web uses Button only; desktop uiClasses retained; WEB_HIDDEN and field-focus intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shared section TSX → web + desktop render | Semantic class migration can affect desktop glance visuals |
| ConfigApp presentation → save/download/share handlers | Verify-only must not rewrite enablement predicates |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Tampering | Shared section class migration | medium | mitigate | Surgical classNames only; no handler rewrites; phase04 + preferencesCore tests |
| T-05-06 | Information Disclosure | ConfigApp share/download paths | high | mitigate | Do not edit share/redact/download handler bodies; presentation verify only |
| T-05-07 | Spoofing | sectionConfig visibility | medium | mitigate | Freeze WEB_HIDDEN_SECTIONS — no edits this plan |
| T-05-SC | Tampering | npm installs | high | mitigate | No package installs; reuse existing primitives |
</threat_model>

<verification>
- P1/P2 web-visible sections free of residual gsd-* color utilities (libraries excluded)
- ConfigApp dual path intact (web Button, desktop uiClasses)
- phase04.forms + preferencesCore green
- sectionConfig unchanged
</verification>

<success_criteria>
- D-04 residual web color migration complete on preference sections
- D-03 desktop isolation branches preserved in ConfigApp + libraries
- ISO-05 no IA/route/capability changes
- Residual a11y upgrades applied where dense CTAs were touched (D-10/D-12)
</success_criteria>

<output>
Create `.planning/phases/05-hardening-polish-gates/05-02-SUMMARY.md` when done
</output>
