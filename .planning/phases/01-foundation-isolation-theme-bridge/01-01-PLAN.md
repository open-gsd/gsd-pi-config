---
phase: 01-foundation-isolation-theme-bridge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tsconfig.json
  - vite.config.ts
  - src/lib/utils.ts
  - src/lib/utils.test.ts
  - src/lib/theme.ts
  - src/lib/theme.test.ts
autonomous: true
requirements:
  - FND-01
  - THM-02
  - THM-03
must_haves:
  truths:
    - "cn merges conflicting Tailwind utilities (later classes win) and drops falsy conditionals"
    - "@/* path alias resolves TypeScript imports under src/"
    - "applyTheme('dark') sets html data-theme=dark AND classList contains dark"
    - "applyTheme('light') sets data-theme=light AND removes class dark"
    - "resolveTheme preserves dark/light prefs and resolves system via matchMedia"
    - "getStoredTheme allowlists system|dark|light only; invalid → system"
    - "Theme storage key remains gsd-pi-config.theme (single authority)"
    - "bootstrapTheme API still applies resolved preference without flash path change"
    - "No next-themes, second ThemeProvider, or second storage key introduced"
    - "ThemeToggle labels/ARIA remain Auto/Dark/Light radiogroup (behavior preserve)"
  artifacts:
    - path: src/lib/utils.ts
      provides: "export function cn(...inputs: ClassValue[])"
      contains: "twMerge"
    - path: src/lib/utils.test.ts
      provides: "Wave 0 cn unit tests"
    - path: src/lib/theme.test.ts
      provides: "Wave 0 dual-write + resolveTheme unit tests"
    - path: tsconfig.json
      provides: "baseUrl + paths @/*"
    - path: vite.config.ts
      provides: "resolve.alias @ → src"
  key_links:
    - from: src/lib/theme.ts applyTheme
      to: document.documentElement dataset.theme + classList.dark
      via: "dual-write in single function"
    - from: useTheme / bootstrapTheme
      to: applyTheme
      via: "existing call sites — dual-write rides them"
    - from: tsconfig paths
      to: vite resolve.alias
      via: "both must map @ → ./src for CLI and tsc"
---

<objective>
Deliver the walking-skeleton **theme bridge + developer utilities**: failing Wave 0 tests first, then `@/*` aliases + `cn`, then dual-write `applyTheme` so Auto/Dark/Light keeps working while shadcn `.dark` stays in sync (per THM-02, THM-03; FND-01 partial for aliases/`cn`).

Purpose: Unblock all later shadcn work with a single theme authority and import path contract — without touching CSS entries, FormControls, or product routes.

Output: Green `src/lib/utils.test.ts` + `src/lib/theme.test.ts`; `src/lib/utils.ts`; updated `theme.ts` dual-write; `@` alias in tsconfig + Vite.
</objective>

## Phase Goal

**As a** dual-platform GSD Pi Config maintainer, **I want to** dual-write theme preference onto both GSD and shadcn dark-mode signals with a working `cn`/`@/*` foundation, **so that** later web restyles flip tokens correctly without a second theme system.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-isolation-theme-bridge/01-RESEARCH.md
@.planning/phases/01-foundation-isolation-theme-bridge/01-UI-SPEC.md
@.planning/phases/01-foundation-isolation-theme-bridge/01-VALIDATION.md
@.planning/phases/01-foundation-isolation-theme-bridge/01-PATTERNS.md
@.planning/phases/01-foundation-isolation-theme-bridge/01-SKELETON.md
@src/lib/theme.ts
@src/lib/preferencesCore.test.ts
@vite.config.ts
@tsconfig.json
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 failing tests for cn and theme dual-write</name>
  <files>src/lib/utils.test.ts, src/lib/theme.test.ts</files>
  <read_first>
    - src/lib/preferencesCore.test.ts (Vitest describe/it/expect style, co-located *.test.ts)
    - src/lib/theme.ts (current applyTheme only sets dataset.theme; STORAGE_KEY; resolveTheme)
    - .planning/phases/01-foundation-isolation-theme-bridge/01-VALIDATION.md (Wave 0 requirements)
    - vite.config.ts test block (environment: node, include src/**/*.test.ts only — no *.test.tsx)
  </read_first>
  <behavior>
    - cn("p-2", "p-4") → string containing p-4 and not conflicting p-2
    - cn("foo", false && "bar", undefined, "baz") → includes foo and baz only
    - resolveTheme("dark"|"light") returns same; resolveTheme("system") uses matchMedia mock
    - applyTheme("dark") sets dataset.theme === "dark" and classList contains "dark"
    - applyTheme("light") sets dataset.theme === "light" and removes "dark"
    - getStoredTheme: invalid localStorage value → "system"; allowlisted values returned
  </behavior>
  <action>
    Create Wave 0 unit tests only (no production implementation yet) so the next tasks turn RED→GREEN.

    1. Create `src/lib/utils.test.ts` importing `cn` from `./utils`. Cover merge of conflicting Tailwind padding utilities and falsy conditional inputs. Tests will fail until Task 2 adds `utils.ts` and deps.
    2. Create `src/lib/theme.test.ts` importing `applyTheme`, `resolveTheme`, `getStoredTheme` from `./theme`. In node env, mock a minimal `document.documentElement` with `dataset` and a classList that supports `toggle`/`contains`/`remove` (or real DOM classList via a tiny stub object). Mock `window.matchMedia` for system resolution. Do not install jsdom/happy-dom.
    3. Do not expand Vitest `include` to `*.test.tsx`. Do not edit `theme.ts` or create `utils.ts` in this task.
    4. Run tests once to confirm RED (import/missing dual-write failures expected).

    After tests exist: commit intent is RED for dual-write assertions that current applyTheme cannot pass.
  </action>
  <verify>
    <automated>npx vitest run src/lib/utils.test.ts src/lib/theme.test.ts ; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - Files `src/lib/utils.test.ts` and `src/lib/theme.test.ts` exist
    - Each file uses vitest `describe`/`it`/`expect`
    - theme tests assert both `dataset.theme` and class `dark` for dual-write
    - Running the two files fails (RED) before Tasks 2–3 implement production code
    - No `*.test.tsx` added; vitest include unchanged
  </acceptance_criteria>
  <done>Wave 0 tests committed in RED state covering cn + THM-02/THM-03 dual-write contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Path aliases + cn util (FND-01 partial)</name>
  <files>tsconfig.json, vite.config.ts, src/lib/utils.ts, package.json, package-lock.json</files>
  <read_first>
    - tsconfig.json (single config — no tsconfig.app.json split)
    - vite.config.ts (preserve dual-mode isWeb, define VITE_PLATFORM, test block, ports)
    - .planning/phases/01-foundation-isolation-theme-bridge/01-RESEARCH.md Pattern 1 + cn helper
    - .planning/phases/01-foundation-isolation-theme-bridge/01-PATTERNS.md utils.ts / vite alias sections
    - package.json (no clsx/tailwind-merge yet)
  </read_first>
  <behavior>
    - cn merges classes per Task 1 utils tests
    - TypeScript resolves @/lib/utils via paths
  </behavior>
  <action>
    Implement the FND-01 alias + `cn` half of the foundation (deps for cn only — not full shadcn install yet).

    1. `npm install clsx tailwind-merge` (versions compatible with RESEARCH: clsx ~2.1.x, tailwind-merge 3.x for TW4). Do not install lucide-react, cva, shadcn, or @base-ui/react in this plan.
    2. Create `src/lib/utils.ts` with named export `cn` using `clsx` + `twMerge` (RESEARCH code example). Match file header copyright style of `theme.ts`.
    3. Edit `tsconfig.json` compilerOptions: add `"baseUrl": "."` and `"paths": { "@/*": ["./src/*"] }`. Do not invent a second tsconfig.
    4. Edit `vite.config.ts`: import `path` from `node:path` (or `path`); add `resolve.alias: { "@": path.resolve(__dirname, "./src") }` inside the returned config. Preserve plugins, define, server, test, build, clearScreen, base. Do **not** add `@platform-css` yet (Plan 02 owns CSS isolation wiring).
    5. Run utils tests until GREEN. Existing `preferencesCore.test.ts` must remain green.

    Per FND-01: aliases + cn only this task — Button and components.json are Plan 03.
  </action>
  <verify>
    <automated>npx vitest run src/lib/utils.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/utils.ts` exports `cn`
    - `tsconfig.json` has baseUrl `.` and paths `@/*` → `./src/*`
    - `vite.config.ts` has resolve.alias `@` → src (grep-able)
    - package.json lists clsx and tailwind-merge
    - `npx vitest run src/lib/utils.test.ts` exits 0
    - `npm test` still passes preferencesCore tests
    - No `src/components/ui/` created yet
  </acceptance_criteria>
  <done>cn + @ alias work; utils Wave 0 tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Theme dual-write data-theme + .dark (THM-02, THM-03)</name>
  <files>src/lib/theme.ts</files>
  <read_first>
    - src/lib/theme.ts (full file — STORAGE_KEY, getStoredTheme, resolveTheme, applyTheme, useTheme, bootstrapTheme)
    - src/lib/theme.test.ts (from Task 1)
    - .planning/phases/01-foundation-isolation-theme-bridge/01-UI-SPEC.md Theme Toggle Visual Contract + Dual-write application
    - src/components/ThemeToggle.tsx (confirm labels Auto/Dark/Light — do not restyle)
    - src/main.tsx (bootstrapTheme before React — do not reorder this plan)
  </read_first>
  <behavior>
    - applyTheme dual-write per Task 1 theme tests
    - resolveTheme / getStoredTheme behavior unchanged except dual-write surface
  </behavior>
  <action>
    Extend theme application only — preserve API, storage key, and boot contract (THM-02, THM-03).

    1. Update `applyTheme(effective)` so when `document` exists:
       - set `document.documentElement.dataset.theme = effective` (keep GSD/desktop CSS)
       - `classList.toggle("dark", effective === "dark")` for shadcn
       - optionally `classList.toggle("light", effective === "light")` for docs parity (allowed, not required)
    2. Keep `STORAGE_KEY = "gsd-pi-config.theme"`. Do not add next-themes or ThemeProvider. Do not change `useTheme` public return shape. Dual-write must ride existing useEffect + bootstrapTheme paths.
    3. Do not edit ThemeToggle visuals (THM-04 is Phase 2). Do not touch FormControls, ConfigApp, share/redact, or CSS files.
    4. Run theme tests until GREEN; full `npm test` green.

    Using existing theme.ts as single authority per user/project decision (research also recommended dual-write over data-theme-only custom variant).
  </action>
  <verify>
    <automated>npx vitest run src/lib/theme.test.ts src/lib/utils.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - applyTheme dark: dataset.theme is dark AND classList has dark
    - applyTheme light: dataset.theme is light AND classList does not have dark
    - STORAGE_KEY string remains gsd-pi-config.theme
    - No import of next-themes anywhere
    - ThemeToggle.tsx file content unchanged this plan
    - `npx vitest run src/lib/theme.test.ts` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <done>Theme dual-write green; Auto/Dark/Light semantics preserved with shadcn class sync.</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol / file | Plan | Notes |
|---------------|------|-------|
| `cn` / `src/lib/utils.ts` | 01 | Required by all ui/* |
| `src/lib/utils.test.ts` | 01 | Wave 0 |
| Dual-write `applyTheme` | 01 | THM-03 |
| `src/lib/theme.test.ts` | 01 | Wave 0 |
| `@/*` tsconfig + Vite alias | 01 | FND-01 |
| `src/index.web.css` | 02 | Web tokens |
| `src/index.desktop.css` | 02 | Legacy gsd-* |
| `@platform-css` alias | 02 | Isolation |
| Semantic tokens THM-01 | 02 | Web only |
| `components.json` | 03 | Locked base-nova/neutral |
| `src/components/ui/button.tsx` | 03 | Only day-one primitive |
| `01-SKELETON.md` | planning | Walking skeleton contract |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → theme.ts | Untrusted preference string; must allowlist system\|dark\|light |
| npm install (later plans) | Supply chain — not in this plan's install set beyond clsx/tailwind-merge |
| Theme DOM writes | Browser documentElement only; no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | getStoredTheme localStorage | low | mitigate | Allowlist only system\|dark\|light; invalid → system (existing + tests) |
| T-01-02 | Information Disclosure | theme.ts / this plan | low | mitigate | Do not log storage contents; do not touch keys store or share/redact paths |
| T-01-03 | Elevation | N/A this plan | low | accept | No auth/session changes in foundation utilities |
| T-01-SC | Tampering | clsx / tailwind-merge npm | low | mitigate | Install only mature OK packages from RESEARCH legitimacy table; review lockfile diff |
</threat_model>

<verification>
- `npx vitest run src/lib/utils.test.ts src/lib/theme.test.ts` green
- `npm test` green including preferencesCore
- Grep: applyTheme toggles class dark; STORAGE_KEY unchanged
</verification>

<success_criteria>
- Wave 0 theme + utils tests green (THM-02, THM-03, FND-01 cn/aliases)
- Dual-write works without second theme library
- No CSS split yet (Plan 02); no Button yet (Plan 03)
</success_criteria>

<output>
Create `.planning/phases/01-foundation-isolation-theme-bridge/01-01-SUMMARY.md` when done
</output>
