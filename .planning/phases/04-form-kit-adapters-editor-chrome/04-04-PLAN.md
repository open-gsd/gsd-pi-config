---
phase: 04-form-kit-adapters-editor-chrome
plan: 04
type: execute
wave: 4
depends_on:
  - "03"
files_modified:
  - src/components/Sidebar.tsx
  - src/ConfigApp.tsx
autonomous: true
requirements:
  - WEB-04
  - FRM-04
must_haves:
  truths:
    - "Sidebar web: linear list, left-edge active (primary 2–3px + soft wash), uppercase muted group labels, 4px dirty dots, no backdrop blur (D-13)"
    - "SECTION_GROUPS / section visibility / dirtySections membership unchanged"
    - "ConfigApp web toolbar uses Button language (primary Save/Download when enabled; outline secondaries); enablement predicates unchanged (D-14, FRM-04)"
    - "useDirty, anyDirty, webWorkspaceReady, save/download handlers, import/load/share/submit openers, scope semantics unchanged (FRM-04)"
    - "Error banner quiet soft-danger with role=alert and Dismiss title case (D-15)"
    - "Mobile drawer keeps useSidebarDrawerLayout + scrim dismiss; panel restyled only (D-16)"
    - "data-field-path pendingFocus query still works"
  artifacts:
    - path: src/components/Sidebar.tsx
      provides: "Mist Sky linear sidebar chrome (web)"
    - path: src/ConfigApp.tsx
      provides: "Web toolbar Button language + quiet banners; FRM-04 handlers intact"
  key_links:
    - from: dirtySections
      to: Sidebar dirty dots
      via: "useDirty → Set membership unchanged"
    - from: Save/Download disabled predicate
      to: anyDirty / webWorkspaceReady / needsProjectSelection / status
      via: "FRM-04 verbatim enablement"
    - from: toolbar openers
      to: Phase 3 exclusive overlay helpers
      via: "presentation-only Button wrap"
---

<objective>
Restyle **Sidebar** and **ConfigApp** loaded-editor chrome (toolbar, banners, mobile drawer panel) on web without changing dirty/save/import/download/scope semantics (per D-13–D-16, WEB-04, FRM-04).

Purpose: Cloud editor route shell matches Mist Sky end-to-end after form kit.

Output: Updated `Sidebar.tsx` + `ConfigApp.tsx` presentation; handlers stable.
</objective>

## Phase Goal

**As a** web user of GSD Pi Config, **I want to** edit preferences with Mist Sky form controls and a restyled editor shell, **so that** the cloud editor looks cohesive without changing dirty, save, download, or domain behavior.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-form-kit-adapters-editor-chrome/04-03-SUMMARY.md
@.planning/phases/04-form-kit-adapters-editor-chrome/04-CONTEXT.md
@.planning/phases/04-form-kit-adapters-editor-chrome/04-UI-SPEC.md
@.planning/phases/04-form-kit-adapters-editor-chrome/04-RESEARCH.md
@.planning/phases/04-form-kit-adapters-editor-chrome/04-PATTERNS.md
@src/components/Sidebar.tsx
@src/ConfigApp.tsx
@src/components/ui/button.tsx
@src/hooks/useDirty.ts
@.planning/phases/03-modals-palette-overlays/03-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sidebar linear left-edge active + type scale (D-13)</name>
  <files>src/components/Sidebar.tsx</files>
  <read_first>
    - src/components/Sidebar.tsx (SECTION_GROUPS, web header Sections kicker, gsd-nav-item, dirty h-1.5, backdrop-blur)
    - .planning/phases/04-form-kit-adapters-editor-chrome/04-UI-SPEC.md Sidebar + dirty-dot 4px
    - .planning/phases/04-form-kit-adapters-editor-chrome/04-PATTERNS.md Sidebar section
  </read_first>
  <behavior>
    - SECTION_GROUPS / SECTIONS / sectionLabel / props API unchanged
    - Web surface: secondary/card, border-r border-border, no backdrop-blur
    - Web kicker Sections: text-xs font-semibold uppercase tracking-wider muted (not text-[10px])
    - Group labels: 12px uppercase muted
    - Items: min-h-10; active left-edge primary 2–3px + soft wash + font-semibold; idle muted
    - Dirty dot: h-1 w-1 (4px) bg-primary; aria-label Unsaved changes
    - Desktop BrandMark block + legacy nav classes OK
  </behavior>
  <action>
    Restyle Sidebar chrome (per D-13, WEB-04).

    1. Keep SECTION_GROUPS data, filter consumers, dirtySections Set checks, footerLink, variant web/desktop header split, onNavigate, aria-label Settings sections on web.
    2. Web classes: drop backdrop-blur-sm; use border-border / bg-card or secondary surface; replace gsd-nav-item-active fill with border-l-[3px] border-l-primary bg-primary/10 font-semibold (or equivalent left-edge 2–3px); idle text-muted-foreground hover:bg-muted.
    3. Replace tiny type utilities (text-[9px]/text-[10px]) on web with text-xs (12px) Label role.
    4. Dirty indicator: h-1 w-1 rounded-full bg-primary (not h-1.5); keep Unsaved changes aria-label.
    5. Desktop: may keep BrandMark + gsd-nav-item* for ISO.
    6. Do not change section IA or which sections appear on web.

    Linear left-edge active per D-13.
  </action>
  <verify>
    <automated>npx tsc --noEmit && rg -n 'SECTION_GROUPS|border-l|Unsaved changes|backdrop-blur|h-1 w-1|Sections' src/components/Sidebar.tsx | head -40</automated>
  </verify>
  <done>Sidebar web linear active edge + 4px dirty dots; data contracts intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ConfigApp web toolbar Button language + banners + drawer panel (D-14, D-15, D-16, FRM-04)</name>
  <files>src/ConfigApp.tsx</files>
  <read_first>
    - src/ConfigApp.tsx (toolbar buttons ~753–960, error banner ~985–990, sidebarDrawer host, useDirty, save disabled predicate, pendingFocus data-field-path, exclusive openers)
    - src/components/ui/button.tsx (variants)
    - .planning/phases/04-form-kit-adapters-editor-chrome/04-UI-SPEC.md Toolbar CTA rules + banners + copy
    - .planning/phases/04-form-kit-adapters-editor-chrome/04-PATTERNS.md ConfigApp section
    - .planning/phases/04-form-kit-adapters-editor-chrome/04-RESEARCH.md Pitfall 6 enablement
  </read_first>
  <behavior>
    - useDirty(prefs, originalPrefs) still used; anyDirty / webWorkspaceReady / needsProjectSelection formulas unchanged
    - Save/Download disabled still: status saving OR needsProjectSelection OR (web ? !webWorkspaceReady : !anyDirty)
    - Primary visual still gated by (web ? webWorkspaceReady : anyDirty) && !needsProjectSelection
    - Web toolbar actions use Button from ui/button: outline secondaries; default primary when enabled; ghost banner dismiss
    - Labels Saving…/Downloading…/Saved/Downloaded/Save/Download/Import/Load preset/Share/Export/Submit/Discard/Sections unchanged in meaning
    - Error banner role=alert; Dismiss title case; quiet soft-danger wash
    - Drawer: useSidebarDrawerLayout behavior + bg-black/50 scrim; Sidebar panel only restyled (Task 1)
    - pendingFocus still queries data-field-path; exclusive overlay helpers stay
    - Desktop may keep btn/btnPrimary for non-web branches and update banner
  </behavior>
  <action>
    Restyle ConfigApp shell presentation only (per D-14, D-15, D-16, FRM-04, WEB-04).

    1. Import Button from @/components/ui/button. Prefer isWeb branches: web toolbar uses Button variants; desktop may keep uiClasses btn/btnPrimary (D-02).
    2. Map web actions: Sections outline; Import/Load preset/Share/Export/Submit/Discard outline; Save/Download default when enablement true else outline+disabled. Copy predicates VERBATIM — do not refactor enablement logic while changing classNames (Pitfall 6).
    3. Status labels remain on the primary button (Saving…/Saved/Downloading…/Downloaded) as today.
    4. Error banner: quiet soft-danger bar (bg-destructive/10 border-destructive/30 text-xs); role=alert; Button ghost sm Dismiss (title case, not lowercase dismiss). Keep host error string.
    5. Mobile drawer host: keep useSidebarDrawerLayout, slide transform, scrim bg-black/50, aria-expanded on Sections. Do not change drawer open logic.
    6. Do not change: useDirty, save()/download packaging, import/load/share/submit handlers, scope/project selection, overlay exclusivity helpers, preferencesCore, confirm dirty dialogs, ApiKeys section internals.
    7. Desktop update banner / ThemeToggle / scope segment: behavior stable; visual restyle not required for desktop success.

    FRM-04 hard constraint: presentation only.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test && rg -n 'useDirty|webWorkspaceReady|anyDirty|from \"@/components/ui/button\"|Dismiss|role=\"alert\"' src/ConfigApp.tsx | head -50</automated>
  </verify>
  <done>Web toolbar Button language + quiet Dismiss banner; FRM-04 enablement and handlers unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Toolbar actions → save/download/share | UI chrome must not alter secret redaction or file outputs |
| Error banner → user | Error strings may include paths; no secret logging |
| Overlay openers | Phase 3 exclusivity must remain |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-10 | Tampering | Save/Download enablement | high | mitigate | Copy predicates verbatim; FRM-04 source contracts in Plan 05 |
| T-04-11 | Information Disclosure | Share/Export/Submit openers | high | mitigate | Do not touch redaction/scan/OAuth handlers; keep phase03 + preferencesCore green |
| T-04-12 | Spoofing | Error banner | low | mitigate | Keep role=alert; visible Dismiss; no toast-only failures |
| T-04-SC | Tampering | no new installs | low | accept | Button already installed |
</threat_model>

<verification>
- tsc + npm test green
- useDirty and enablement keywords still present
- Web Button import present; Sidebar left-edge language present
- Overlay exclusivity and preferencesCore still green via full suite
</verification>

<success_criteria>
- WEB-04 editor shell restyled on web
- FRM-04 dirty/save/import/download/scope semantics unchanged
- D-13–D-16 satisfied
</success_criteria>

<output>
Create `.planning/phases/04-form-kit-adapters-editor-chrome/04-04-SUMMARY.md` when done
</output>
