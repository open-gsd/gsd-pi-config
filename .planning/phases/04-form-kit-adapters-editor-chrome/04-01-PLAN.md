---
phase: 04-form-kit-adapters-editor-chrome
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/foundation.isolation.test.ts
  - src/components/ui/switch.tsx
  - src/components/ui/select.tsx
  - src/components/ui/checkbox.tsx
  - src/components/ui/popover.tsx
  - src/components/ui/switch.import.test.ts
  - src/components/ui/select.import.test.ts
  - src/components/ui/checkbox.import.test.ts
  - src/components/ui/popover.import.test.ts
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - FRM-01
must_haves:
  truths:
    - "Official base-nova Switch, Select, Checkbox, and Popover primitives exist under src/components/ui (Base UI only — never product Radix)"
    - "FND-03 allowlist requires switch/select/checkbox/popover (+ import tests); card/sheet/drawer/alert-dialog still forbidden"
    - "Phase 3 forbid of select/popover is flipped — those basenames are required, not forbidden"
    - "Mist Sky linear overrides applied: radius 0 on select/popover/checkbox chrome; Select trigger min-h 40; Switch capsule allowed as sole non-square exception (h-5 w-9 preferred)"
    - "Import-only tests for all four primitives pass without DOM render"
    - "No FormControls or ConfigApp product restyle in this plan — Plans 02–05 consume primitives"
    - "No new runtime npm deps expected; @base-ui/react already present"
  artifacts:
    - path: src/components/ui/switch.tsx
      provides: "shadcn Switch primitive (Base UI)"
      contains: "@base-ui/react"
    - path: src/components/ui/select.tsx
      provides: "shadcn Select compound primitive (Base UI)"
    - path: src/components/ui/checkbox.tsx
      provides: "shadcn Checkbox primitive (Base UI)"
    - path: src/components/ui/popover.tsx
      provides: "shadcn Popover primitive (Base UI)"
    - path: src/components/ui/switch.import.test.ts
      provides: "Import-only Switch proof"
    - path: src/components/ui/select.import.test.ts
      provides: "Import-only Select proof"
    - path: src/components/ui/checkbox.import.test.ts
      provides: "Import-only Checkbox proof"
    - path: src/components/ui/popover.import.test.ts
      provides: "Import-only Popover proof"
    - path: src/lib/foundation.isolation.test.ts
      provides: "FND-03 Phase 4 allowlist + required primitives"
  key_links:
    - from: components.json
      to: "npx shadcn@4.13.1 add switch select checkbox popover -y"
      via: "locked base-nova / css → index.web.css"
    - from: foundation.isolation UI_ALLOWLIST
      to: ui/*.tsx basenames
      via: "FND-03 Phase 4 expand; remove select/popover from FORBIDDEN_DUMP"
    - from: select/switch/checkbox/popover defaults
      to: Mist Sky linear grammar
      via: "rounded-none + min-h-10 overrides; Switch capsule exception only"
---

<objective>
Install official **Switch + Select + Checkbox + Popover** via pinned `shadcn@4.13.1`, apply Mist Sky form overrides, and expand FND-03 allowlist so later plans can restyle FormControls (per D-05–D-08, FRM-01, FND-03).

Purpose: Primitives-first cutover — isolation and CLI install land together so CI never forbids the files we just added.

Output: `switch.tsx`, `select.tsx`, `checkbox.tsx`, `popover.tsx`, four import tests, expanded isolation allowlist.
</objective>

## Phase Goal

**As a** web user of GSD Pi Config, **I want to** edit preferences with Mist Sky form controls and a restyled editor shell, **so that** the cloud editor looks cohesive without changing dirty, save, download, or domain behavior.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/design/PALETTE.md
@.planning/phases/04-form-kit-adapters-editor-chrome/04-CONTEXT.md
@.planning/phases/04-form-kit-adapters-editor-chrome/04-UI-SPEC.md
@.planning/phases/04-form-kit-adapters-editor-chrome/04-RESEARCH.md
@.planning/phases/04-form-kit-adapters-editor-chrome/04-PATTERNS.md
@.planning/phases/04-form-kit-adapters-editor-chrome/04-VALIDATION.md
@.planning/phases/03-modals-palette-overlays/03-01-SUMMARY.md
@components.json
@src/components/ui/button.tsx
@src/components/ui/input.tsx
@src/components/ui/dialog.import.test.ts
@src/lib/foundation.isolation.test.ts
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand FND-03 allowlist for Switch/Select/Checkbox/Popover — expect RED until install</name>
  <files>src/lib/foundation.isolation.test.ts</files>
  <read_first>
    - src/lib/foundation.isolation.test.ts (UI_ALLOWLIST, REQUIRED_PHASE3, FORBIDDEN_DUMP, bridge test)
    - .planning/phases/04-form-kit-adapters-editor-chrome/04-RESEARCH.md Pattern 7 FND-03 + Package Legitimacy Audit
    - .planning/phases/04-form-kit-adapters-editor-chrome/04-PATTERNS.md foundation.isolation section
    - .planning/phases/04-form-kit-adapters-editor-chrome/04-VALIDATION.md Wave 0 requirements
  </read_first>
  <behavior>
    - UI_ALLOWLIST includes switch.tsx, select.tsx, checkbox.tsx, popover.tsx and matching *.import.test.ts basenames
    - Presence required: switch.tsx, select.tsx, checkbox.tsx, popover.tsx under src/components/ui
    - FORBIDDEN_DUMP no longer includes select or popover; still forbids card, sheet, drawer, alert-dialog (and similar dump peers)
    - Phase 3 required dialog/command/input-group presence remains
    - Desktop isolation and Button Base-UI-only asserts remain green-capable
    - Do not yet change .gsd-btn bridge expectation (toolbar migration is Plan 04/05)
  </behavior>
  <action>
    Update isolation tests only (no CLI install in this task) so Task 2 turns RED→GREEN for Phase 4 primitives (per FND-03, FRM-01).

    1. Expand UI_ALLOWLIST with: switch.tsx, switch.import.test.ts, select.tsx, select.import.test.ts, checkbox.tsx, checkbox.import.test.ts, popover.tsx, popover.import.test.ts (keep all Phase 1–3 entries).
    2. Add REQUIRED_PHASE4 (or extend the required-presence test) for switch.tsx, select.tsx, checkbox.tsx, popover.tsx.
    3. Remove select and popover from FORBIDDEN_DUMP. Keep forbidding card, sheet, drawer, alert-dialog (add sonner/tooltip if listed). Do not forbid Phase 4 targets.
    4. Keep components.json base-nova lock, desktop no-shadcn stack, Button no product Radix import asserts.
    5. Leave the web .gsd-btn bridge requirement intact until Plan 04/05 drops toolbar bridge usage.
    6. Do not run shadcn add yet. Do not edit FormControls, ConfigApp, Sidebar, preferencesCore, or desktop CSS.
    7. Run isolation once — expect RED on missing switch/select/checkbox/popover files until Task 2.

    Cite FND-03 expand for FRM-01 peers only; no Alert/Card/Sheet dump.
  </action>
  <verify>
    <automated>npx vitest run src/lib/foundation.isolation.test.ts ; test $? -ne 0</automated>
  </verify>
  <done>FND-03 contracts require switch/select/checkbox/popover and forbid dump; tests RED until CLI install lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install four form primitives via pinned CLI + Mist Sky overrides + import tests</name>
  <files>src/components/ui/switch.tsx, src/components/ui/select.tsx, src/components/ui/checkbox.tsx, src/components/ui/popover.tsx, src/components/ui/switch.import.test.ts, src/components/ui/select.import.test.ts, src/components/ui/checkbox.import.test.ts, src/components/ui/popover.import.test.ts, package.json, package-lock.json, src/lib/foundation.isolation.test.ts</files>
  <read_first>
    - components.json (style base-nova, css path index.web.css, aliases)
    - src/components/ui/button.tsx and input.tsx (Mist Sky linear min-h-10 rounded-none pattern)
    - src/components/ui/dialog.import.test.ts (import-only + Base UI + no Radix pattern)
    - .planning/phases/04-form-kit-adapters-editor-chrome/04-RESEARCH.md Standard Stack install + post-install override table + Package Legitimacy
    - .planning/phases/04-form-kit-adapters-editor-chrome/04-UI-SPEC.md Design System + Switch radius exception
    - package.json (existing @base-ui/react, shadcn pin)
  </read_first>
  <behavior>
    - Same as Task 1 isolation asserts — all GREEN after install
    - switch/select/checkbox/popover import as named exports without throwing
    - Sources use @base-ui/react/* modules; no product @radix-ui/ imports
    - Select trigger/content/item and Popover content use rounded-none; Select trigger min-h-10 / h-10 language
    - Switch may keep capsule track geometry; prefer h-5 w-9 sizing
    - No card.tsx, sheet.tsx, drawer.tsx, alert-dialog.tsx under ui/
    - No unexpected new runtime dependency beyond what CLI legitimately needs (prefer zero new deps)
  </behavior>
  <action>
    Install and lock primitives (per D-05–D-08, FRM-01, UI-SPEC registry policy).

    1. Run exactly: npx shadcn@4.13.1 add switch select checkbox popover -y (official registry only). Expect four component files. Do NOT add alert, card, sheet, sonner, tooltip, or --all. Do NOT change components.json style away from base-nova.
    2. Immediately override registry defaults to Mist Sky / UI-SPEC (D-00a): SelectTrigger → h-10 min-h-10 rounded-none w-full capable; SelectContent/Item → rounded-none; PopoverContent → rounded-none, flat (no glass blur); Checkbox → prefer square/near-square; Switch → prefer h-5 w-9, capsule allowed as sole non-square exception. Mirror Input/Button focus-visible ring language where applicable; keep aria-invalid destructive border path on select/input-like triggers when present.
    3. Keep Base UI imports only — never introduce product @radix-ui/* imports in the four files.
    4. Add import-only tests mirroring dialog.import.test.ts: switch.import.test.ts, select.import.test.ts, checkbox.import.test.ts, popover.import.test.ts — export callable/object components; source asserts for @base-ui/react, no @radix-ui/, rounded-none where required (Switch capsule exception: do not force rounded-none on switch track if capsule is intentional).
    5. If CLI omitted a peer already covered by @base-ui/react, hand-install only that peer (Phase 1 lesson). Prefer no new packages. npm ls @base-ui/react.
    6. Do not restyle FormControls, ConfigApp, Sidebar, or sections this plan. Do not edit preferencesCore. Do not import shadcn/tailwind into index.desktop.css.
    7. Run isolation + ui import tests until GREEN; run full npm test.

    Using official shadcn@4.13.1 base-nova only (Package Legitimacy: no new [SLOP]; shadcn pin approved).
  </action>
  <verify>
    <automated>npx vitest run src/lib/foundation.isolation.test.ts src/components/ui/ && npm test && test -f src/components/ui/switch.tsx && test -f src/components/ui/select.tsx && test -f src/components/ui/checkbox.tsx && test -f src/components/ui/popover.tsx && npm ls @base-ui/react</automated>
  </verify>
  <done>Four form primitives installed with Mist Sky overrides; FND-03 green; import tests green; no registry dump.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local ui primitives | CLI install adds/generates third-party component code |
| UI primitives → FormControls (later plans) | No secrets in primitives |
| Desktop CSS entry | Must not gain shadcn/tailwind import from this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | shadcn CLI install | medium | mitigate | Pin shadcn@4.13.1 only; official registry; FND-03 forbids dump primitives |
| T-04-02 | Tampering | switch/select/checkbox/popover Base UI vs Radix | medium | mitigate | Assert @base-ui/react imports; no product @radix-ui/ in new primitives |
| T-04-03 | Information Disclosure | this plan | low | accept | No OAuth/share/redaction/form handlers touched |
| T-04-SC | Tampering | npm/registry install | high | mitigate | Package Legitimacy: no new deps expected; official four names only; isolation + npm ls after install |
</threat_model>

<verification>
- foundation.isolation.test.ts green for expanded allowlist + required Phase 4 primitives + forbid dump
- ui import tests green for switch/select/checkbox/popover
- npm test green
- No card/sheet/drawer/alert-dialog under src/components/ui
</verification>

<success_criteria>
- FRM-01 primitives ready (Switch, Select, Checkbox, Popover)
- FND-03 allowlist expanded without registry dump
- Mist Sky linear overrides on Select/Popover/Checkbox; Switch capsule exception only
- Desktop CSS isolation untouched
</success_criteria>

<output>
Create `.planning/phases/04-form-kit-adapters-editor-chrome/04-01-SUMMARY.md` when done
</output>
