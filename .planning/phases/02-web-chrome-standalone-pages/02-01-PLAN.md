---
phase: 02-web-chrome-standalone-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.web.css
  - src/lib/foundation.isolation.test.ts
autonomous: true
requirements:
  - WEB-06
  - WEB-07
must_haves:
  truths:
    - "Web CSS uses Mist Sky primary #a8c5e8 (dark) and #5a7fa8 (light), not logo cyan as --primary"
    - "--radius is 0 (strict linear) on web"
    - "Bridge --color-gsd-accent maps to Mist Sky primary path, not cyan bridge values"
    - "Bridge --color-gsd-danger maps to --destructive soft rose"
    - ".gsd-btn CSS rules remain in index.web.css for unrestyled editor/forms until Phase 4 (D-22)"
    - "Desktop CSS isolation still holds — no shadcn stack, no Mist Sky restyle on desktop (ISO-01)"
    - "Dual-write theme path unchanged — this plan does not edit theme.ts"
    - "UI Considerations: token cutover is foundation for quiet load/empty/error colors on later pages; no product page mount yet"
  artifacts:
    - path: src/index.web.css
      provides: "Mist Sky semantic tokens + radius 0 + remapped gsd bridge"
      contains: "#a8c5e8"
    - path: src/lib/foundation.isolation.test.ts
      provides: "Mist Sky + radius + non-cyan accent isolation contracts"
  key_links:
    - from: src/index.web.css :root / .dark
      to: shadcn Button/Input color utilities
      via: "--primary / --destructive / --radius"
    - from: --color-gsd-accent
      to: var(--primary)
      via: "@theme inline bridge remap (D-21)"
    - from: foundation.isolation.test.ts
      to: index.web.css + index.desktop.css
      via: "readFileSync source contracts"
---

<objective>
Cut over **Mist Sky design tokens** on the web CSS entry and update isolation tests so Phase 2 surfaces inherit the locked palette (per D-00a, D-21, D-23) without touching product components yet.

Purpose: Tokens-first cutover so shell/pages never flash logo cyan mid-restyle; keep desktop and editor bridge CSS stable.

Output: Mist Sky `:root` / `.dark` in `src/index.web.css`; green updated `foundation.isolation.test.ts`; dual builds still viable.
</objective>

## Phase Goal

**As a** web user of GSD Pi Config, **I want** shared chrome and standalone routes fully on Mist Sky shadcn with consistent buttons and quiet states, **so that** the site looks cohesive without changing product flows.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/design/PALETTE.md
@.planning/phases/02-web-chrome-standalone-pages/02-CONTEXT.md
@.planning/phases/02-web-chrome-standalone-pages/02-UI-SPEC.md
@.planning/phases/02-web-chrome-standalone-pages/02-RESEARCH.md
@.planning/phases/02-web-chrome-standalone-pages/02-PATTERNS.md
@.planning/phases/02-web-chrome-standalone-pages/02-VALIDATION.md
@.planning/phases/01-foundation-isolation-theme-bridge/01-02-SUMMARY.md
@src/index.web.css
@src/index.desktop.css
@src/lib/foundation.isolation.test.ts
@src/lib/theme.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 isolation contracts for Mist Sky tokens (D-00a, D-23)</name>
  <files>src/lib/foundation.isolation.test.ts</files>
  <read_first>
    - src/lib/foundation.isolation.test.ts (full — UI_ALLOWLIST, bridge test, desktop suite, forbidden cyan primary)
    - .planning/phases/02-web-chrome-standalone-pages/02-VALIDATION.md (Wave 0 requirements)
    - .planning/phases/02-web-chrome-standalone-pages/02-PATTERNS.md (foundation.isolation retarget section)
    - .planning/design/PALETTE.md (locked hex values)
  </read_first>
  <behavior>
    - Web CSS declares --primary containing Mist Sky dark primary hex and light primary hex (or both present across :root/.dark)
    - Web CSS declares --radius as 0 (not 0.625rem)
    - Web CSS never declares --primary as logo cyan
    - --color-gsd-accent is bound to var(--primary) (or equivalent Mist Sky primary path), not a cyan bridge literal as the accent utility source
    - .gsd-btn / .gsd-btn-primary / .gsd-btn-segment CSS class rules still present (D-22 keep bridge for editor)
    - Desktop CSS still has legacy gsd tokens and excludes shadcn/tailwind + tw-animate
    - UI_ALLOWLIST still button-only this plan (Plan 02 expands for input/textarea)
  </behavior>
  <action>
    Update Wave 0 isolation tests only (no CSS implementation in this task) so Task 2 turns RED→GREEN for Mist Sky (per D-00a, D-23, D-21).

    1. Retarget the transitional bridge test currently requiring cyan `--bridge-accent` as the accent source. Keep requiring presence of `--color-gsd-bg`, `--color-gsd-accent`, and `.gsd-btn` / `.gsd-btn-segment` / `.gsd-btn-primary` class rules (editor still needs bridge CSS until Phase 4 per D-22). Replace cyan-as-accent expectations with: Mist Sky primaries present (`#a8c5e8` and `#5a7fa8`); `--color-gsd-accent` maps via `var(--primary)` (or documents Mist Sky primary, not logo cyan as the utility target).
    2. Add explicit asserts for `--radius` zero (match a declaration of radius zero, not the old 0.625rem scaffold).
    3. Keep existing “does not map GSD cyan into --primary” and desktop ISO-01 suites green-capable after Task 2.
    4. Do **not** expand UI_ALLOWLIST for input/textarea yet (Plan 02). Do not edit `index.web.css`, `theme.ts`, product components, or desktop CSS.
    5. Run isolation tests once — expect RED on new Mist Sky/radius/accent-map asserts until Task 2.

    Cite D-00a / D-23 in commit intent: token contract before product restyle.
  </action>
  <verify>
    <automated>npx vitest run src/lib/foundation.isolation.test.ts ; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - foundation.isolation.test.ts asserts Mist Sky primary hexes and radius zero
    - foundation.isolation.test.ts no longer requires cyan bridge as the sole accent source of truth
    - .gsd-btn CSS presence still required
    - UI_ALLOWLIST remains button-only
    - Running isolation tests fails (RED) before Task 2 CSS cutover
  </acceptance_criteria>
  <done>Wave 0 isolation contracts encode Mist Sky + radius 0 + non-cyan accent bridge remap; tests RED until token CSS lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mist Sky token cutover + radius 0 + bridge remap (D-00a, D-21, D-23)</name>
  <files>src/index.web.css</files>
  <read_first>
    - src/index.web.css (full :root, .dark, @theme inline gsd bridge, .gsd-btn rules)
    - .planning/design/PALETTE.md
    - .planning/phases/02-web-chrome-standalone-pages/02-UI-SPEC.md Color + Token cutover sections
    - .planning/phases/02-web-chrome-standalone-pages/02-RESEARCH.md Pattern 1 Mist Sky mapping table + code skeleton
    - src/lib/foundation.isolation.test.ts (from Task 1)
  </read_first>
  <behavior>
    - Same as Task 1 isolation asserts — all GREEN after this task
  </behavior>
  <action>
    Implement D-21 step 1 token cutover in web CSS only (per D-00a, D-00b, D-23).

    1. Replace Phase 1 neutral OKLCH semantic tokens under `:root` (light) and `.dark` (dark) with Mist Sky hex from PALETTE.md / UI-SPEC:
       - Light: background `#f5f7fa`, card/popover `#ffffff`, muted/secondary/accent wash `#eef1f5`, border/input `#d8dee8`, foreground `#14171c`, muted-foreground `#5c6570`, primary `#5a7fa8`, primary-foreground `#f5f7fa`, destructive `#b85c56`
       - Dark: background `#0b0c0e`, card/popover `#111316`, muted/secondary/accent wash `#181b20`, border/input `#2a2e36`, foreground `#f2f4f7`, muted-foreground `#8b929e`, primary `#a8c5e8`, primary-foreground `#0b0c0e`, destructive `#e8b4b0`
    2. Set `--radius: 0` in both themes (D-23).
    3. Add optional `--primary-hover` and `--accent-soft` per RESEARCH skeleton; bind `--color-primary-hover` in `@theme inline` if added.
    4. Remap bridge utilities so unrestyled editor chrome stops neon flash (D-21):
       - `--color-gsd-accent: var(--primary)`
       - accent hover/dim → primary-hover / accent-soft (or primary-tinted mixes)
       - `--color-gsd-on-accent: var(--primary-foreground)`
       - `--color-gsd-danger: var(--destructive)`
       - Retire cyan values as the effective accent source (delete or stop referencing cyan `--bridge-accent*` for product utilities). Do **not** map primary to logo cyan.
    5. Keep all `.gsd-btn*` / segment / choice CSS rules for ConfigApp until Phase 4 (D-22). Do not delete the bridge class chrome.
    6. Do not edit `index.desktop.css`, `theme.ts`, FormControls, ConfigApp chrome, or product pages. Keep `@import` order, `@custom-variant dark`, and `@theme inline` semantic color bindings intact (update values only).
    7. Run isolation tests until GREEN; run full `npm test`.

    Using Mist Sky locked palette per user decision D-00a (not logo cyan/purple).
  </action>
  <verify>
    <automated>npx vitest run src/lib/foundation.isolation.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - index.web.css contains Mist Sky dark and light primary hexes
    - --radius declared as 0
    - --color-gsd-accent uses var(--primary) (grep-able)
    - .gsd-btn rules still present in index.web.css
    - index.desktop.css unchanged this plan
    - npx vitest run src/lib/foundation.isolation.test.ts exits 0
    - npm test exits 0
  </acceptance_criteria>
  <done>Mist Sky tokens + radius 0 + non-cyan accent bridge live; isolation green; desktop CSS untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web CSS → browser paint | Presentation only; no secrets |
| Platform CSS split | Web token edits must not leak into desktop entry |
| Isolation tests read repo files | Trusted local FS only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | index.web.css primary mapping | low | mitigate | Isolation tests forbid logo cyan as --primary; assert Mist Sky hex |
| T-02-02 | Information Disclosure | this plan | low | accept | No logging; no OAuth/keys/share paths touched |
| T-02-03 | Elevation | N/A token CSS | low | accept | No auth or capability changes |
| T-02-SC | Tampering | npm installs | low | accept | No package installs in this plan |
</threat_model>

<verification>
- foundation.isolation.test.ts green for Mist Sky, radius 0, non-cyan accent bridge, desktop ISO-01
- npm test green
- Grep: no --primary logo-cyan mapping; .gsd-btn still present
</verification>

<success_criteria>
- Mist Sky semantic tokens power web UI (D-00a, D-23)
- Bridge accent no longer cyan (D-21)
- Editor bridge CSS retained (D-22)
- Desktop isolation preserved (ISO-01)
</success_criteria>

<output>
Create `.planning/phases/02-web-chrome-standalone-pages/02-01-SUMMARY.md` when done
</output>
