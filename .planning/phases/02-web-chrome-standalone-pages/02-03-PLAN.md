---
phase: 02-web-chrome-standalone-pages
plan: 03
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - src/components/WebShell.tsx
  - src/components/ThemeToggle.tsx
  - src/components/BrandMark.tsx
autonomous: true
requirements:
  - WEB-01
  - THM-04
  - WEB-06
must_haves:
  truths:
    - "WebShell header is ~56px, opaque/near-opaque, 1px bottom border — not filled segment pill nav"
    - "Main nav Editor / Gallery / New preset uses underline tabs (active 1px primary bottom rule)"
    - "ThemeToggle is Auto · Dark · Light text trio with radiogroup semantics; setTheme API unchanged"
    - "BrandMark PNG retained (no flat-square replacement)"
    - "External opengsd.net control uses buttonVariants outline/ghost — no uiClasses btn on shell"
    - "WebShell and ThemeToggle do not import btn / btnSegment / segmentGroup from uiClasses"
    - "theme.ts dual-write and storage key untouched"
    - "Workspace strip still 36px when active editor; label uses primary mono Mist Sky tokens"
    - "UI Considerations: shell always populated; theme control empty/error N/A; focus-visible ring on theme radios and nav"
  artifacts:
    - path: src/components/WebShell.tsx
      provides: "Underline nav shared chrome"
    - path: src/components/ThemeToggle.tsx
      provides: "Linear text theme trio (THM-04)"
  key_links:
    - from: ThemeToggle
      to: useTheme / setTheme / applyTheme dual-write
      via: "presentation-only restyle (THM-04)"
    - from: WebShell NavLink
      to: react-router routes
      via: "isActive underline, not segment pills"
    - from: WebShell external link
      to: buttonVariants
      via: "WEB-06 single button system"
---

<objective>
Restyle **shared web chrome** — WebShell header/nav/workspace strip and ThemeToggle — onto Mist Sky linear grammar with shadcn Button language (per D-01–D-04, D-08, THM-04, WEB-01).

Purpose: Every standalone route wrapped in WebShell inherits consistent chrome; theme semantics stay Auto/Dark/Light dual-write.

Output: Restyled `WebShell.tsx` + `ThemeToggle.tsx`; optional light BrandMark type align; no uiClasses segment/btn on shell.
</objective>

## Phase Goal

**As a** web user of GSD Pi Config, **I want** shared chrome and standalone routes fully on Mist Sky shadcn with consistent buttons and quiet states, **so that** the site looks cohesive without changing product flows.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-web-chrome-standalone-pages/02-01-SUMMARY.md
@.planning/phases/02-web-chrome-standalone-pages/02-02-SUMMARY.md
@.planning/phases/02-web-chrome-standalone-pages/02-CONTEXT.md
@.planning/phases/02-web-chrome-standalone-pages/02-UI-SPEC.md
@.planning/phases/02-web-chrome-standalone-pages/02-RESEARCH.md
@.planning/phases/02-web-chrome-standalone-pages/02-PATTERNS.md
@src/components/WebShell.tsx
@src/components/ThemeToggle.tsx
@src/components/BrandMark.tsx
@src/components/ui/button.tsx
@src/lib/theme.ts
@src/lib/theme.test.ts
@src/lib/uiClasses.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ThemeToggle linear text trio (D-02, THM-04)</name>
  <files>src/components/ThemeToggle.tsx</files>
  <read_first>
    - src/components/ThemeToggle.tsx (current segment pills via btnSegment*)
    - src/lib/theme.ts (useTheme / setTheme / STORAGE_KEY — do not change)
    - src/lib/theme.test.ts (must remain green)
    - .planning/phases/02-web-chrome-standalone-pages/02-UI-SPEC.md ThemeToggle + Typography (12px trio)
    - .planning/phases/02-web-chrome-standalone-pages/02-CONTEXT.md D-02
  </read_first>
  <behavior>
    - OPTIONS remain system→Auto, dark→Dark, light→Light
    - role=radiogroup + role=radio + aria-checked preserved
    - setTheme(opt.value) still called on click — no alternate storage
    - No imports of btnSegment / btnSegmentActive / segmentGroup from uiClasses
  </behavior>
  <action>
    Restyle ThemeToggle presentation only (THM-04, D-02) — semantics unchanged.

    1. Remove all `uiClasses` segment imports (`btnSegment`, `btnSegmentActive`, `segmentGroup`).
    2. Render linear text trio: labels Auto · Dark · Light at 12px; idle muted-foreground; active = foreground emphasis + 1px bottom primary rule (CSS underline — not filled pills; not icon-only cycle). Prefer pure buttons + Tailwind; no ToggleGroup pills.
    3. Keep `role="radiogroup"`, `aria-label="Theme"`, each option `role="radio"` + `aria-checked`, `title` tooltips, `type="button"`, `onClick={() => setTheme(opt.value)}`.
    4. Hit targets ≥40px height where practical inside 56px header; focus-visible uses ring/primary.
    5. Do **not** edit `theme.ts`, storage key, `applyTheme`, or bootstrap order. Do not restyle WebShell in this task.
    6. Run `npx vitest run src/lib/theme.test.ts` and full `npm test` (behavior preservation).

    Using text trio per user decision D-02 (research also preferred CSS over filled ToggleGroup).
  </action>
  <verify>
    <automated>npx vitest run src/lib/theme.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - ThemeToggle.tsx has no btnSegment / segmentGroup imports
    - role radiogroup/radio and setTheme calls remain
    - Labels Auto, Dark, Light present
    - theme.test.ts exits 0
    - npm test exits 0
  </acceptance_criteria>
  <done>ThemeToggle is linear Mist Sky text trio with unchanged Auto/Dark/Light semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WebShell underline nav + Button chrome (D-01, D-03, D-04, D-08, WEB-01)</name>
  <files>src/components/WebShell.tsx, src/components/BrandMark.tsx</files>
  <read_first>
    - src/components/WebShell.tsx (full — segment nav, glass header, workspace strip)
    - src/components/ThemeToggle.tsx (from Task 1)
    - src/components/BrandMark.tsx (PNG — keep)
    - src/components/ui/button.tsx (buttonVariants export)
    - .planning/phases/02-web-chrome-standalone-pages/02-UI-SPEC.md WebShell contract + spacing (56px, 36px strip)
    - .planning/phases/02-web-chrome-standalone-pages/02-PATTERNS.md WebShell section
  </read_first>
  <behavior>
    - NAV items remain Editor / Gallery / New preset with same routes and end= for editor
    - Header height uses 3.5rem / 56px constant (D-04)
    - No uiClasses btn / btnSegment / segmentGroup imports in WebShell
    - External opengsd.net uses buttonVariants (outline or ghost), hidden below sm as today
    - BrandMark PNG still rendered (D-03)
  </behavior>
  <action>
    Restyle WebShell shared chrome (WEB-01, D-01, D-03, D-04, D-08) without changing route structure or workspace strip logic.

    1. Drop `uiClasses` imports (`btn`, `btnSegment*`, `segmentGroup`). Use semantic tokens: `bg-background`, `text-foreground`, `border-border`, `text-muted-foreground`, `text-primary` as needed (not filled segment pills).
    2. Header: height 56px (`h-14` / existing CSS var `3.5rem`); 1px bottom border; opaque/near-opaque background — remove heavy glass as primary chrome (drop or neutralize `backdrop-blur-md` dependency on translucency).
    3. Main nav (`aria-label="Main"`): underline tabs via `NavLink` className — idle muted text; active foreground + `border-b` primary 1px (D-01). Keep labels **Editor · Gallery · New preset** and same `to` / `end` behavior.
    4. External “opengsd.net”: style with `buttonVariants({ variant: "outline" })` or ghost + `cn` on `<a>` — never nest Button wrapping anchor incorrectly (prefer className on `<a>`). Keep target blank + rel; hide below `sm` as today.
    5. Brand: keep existing PNG BrandMark (D-03); size/spacing may nudge to h-8; optional light type align only — no flat-square replacement. Touch BrandMark.tsx only if needed for token/type alignment.
    6. Workspace strip: still only when `active === "editor"`; ~36px; quiet meta; workspace label mono + primary (Mist Sky), truncate + title as today.
    7. Children remain `flex-1 min-h-0` column growth. Do not restyle ConfigApp sidebar/toolbar (D-24 / WEB-04 out of scope). Do not change App.web routes.
    8. Run `npm test` and `npm run build:web` smoke if time; at minimum unit suite green.

    Per D-08 / WEB-06: shell uses only shadcn button language for actionable controls (external link via buttonVariants).
  </action>
  <verify>
    <automated>npm test && npm run build:web</automated>
  </verify>
  <acceptance_criteria>
    - WebShell.tsx has no btnSegment / segmentGroup / btn imports from uiClasses
    - Nav labels Editor, Gallery, New preset present with NavLink
    - buttonVariants or Button used for external control
    - Header height constant remains 3.5rem / h-14 class
    - BrandMark PNG path/component still used
    - npm test exits 0
    - npm run build:web exits 0
  </acceptance_criteria>
  <done>Shared web chrome is underline Mist Sky shell with shadcn button language; theme toggle integrated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ThemeToggle → localStorage theme key | Untrusted string already allowlisted in theme.ts |
| External opengsd.net link | User navigation only; noopener noreferrer |
| WebShell children | Pass-through; no secret rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Tampering | ThemeToggle setTheme | low | mitigate | Keep useTheme API; theme.test.ts dual-write green; no second storage key |
| T-02-08 | Information Disclosure | WebShell | low | accept | Workspace label is non-secret path/name only |
| T-02-09 | Spoofing | external link | low | mitigate | Keep target=_blank rel=noopener noreferrer |
| T-02-SC | Tampering | npm | low | accept | No installs this plan |
</threat_model>

<verification>
- theme.test.ts green (dual-write untouched)
- npm test green
- build:web green
- Source: WebShell/ThemeToggle free of segment uiClasses imports
</verification>

<success_criteria>
- WEB-01: shared chrome restyled with underline nav + 56px header + PNG brand
- THM-04: theme trio restyled without semantic change
- WEB-06: shell uses buttonVariants/Button only for CTAs
</success_criteria>

<output>
Create `.planning/phases/02-web-chrome-standalone-pages/02-03-SUMMARY.md` when done
</output>
