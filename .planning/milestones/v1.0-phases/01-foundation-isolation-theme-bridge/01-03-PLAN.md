---
phase: 01-foundation-isolation-theme-bridge
plan: 03
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - components.json
  - package.json
  - package-lock.json
  - src/components/ui/button.tsx
  - src/lib/utils.ts
  - src/lib/foundation.isolation.test.ts
  - src/components/ui/button.import.test.ts
  - src/index.web.css
autonomous: false
requirements:
  - FND-01
  - FND-02
  - FND-03
  - ISO-01
must_haves:
  truths:
    - "components.json locked: style base-nova, rsc false, cssVariables true, baseColor neutral, css path src/index.web.css, blank tailwind config"
    - "Single primitive base Base UI — pin base-nova / -b base; never mix Radix adds"
    - "Only Button (plus CLI-required support files for Button) under src/components/ui/ — no full registry dump"
    - "Button module imports and typechecks via @/components/ui/button and cn"
    - "Button CVA variants include default, secondary, destructive, outline, ghost, link"
    - "No product mount of Button replacing gsd-btn site-wide; import-only or temporary smoke only"
    - "Desktop build still succeeds and desktop CSS still excludes shadcn stack (ISO-01)"
    - "No third-party shadcn registries; official CLI only pinned 4.13.1"
    - "Share/redact/backends/FormControls untouched"
    - "UI Considerations: Button populated happy path = import-only (or temporary smoke label 'shadcn Button proof'); no product empty/loading surfaces"
    - "UI Considerations: focus/disabled defaults on Button (focus-visible ring, disabled opacity); ISO-01 desktop isolation still holds after Button add"
  artifacts:
    - path: components.json
      provides: "Locked shadcn CLI config"
      contains: "base-nova"
    - path: src/components/ui/button.tsx
      provides: "Walking skeleton Button primitive"
    - path: src/components/ui/button.import.test.ts
      provides: "Import-only proof without render"
    - path: src/lib/utils.ts
      provides: "cn consumed by Button"
  key_links:
    - from: components.json aliases
      to: src/lib/utils.ts + src/components/ui
      via: "@ paths"
    - from: button.tsx
      to: cn + CVA + tokens
      via: "className merge + semantic color utilities"
    - from: shadcn add button
      to: package.json deps
      via: "@base-ui/react, class-variance-authority, lucide-react as pulled by CLI"
---

<objective>
Lock **shadcn foundation** (`components.json` + minimal Button) so the web walking skeleton is complete: CLI config irreversible fields pinned, only needed primitives installed, dual-platform builds green (FND-01, FND-02, FND-03, ISO-01).

Purpose: Make subsequent phases able to `shadcn add` safely against a known base without re-deciding style/baseColor/cssVariables or dumping the registry.

Output: `components.json`, `src/components/ui/button.tsx`, deps from official add, import test, phase dual-build gate + human isolation/theme smoke.
</objective>

## Phase Goal

**As a** dual-platform GSD Pi Config maintainer, **I want** a locked shadcn Button walking skeleton on the web token system, **so that** Phase 2 can restyle chrome without redoing foundation decisions.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-isolation-theme-bridge/01-02-SUMMARY.md
@.planning/phases/01-foundation-isolation-theme-bridge/01-RESEARCH.md
@.planning/phases/01-foundation-isolation-theme-bridge/01-UI-SPEC.md
@.planning/phases/01-foundation-isolation-theme-bridge/01-PATTERNS.md
@.planning/phases/01-foundation-isolation-theme-bridge/01-SKELETON.md
@src/lib/utils.ts
@src/index.web.css
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock components.json for Vite + React + Tailwind 4 (FND-02)</name>
  <files>components.json</files>
  <read_first>
    - .planning/phases/01-foundation-isolation-theme-bridge/01-RESEARCH.md locked components.json shape
    - .planning/phases/01-foundation-isolation-theme-bridge/01-UI-SPEC.md Design System table
    - tsconfig.json paths (must match aliases)
    - src/index.web.css (css path target must exist)
  </read_first>
  <action>
    Hand-write root `components.json` (safer brownfield path — do **not** run `shadcn init -d` or `-t vite` which scaffold wrong templates).

    Locked fields (FND-02, irreversible — per RESEARCH):
    - `$schema`: https://ui.shadcn.com/schema.json
    - `style`: `base-nova`
    - `rsc`: false
    - `tsx`: true
    - `tailwind.config`: `""` (blank — TW4 CSS-first)
    - `tailwind.css`: `src/index.web.css` (web-only — never desktop CSS)
    - `tailwind.baseColor`: `neutral`
    - `tailwind.cssVariables`: true
    - `tailwind.prefix`: `""`
    - aliases: components `@/components`, utils `@/lib/utils`, ui `@/components/ui`, lib `@/lib`, hooks `@/hooks`
    - `iconLibrary`: `lucide`

    Do not run package installs in this task. Do not add components yet.
    Extend `src/lib/foundation.isolation.test.ts` (or add assertions) to parse components.json and assert style/baseColor/cssVariables/rsc/css path — keeps FND-02 regression-proof.
  </action>
  <verify>
    <automated>node -e "const j=require('./components.json'); if(j.style!=='base-nova'||j.rsc!==false||j.tailwind.baseColor!=='neutral'||j.tailwind.cssVariables!==true||j.tailwind.css!=='src/index.web.css'||j.tailwind.config!=='') process.exit(1);" && npx vitest run src/lib/foundation.isolation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `components.json` exists at repo root with exact locked fields above
    - css points at `src/index.web.css` not `src/index.css` or desktop
    - foundation isolation test asserts components.json lock fields
    - No `src/components/ui/**` yet (Button is Task 3)
  </acceptance_criteria>
  <done>components.json committed and statically asserted (FND-02).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Package legitimacy gate before shadcn/lucide install</name>
  <files>package.json</files>
  <read_first>
    - .planning/phases/01-foundation-isolation-theme-bridge/01-RESEARCH.md Package Legitimacy Audit table
    - package.json current dependencies
  </read_first>
  <what-built>
    Plan is about to install foundation packages flagged SUS-by-age in RESEARCH (false positives with multi-million weekly downloads) plus CLI-driven Base UI deps:
    - npm package name must be exactly `shadcn` (CLI via `npx shadcn@4.13.1`) — not typosquat names
    - `lucide-react` (official Lucide React bindings)
    - `class-variance-authority`, `@base-ui/react` (via add), and any deps `shadcn add button` pulls
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/shadcn — confirm publisher/name is official shadcn CLI (not a lookalike).
    2. Open https://www.npmjs.com/package/lucide-react — confirm official Lucide package.
    3. Confirm planned CLI pin is `npx shadcn@4.13.1` (not unpinned latest for this phase) and style base is Base UI (`-b base`), not a random third-party registry.
    4. Reply **approved** to proceed with install + `add button`, or list packages to block.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <action>
    Blocking human checkpoint per RESEARCH Package Legitimacy Gate for [SUS] age false-positives on `shadcn` and `lucide-react`. Do not run npm install of these packages until resume-signal approved. Not auto-approvable under workflow.auto_advance for legitimacy checkpoints.
  </action>
  <verify>
    <automated>MISSING — human gate; no automated substitute</automated>
  </verify>
  <acceptance_criteria>
    - Human approved package names before Task 3 install
    - No third-party shadcn registry configured
  </acceptance_criteria>
  <done>Human confirmed official package identities; install may proceed.</done>
</task>

<task type="auto">
  <name>Task 3: Install deps, add Button only, dual-build proof (FND-01, FND-03, ISO-01)</name>
  <files>package.json, package-lock.json, src/components/ui/button.tsx, src/components/ui/button.import.test.ts, src/index.web.css, src/lib/foundation.isolation.test.ts</files>
  <read_first>
    - components.json (Task 1)
    - src/lib/utils.ts (cn must exist)
    - src/index.web.css (token surface; may add shadcn/tailwind import if package provides it)
    - .planning/phases/01-foundation-isolation-theme-bridge/01-UI-SPEC.md Button Variants Contract + Registry Safety
    - .planning/phases/01-foundation-isolation-theme-bridge/01-RESEARCH.md Exact CLI steps 7–14; FND-03 allowlist
  </read_first>
  <action>
    Complete walking skeleton primitive install after legitimacy approval (FND-01, FND-03).

    1. Install helper deps if not already present: `class-variance-authority`, `lucide-react` (and `shadcn@4.13.1` as dependency if required for `@import "shadcn/tailwind.css"`). Prefer versions from RESEARCH (cva 0.7.x, lucide-react current, shadcn 4.13.1).
    2. Run **only**: `npx shadcn@4.13.1 add button -y` (Base UI via existing components.json / `-b base` if required). **Never** `add --all`. **Never** third-party registries.
    3. Diff generated files: expect `src/components/ui/button.tsx` (and only CLI support files required by Button — e.g. no card/dialog/input). If CLI writes unexpected components, remove extras to satisfy FND-03 allowlist.
    4. Ensure web CSS can resolve any shadcn CSS import the Button stack needs; if `@import "shadcn/tailwind.css"` was deferred in Plan 02, add it now after package install. Keep desktop CSS free of that import.
    5. Create `src/components/ui/button.import.test.ts` that imports `{ Button }` from `./button` (or `@/components/ui/button`) and asserts `typeof Button === "function"` (or object for forward components) — **import-only, no DOM render**, keep vitest node env.
    6. Extend isolation test allowlist: `src/components/ui` basenames ⊆ expected set (button.tsx + any single helper the CLI added for button only). Fail if dialog/card/input/etc. appear.
    7. Do **not** wire Button into ConfigApp, FormControls, WebShell, or desktop paths. Do not replace gsd-btn. Prefer no temporary smoke mount; if any smoke is added it must be web-only, labeled `shadcn Button proof`, and noted in SUMMARY for Phase 2 removal.
    8. Do not touch share/redact, OAuth, backends, preference serialization.
    9. Run full gate: `npm test && npm run build:web && npm run build`. Confirm desktop CSS still isolation-clean.
    10. After automated green: stop for phase manual smoke (see verify human-check) — no-flash web boot, dual-write via ThemeToggle, desktop legacy look (ISO-01). Do not start Phase 2 restyles in this task.

    Pin Base UI only — if add pulls radix packages unexpectedly, stop and re-check components.json style/base rather than mixing.
  </action>
  <verify>
    <automated>npx vitest run src/lib/utils.test.ts src/lib/theme.test.ts src/lib/foundation.isolation.test.ts src/components/ui/button.import.test.ts && npm test && npm run build:web && npm run build</automated>
    <human-check>
      1. Web `npm run dev:web`: set Light, hard reload — no dark flash (THM-02).
      2. Cycle Auto/Dark/Light — html has matching data-theme and .dark class presence (THM-03).
      3. Desktop `npm run dev` or desktop build: legacy gsd look intact, not shadcn neutral body (ISO-01).
      4. Type approved when visual matrix passes.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `src/components/ui/button.tsx` exists
    - Button source mentions variants default, secondary, destructive, outline, ghost, link (CVA)
    - `button.import.test.ts` passes
    - `src/components/ui/` has no card/dialog/input/select/command files (FND-03)
    - package.json includes class-variance-authority and lucide-react (and @base-ui/react if CLI added)
    - Desktop CSS still has zero `shadcn/tailwind` imports
    - `npm test`, `npm run build:web`, `npm run build` all exit 0
    - FormControls.tsx and ConfigApp.tsx unchanged this plan
    - Human smoke for theme matrix + desktop isolation completed (see human-check)
  </acceptance_criteria>
  <done>shadcn Button walking skeleton installed; FND-01/02/03 satisfied; dual builds green; manual isolation/theme smoke done.</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol / file | Plan | Notes |
|---------------|------|-------|
| `components.json` | 03 | FND-02 lock |
| `src/components/ui/button.tsx` | 03 | FND-01/03 skeleton |
| `button.import.test.ts` | 03 | Import proof |
| `class-variance-authority`, `lucide-react`, `@base-ui/react` | 03 | Via install/add |
| Full dual-platform build gate | 03 | ISO-01 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm/npx registry → node_modules | Supply-chain install of CLI + UI deps |
| shadcn CLI → src/components/ui | Generated owned source; must not pull network code at runtime |
| Official vs third-party registry | Only ui.shadcn.com / default registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08 | Tampering | npx shadcn / lucide-react install | high | mitigate | Blocking human legitimacy checkpoint; pin `shadcn@4.13.1`; verify npm package names; review lockfile |
| T-01-09 | Tampering | Third-party shadcn registry | high | mitigate | UI-SPEC Registry Safety: official button only; no third-party registries; no add --all |
| T-01-10 | Information Disclosure | CLI / generated components | medium | mitigate | Do not pass secrets to CLI; do not edit share/redact/ApiKeys; generated Button is presentational only |
| T-01-11 | Tampering | Generated button.tsx | low | mitigate | Diff generated file; no unexpected network/dynamic code; import-only proof |
| T-01-SC | Tampering | npm/pip-style installs | high | mitigate | slopcheck via RESEARCH legitimacy table + blocking human checkpoint for [SUS] shadcn + lucide-react |
| T-01-12 | Elevation / Isolation | Desktop bundle | high | mitigate | FND-03 allowlist + isolation tests + `npm run build` desktop CSS still free of shadcn base |
</threat_model>

<verification>
- components.json field asserts
- button import test + isolation allowlist
- `npm test && npm run build:web && npm run build`
- Human theme + desktop smoke
</verification>

<success_criteria>
- FND-01 complete (aliases, cn, components/ui Button)
- FND-02 components.json locked
- FND-03 only Button primitive set
- ISO-01 desktop build + legacy CSS isolation holds
- Walking skeleton capability in 01-SKELETON.md proven
</success_criteria>

<output>
Create `.planning/phases/01-foundation-isolation-theme-bridge/01-03-SUMMARY.md` when done
</output>
