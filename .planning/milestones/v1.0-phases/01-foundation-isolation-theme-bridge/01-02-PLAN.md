---
phase: 01-foundation-isolation-theme-bridge
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - vite.config.ts
  - src/main.tsx
  - src/vite-env.d.ts
  - src/index.web.css
  - src/index.desktop.css
  - src/index.css
  - src/lib/foundation.isolation.test.ts
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - FND-04
  - THM-01
  - ISO-01
must_haves:
  truths:
    - "Web CSS entry exposes semantic tokens background, foreground, primary, muted, destructive, border, ring (and scaffold card/popover/secondary/accent/input/radius)"
    - "Web CSS includes @custom-variant dark for .dark descendants"
    - "Web CSS does not copy global input/select/textarea tag selectors from legacy form chrome"
    - "Desktop CSS is legacy gsd-* system with no shadcn/tailwind import and no tw-animate-css"
    - "main.tsx loads platform CSS via static @platform-css import (FOUC-safe), not dual simultaneous CSS"
    - "bootstrapTheme still runs before React mount after CSS import order is set"
    - "Clean neutral shadcn defaults — no GSD cyan mapped into --primary this phase"
    - "Desktop visual isolation: desktop entry does not inherit shadcn @layer base body rules"
    - "npm run build:web and npm run build both succeed after split"
    - "UI Considerations: isolation boundary covered (desktop must not load shadcn tokens/components); dark/light token surfaces exist on web only"
    - "UI Considerations: empty/loading/error product surfaces remain N/A this phase (WEB-07 deferred)"
  artifacts:
    - path: src/index.web.css
      provides: "Web Tailwind + shadcn semantic tokens + dark variant"
      contains: "--background"
    - path: src/index.desktop.css
      provides: "Byte-stable legacy gsd-* styles"
      contains: "--gsd-bg"
    - path: src/main.tsx
      provides: "import @platform-css"
    - path: vite.config.ts
      provides: "@platform-css alias gated by isWeb"
    - path: src/lib/foundation.isolation.test.ts
      provides: "Static isolation + token assertions"
  key_links:
    - from: vite.config.ts isWeb
      to: "@platform-css path"
      via: "resolve.alias points web or desktop CSS"
    - from: src/main.tsx
      to: platform CSS entry
      via: "static import @platform-css"
    - from: index.web.css tokens
      to: future Button / utilities
      via: "@theme inline color bindings"
---

<objective>
Split platform CSS so the **web build loads shadcn semantic tokens** while **desktop keeps legacy gsd-* visuals** (FND-04, THM-01, ISO-01), wired through a FOUC-safe `@platform-css` Vite alias.

Purpose: Isolation seam that prevents shadcn base layers from restyling Tauri; web gets neutral OKLCH tokens ready for Button and later chrome.

Output: `src/index.web.css`, `src/index.desktop.css`, main/vite wiring, isolation unit tests, both platform builds green.
</objective>

## Phase Goal

**As a** dual-platform GSD Pi Config maintainer, **I want** web-only shadcn tokens and desktop-only legacy CSS loaded by build mode, **so that** foundation restyle cannot force desktop visual change.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-isolation-theme-bridge/01-01-SUMMARY.md
@.planning/phases/01-foundation-isolation-theme-bridge/01-RESEARCH.md
@.planning/phases/01-foundation-isolation-theme-bridge/01-UI-SPEC.md
@.planning/phases/01-foundation-isolation-theme-bridge/01-PATTERNS.md
@.planning/phases/01-foundation-isolation-theme-bridge/01-SKELETON.md
@src/main.tsx
@src/index.css
@vite.config.ts
@src/vite-env.d.ts
@src/lib/theme.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Platform CSS split + @platform-css bootstrap wiring</name>
  <files>src/index.desktop.css, src/index.web.css, src/index.css, src/main.tsx, vite.config.ts, src/vite-env.d.ts</files>
  <read_first>
    - src/index.css (full ~634 lines — structure: tokens, @theme, body, form tags, gsd-btn, modals)
    - src/main.tsx (current import ./index.css + migrate + bootstrapTheme order)
    - vite.config.ts (after Plan 01: has @ alias; add @platform-css)
    - src/vite-env.d.ts
    - .planning/phases/01-foundation-isolation-theme-bridge/01-RESEARCH.md Pattern 2 + Pitfall 7 (prefer alias)
    - .planning/phases/01-foundation-isolation-theme-bridge/01-UI-SPEC.md Isolation Rules
  </read_first>
  <action>
    Implement the FND-04 / ISO-01 CSS isolation seam with FOUC-safe static import (RESEARCH preferred `@platform-css` pattern).

    1. Copy current `src/index.css` **byte-for-byte** to `src/index.desktop.css` (preserve all gsd-* tokens, form tag selectors, .gsd-btn, modal, grid). Do not add shadcn imports to desktop.
    2. Create `src/index.web.css` starting with:
       - `@import "tailwindcss";`
       - Placeholder comment that Task 2 fills tokens / tw-animate / shadcn base
       - Minimal body font stack using existing Geist family only (no gsd form tag selectors)
       - At minimum web must still paint after Task 1 (Tailwind import required)
    3. Update `vite.config.ts` resolve.alias to add `@platform-css` resolving to `./src/index.web.css` when `isWeb` else `./src/index.desktop.css`. Keep existing `@` alias from Plan 01.
    4. Replace `import "./index.css"` in `src/main.tsx` with `import "@platform-css";`. Keep order: CSS import → migrateLegacyStorageKeys → bootstrapTheme → dynamic App load.
    5. Add ambient module declaration for `@platform-css` in `src/vite-env.d.ts` so tsc accepts the import.
    6. Retire direct consumer of `src/index.css`: either delete it or replace with a short comment-only shim that must **not** be imported by main (avoid dual CSS). Prefer delete if no other imports (`rg 'index\\.css' src`).
    7. Do not install shadcn packages yet (Plan 03). Do not restyle FormControls/ConfigApp/WebShell.

    Verify both builds still typecheck/bundle after wiring (web may lack full tokens until Task 2 — still must compile).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build:web && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/index.desktop.css` exists and contains `--gsd-bg` and `.gsd-btn`
    - `src/index.web.css` exists and contains `@import "tailwindcss"`
    - `src/main.tsx` imports `@platform-css` and does not import `./index.css`
    - vite.config.ts alias `@platform-css` branches on isWeb
    - `src/vite-env.d.ts` declares module `@platform-css`
    - Desktop CSS file has zero occurrences of `shadcn/tailwind`
    - `npm run build:web` and `npm run build` exit 0
  </acceptance_criteria>
  <done>Platform CSS entries exist and bootstrap selects them by VITE_PLATFORM via static alias.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Web semantic tokens + isolation static tests (THM-01, ISO-01)</name>
  <files>src/index.web.css, src/lib/foundation.isolation.test.ts, package.json, package-lock.json</files>
  <read_first>
    - src/index.web.css (from Task 1)
    - src/index.desktop.css (must remain free of shadcn)
    - .planning/phases/01-foundation-isolation-theme-bridge/01-UI-SPEC.md Color semantic tokens table + non-goals
    - .planning/phases/01-foundation-isolation-theme-bridge/01-RESEARCH.md Web CSS skeleton (OKLCH + @theme inline + @layer base)
    - package.json (tw-animate-css not installed yet)
  </read_first>
  <behavior>
    - foundation.isolation.test.ts: web CSS contains required semantic token names; desktop CSS lacks shadcn/tailwind and tw-animate imports; web CSS includes custom-variant dark
    - Web CSS maps tokens through @theme inline (color-background etc. or official scaffold bindings)
  </behavior>
  <action>
    Complete THM-01 web token surface and automated isolation gates (ISO-01 / FND-04).

    1. `npm install tw-animate-css` (RESEARCH ~1.4.x). Do not install lucide/cva/shadcn/@base-ui yet unless required solely for CSS import resolution — if `@import "shadcn/tailwind.css"` needs the `shadcn` package, defer that import to Plan 03 after legitimacy gate, and use manual OKLCH token + `@theme inline` + `@layer base` from official manual install docs without third-party registry CSS until then. Prefer: if Plan 03 will add `shadcn` package, keep web CSS structure ready with a clearly marked import line only after package exists; for this task ensure tokens compile with `@import "tailwindcss"` + `@import "tw-animate-css"` + full neutral OKLCH `:root`/`.dark` block + `@custom-variant dark (&:is(.dark *));` + `@theme inline` bindings + `@layer base` border/body rules per RESEARCH/UI-SPEC.
    2. Paste **official shadcn neutral OKLCH scaffold** values (not hand-tuned cyan). Required tokens at minimum: `--background`, `--foreground`, `--primary`, `--primary-foreground`, `--secondary`, `--muted`, `--muted-foreground`, `--destructive`, `--border`, `--input`, `--ring`, `--radius`, plus card/popover/accent pairs for scaffold completeness. `--radius` default `0.625rem` per UI-SPEC.
    3. Do **not** map `--gsd-accent` / cyan into `--primary`. Do **not** copy legacy global `input`/`select`/`textarea` rules into web CSS (Pitfall 4).
    4. Create `src/lib/foundation.isolation.test.ts` using node `fs` + `path` + vitest to assert:
       - web CSS file text includes each required semantic token name and `@custom-variant dark`
       - desktop CSS text does not include `shadcn/tailwind` or `tw-animate-css`
       - web CSS does not include a bare `input {` / `select {` / `textarea {` tag selector block copied from legacy (assert absence of those patterns if they would fight shadcn — at least assert desktop still has form chrome and web does not include `--gsd-bg` as the primary system)
    5. Keep desktop CSS untouched except if accidental edits — restore gsd integrity.
    6. Run isolation tests + full unit suite + both builds.

    Note: Plan 03 may add `@import "shadcn/tailwind.css"` once `shadcn` package is installed; document in SUMMARY if deferred one plan for legitimacy ordering.
  </action>
  <verify>
    <automated>npx vitest run src/lib/foundation.isolation.test.ts && npm test && npm run build:web && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/index.web.css` defines `--background`, `--foreground`, `--primary`, `--muted`, `--destructive`, `--border`, `--ring`
    - `src/index.web.css` contains `@custom-variant dark` and `@import "tw-animate-css"`
    - No cyan primary override (`#22d3ee` not used as `--primary` value)
    - `src/index.desktop.css` has no `shadcn/tailwind` and no `tw-animate-css`
    - `src/lib/foundation.isolation.test.ts` exists and passes
    - `npm test`, `npm run build:web`, `npm run build` all exit 0
  </acceptance_criteria>
  <done>Web semantic tokens live; desktop isolation enforced by static tests and dual builds.</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol / file | Plan | Notes |
|---------------|------|-------|
| `src/index.web.css` | 02 | THM-01 tokens + dark variant |
| `src/index.desktop.css` | 02 | ISO-01 legacy |
| `@platform-css` | 02 | Vite alias |
| `foundation.isolation.test.ts` | 02 | Static gates |
| (inherits Plan 01 utils/theme) | 01 | Dual-write already live |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time VITE_PLATFORM → CSS graph | Wrong branch could ship shadcn CSS to desktop (integrity of isolation) |
| CSS @import registry | Only official tailwind / tw-animate; no third-party shadcn registries this plan |
| Theme DOM (from Plan 01) | Unchanged boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | Desktop CSS entry | high | mitigate | Byte-move legacy CSS; isolation tests forbid shadcn/tailwind + tw-animate on desktop; dual `npm run build` |
| T-01-05 | Tampering | Web CSS @import | medium | mitigate | Only tailwindcss + tw-animate-css; no third-party registry URLs; official OKLCH scaffold only |
| T-01-06 | Information Disclosure | CSS / tokens | low | accept | Tokens are non-secret design values; no env secrets in CSS |
| T-01-07 | Denial of Service | FOUC / broken CSS load | medium | mitigate | Static `@platform-css` import (not async race); bootstrapTheme order preserved |
| T-01-SC | Tampering | tw-animate-css npm | low | mitigate | RESEARCH legitimacy OK; review lockfile; pin compatible 1.x |
</threat_model>

<verification>
- Isolation unit test green
- Both platform production builds green
- Desktop CSS grep-clean of shadcn base imports
</verification>

<success_criteria>
- FND-04 platform CSS split live
- THM-01 semantic tokens on web
- ISO-01 desktop entry free of shadcn styling stack
</success_criteria>

<output>
Create `.planning/phases/01-foundation-isolation-theme-bridge/01-02-SUMMARY.md` when done
</output>
