---
phase: 03-modals-palette-overlays
plan: 03
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/components/SubmitPresetModal.tsx
autonomous: true
requirements:
  - OVL-01
must_haves:
  truths:
    - "Submit preset modal uses controlled shadcn Dialog + Input/Textarea/Button (D-01, D-08)"
    - "OAuth start and completeOAuthSubmit session/state/API logic unchanged (D-08, D-00e)"
    - "scanForLeakedSecrets gate still runs before OAuth; leak errors visible as soft danger role=alert (D-20)"
    - "Primary Sign in with GitHub / Done; Cancel outline; no uiClasses btn language"
    - "Manual PR path and copy share block remain available"
    - "No OAuth authorization code logging in this modal"
  artifacts:
    - path: src/components/SubmitPresetModal.tsx
      provides: "Dialog Submit restyle with intact OAuth/security handlers"
      contains: "scanForLeakedSecrets"
  key_links:
    - from: startOAuth
      to: scanForLeakedSecrets → sessionStorage → GitHub authorize
      via: "unchanged control flow (D-08)"
    - from: completeOAuthSubmit export
      to: OAuthCallbackPage
      via: "export signature and state check preserved"
    - from: secret-scan error UI
      to: soft danger alert
      via: "visible never tooltip-only (D-20)"
---

<objective>
Restyle **Submit preset** onto Dialog + Phase 2 form controls **without** changing OAuth, sessionStorage, API URLs, or secret-scan gate logic (per D-08, D-20, OVL-01).

Purpose: Complete OVL-01 product modal inventory with security-critical submit path presentation-only.

Output: SubmitPresetModal on Dialog; handlers byte-stable in behavior.
</objective>

## Phase Goal

**As a** web user of GSD Pi Config, **I want** product modals and the ⌘K palette on shadcn Dialog/Command with solid focus and exclusive open, **so that** overlays match Mist Sky chrome without changing share, import, load, submit, or field-jump behavior.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-modals-palette-overlays/03-01-SUMMARY.md
@.planning/phases/03-modals-palette-overlays/03-CONTEXT.md
@.planning/phases/03-modals-palette-overlays/03-UI-SPEC.md
@.planning/phases/03-modals-palette-overlays/03-RESEARCH.md
@.planning/phases/03-modals-palette-overlays/03-PATTERNS.md
@src/components/ui/dialog.tsx
@src/components/ui/button.tsx
@src/components/ui/input.tsx
@src/components/ui/textarea.tsx
@src/components/SubmitPresetModal.tsx
@src/pages/OAuthCallbackPage.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SubmitPresetModal Dialog shell + form fields (D-08)</name>
  <files>src/components/SubmitPresetModal.tsx</files>
  <read_first>
    - src/components/SubmitPresetModal.tsx (full — startOAuth, completeOAuthSubmit, scanForLeakedSecrets, sessionStorage keys, form state, success PR URL)
    - src/components/ShareModal.tsx (Dialog anatomy reference after Plan 02 if available; else RESEARCH Pattern 1)
    - .planning/phases/03-modals-palette-overlays/03-UI-SPEC.md Submit form fields + copywriting
    - .planning/phases/03-modals-palette-overlays/03-PATTERNS.md SubmitPresetModal section
    - .planning/phases/03-modals-palette-overlays/03-RESEARCH.md Security Domain + Pitfall 4/8
  </read_first>
  <behavior>
    - File imports Dialog + Button + Input + Textarea from @/components/ui/*
    - startOAuth still: cleanPrefs → serializePreferences → scanForLeakedSecrets → block with visible error listing leaks → sessionStorage oauth state + pending submit → GitHub authorize redirect
    - completeOAuthSubmit remains exported with same signature and state equality check
    - No console logging of OAuth codes or tokens
    - No uiClasses btn/btnPrimary/modalPanel; no hand-rolled window Escape
    - Titles/copy: Submit to gallery; Sign in with GitHub; Done; manual PR and copy share block paths remain
  </behavior>
  <action>
    Presentation-only restyle of Submit preset (per D-08, D-01, D-04, D-20, D-00e).

    1. Replace hand-rolled scrim/panel with controlled Dialog max-w-lg, rounded-none, header/body/footer per UI-SPEC. Map open/onClose to Dialog open/onOpenChange. Drop window Escape listener (D-13). Prefer mounted controlled Dialog for focus restore (D-14).
    2. Form: visible 12px muted labels + Input for slug and title, Textarea (~2 rows) for description — not placeholder-only labels. Primary Button default Sign in with GitHub (or Done on success); outline Cancel. Loading label Submitting… only if existing busy flag is set — do not redesign OAuth navigation or invent multi-step nested dialogs (Open Question 3 locked: leave busy logic as-is).
    3. Secret-scan / OAuth / config errors: soft danger text-xs with role=alert — never tooltip-only (D-20). Preserve Remove secrets before submitting listing. Preserve GitHub OAuth not configured + manual PR link + copy share block using existing buildShareablePreset path.
    4. Success: Pull request created + URL; primary Done. Keep SUBMIT_API_URL, sessionStorage key names, startOAuth body, and completeOAuthSubmit implementation unchanged in behavior — restyle classNames/JSX chrome only around them.
    5. Do not edit preferencesCore, api/submit-preset.ts, OAuthCallbackPage handlers (page already restyled Phase 2), Palette, or ConfigApp. Do not install AlertDialog. Do not log authorization codes.
    6. Verify with source greps + npm test + preferencesCore tests.

    Restyle modal UI only per user decision D-08 (no flow redesign).
  </action>
  <verify>
    <automated>npx vitest run src/lib/preferencesCore.test.ts && npm test && grep -q 'scanForLeakedSecrets' src/components/SubmitPresetModal.tsx && grep -q 'export async function completeOAuthSubmit' src/components/SubmitPresetModal.tsx && grep -q 'Dialog' src/components/SubmitPresetModal.tsx && ! grep -E 'btnPrimary|modalPanel' src/components/SubmitPresetModal.tsx && ! grep -E 'console\.(log|debug|info|warn|error)' src/components/SubmitPresetModal.tsx</automated>
  </verify>
  <done>Submit on Dialog with Input/Textarea/Button; OAuth and secret-scan handlers behavior-stable and visible.</done>
</task>

<task type="auto">
  <name>Task 2: Submit security source smoke (D-20, ASVS L1)</name>
  <files>src/components/SubmitPresetModal.tsx</files>
  <read_first>
    - src/components/SubmitPresetModal.tsx (post Task 1)
    - src/lib/preferencesCore.ts (exports used — do not edit)
    - .planning/phases/03-modals-palette-overlays/03-RESEARCH.md Security Domain
  </read_first>
  <action>
    Final security hygiene pass on Submit only (per D-20, ASVS L1) — no feature work.

    1. Confirm startOAuth still invokes scanForLeakedSecrets before any redirect and surfaces leak list in UI state (not only console).
    2. Confirm completeOAuthSubmit still validates oauth state from sessionStorage before posting; no code logged.
    3. Confirm share/redaction helpers are not reimplemented in the modal (call existing buildShareablePreset / preferencesCore paths only).
    4. If any restyle accidentally weakened error visibility or removed manual PR fallback, restore per UI-SPEC copywriting.
    5. Do not change preferencesCore implementations. Run full npm test.

    Protect redaction messaging and OAuth code non-logging per phase security gate.
  </action>
  <verify>
    <automated>npm test && grep -q 'scanForLeakedSecrets' src/components/SubmitPresetModal.tsx && grep -q 'completeOAuthSubmit' src/components/SubmitPresetModal.tsx && ! grep -E 'console\.(log|debug|info|warn|error)' src/components/SubmitPresetModal.tsx</automated>
  </verify>
  <done>Submit security anchors verified; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GitHub OAuth | Authorization code returns via callback route — never log |
| Submit modal → sessionStorage | Pending non-redacted cleaned markdown + oauth state (existing design) |
| Submit modal → /api/submit-preset | Server holds client secret; client posts after OAuth |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08 | Information Disclosure | Submit secret-scan UX | high | mitigate | Keep scanForLeakedSecrets gate + visible soft-danger alert (D-20); never tooltip-only |
| T-03-09 | Spoofing | completeOAuthSubmit state check | high | mitigate | Do not alter gsd-oauth-state equality check or session keys |
| T-03-10 | Information Disclosure | OAuth code in logs | high | mitigate | No console logging of code/tokens in Submit modal |
| T-03-11 | Elevation | SUBMIT_API_URL / handler redesign | medium | mitigate | D-08 restyle only — no API URL or flow changes |
| T-03-SC | Tampering | npm installs | low | accept | No package installs in this plan |
</threat_model>

<verification>
- Dialog + form primitives mounted
- scanForLeakedSecrets + completeOAuthSubmit present
- No console logging; npm test green
</verification>

<success_criteria>
- OVL-01 Submit covered
- D-08 handlers intact
- D-20 secret-scan UI visible
- ASVS L1: no OAuth code logging; no preferencesCore weaken
</success_criteria>

<output>
Create `.planning/phases/03-modals-palette-overlays/03-03-SUMMARY.md` when done
</output>
