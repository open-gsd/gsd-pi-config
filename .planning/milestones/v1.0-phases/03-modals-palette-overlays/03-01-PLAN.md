---
phase: 03-modals-palette-overlays
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/foundation.isolation.test.ts
  - src/components/ui/dialog.tsx
  - src/components/ui/command.tsx
  - src/components/ui/input-group.tsx
  - src/components/ui/dialog.import.test.ts
  - src/components/ui/command.import.test.ts
  - src/components/ui/input-group.import.test.ts
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - OVL-01
  - OVL-02
must_haves:
  truths:
    - "Official base-nova Dialog and Command primitives exist under src/components/ui (Base UI only — never product Radix Dialog)"
    - "Command peer input-group is present only because the official command registry requires it (D-24)"
    - "FND-03 allowlist expanded for dialog/command/input-group (+ import tests); card/select/sheet/drawer/popover/alert-dialog still forbidden"
    - "Dialog overlay scrim is soft dark ~60% with no product backdrop-blur (D-03); content radius none (D-00a)"
    - "Import-only tests for Dialog, Command, and input-group pass without DOM render"
    - "No product modal mount restyle in this plan — Plans 02–05 consume primitives"
    - "cmdk installed as official command dependency; package legitimacy OK"
  artifacts:
    - path: src/components/ui/dialog.tsx
      provides: "shadcn Dialog primitive (Base UI)"
      contains: "@base-ui/react"
    - path: src/components/ui/command.tsx
      provides: "shadcn Command primitive (cmdk + Dialog shell)"
    - path: src/components/ui/input-group.tsx
      provides: "command registry peer only"
    - path: src/components/ui/dialog.import.test.ts
      provides: "Import-only Dialog proof"
    - path: src/components/ui/command.import.test.ts
      provides: "Import-only Command proof"
    - path: src/lib/foundation.isolation.test.ts
      provides: "FND-03 allowlist requires dialog/command/input-group; forbids dump"
  key_links:
    - from: components.json
      to: "npx shadcn@4.13.1 add dialog command -y"
      via: "locked base-nova / css → index.web.css"
    - from: dialog.tsx Overlay classes
      to: Mist Sky overlay contract
      via: "bg-black/60, no backdrop-blur, rounded-none (D-03, D-00a)"
    - from: foundation.isolation UI_ALLOWLIST
      to: ui/*.tsx basenames
      via: "FND-03 Phase 3 expand (D-24)"
---

<objective>
Install official **Dialog + Command** (and required **input-group** peer) via pinned `shadcn@4.13.1`, apply Mist Sky overlay overrides, and expand FND-03 allowlist so later plans can restyle product overlays (per D-01, D-03, D-24, OVL-01/02).

Purpose: Primitives-first cutover — isolation and CLI install land together so CI never forbids the files we just added.

Output: `dialog.tsx`, `command.tsx`, `input-group.tsx`, import tests, expanded isolation allowlist, `cmdk` in lockfile.
</objective>

## Phase Goal

**As a** web user of GSD Pi Config, **I want** product modals and the ⌘K palette on shadcn Dialog/Command with solid focus and exclusive open, **so that** overlays match Mist Sky chrome without changing share, import, load, submit, or field-jump behavior.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/design/PALETTE.md
@.planning/phases/03-modals-palette-overlays/03-CONTEXT.md
@.planning/phases/03-modals-palette-overlays/03-UI-SPEC.md
@.planning/phases/03-modals-palette-overlays/03-RESEARCH.md
@.planning/phases/03-modals-palette-overlays/03-PATTERNS.md
@.planning/phases/03-modals-palette-overlays/03-VALIDATION.md
@.planning/phases/02-web-chrome-standalone-pages/02-02-SUMMARY.md
@components.json
@src/components/ui/button.tsx
@src/components/ui/button.import.test.ts
@src/lib/foundation.isolation.test.ts
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand FND-03 allowlist for Dialog/Command (D-24) — expect RED until install</name>
  <files>src/lib/foundation.isolation.test.ts</files>
  <read_first>
    - src/lib/foundation.isolation.test.ts (UI_ALLOWLIST + "does not dump card/dialog/select/command" suite)
    - .planning/phases/03-modals-palette-overlays/03-RESEARCH.md Pattern 4 FND-03 + Package Legitimacy Audit
    - .planning/phases/03-modals-palette-overlays/03-PATTERNS.md foundation.isolation section
    - .planning/phases/03-modals-palette-overlays/03-VALIDATION.md Wave 0 requirements
  </read_first>
  <behavior>
    - UI_ALLOWLIST includes dialog.tsx, command.tsx, input-group.tsx and matching *.import.test.ts basenames
    - Presence required: dialog.tsx, command.tsx, input-group.tsx must exist under src/components/ui
    - Still forbid dump basenames: card, select, sheet, drawer, popover, alert-dialog (not installed this phase per D-02)
    - Flip former Phase 1/2 forbid of dialog/command — those are Phase 3 targets, not forbidden
    - Desktop isolation and Button Base-UI-only asserts remain green-capable
  </behavior>
  <action>
    Update isolation tests only (no CLI install in this task) so Task 2 turns RED→GREEN for Dialog/Command (per D-24, FND-03).

    1. Expand UI_ALLOWLIST with: dialog.tsx, dialog.import.test.ts, command.tsx, command.import.test.ts, input-group.tsx, input-group.import.test.ts (keep existing button/input/textarea entries).
    2. Replace the test that forbids dialog/command presence with: require those three basenames present; forbid card, select, sheet, drawer, popover, alert-dialog (and any other non-allowlisted dump). Do not forbid dialog/command anymore.
    3. Keep components.json base-nova lock, desktop no-shadcn stack, Button no product Radix import asserts.
    4. Do not run shadcn add yet. Do not edit product modals, Palette, ConfigApp, preferencesCore, or desktop CSS.
    5. Run isolation once — expect RED on missing dialog/command/input-group files until Task 2.

    Cite D-24: only Dialog/Command (+ required peers); AlertDialog not required (D-02).
  </action>
  <verify>
    <automated>npx vitest run src/lib/foundation.isolation.test.ts ; test $? -ne 0</automated>
  </verify>
  <done>FND-03 contracts require dialog/command/input-group and forbid dump; tests RED until CLI install lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install Dialog+Command via pinned CLI + Mist Sky overrides + import tests (D-01, D-03, D-24)</name>
  <files>src/components/ui/dialog.tsx, src/components/ui/command.tsx, src/components/ui/input-group.tsx, src/components/ui/dialog.import.test.ts, src/components/ui/command.import.test.ts, src/components/ui/input-group.import.test.ts, package.json, package-lock.json, src/lib/foundation.isolation.test.ts</files>
  <read_first>
    - components.json (style base-nova, css path index.web.css, aliases)
    - src/components/ui/button.tsx and button.import.test.ts (Base UI + cn + import-only pattern)
    - .planning/phases/03-modals-palette-overlays/03-RESEARCH.md Standard Stack install + post-install override table + Package Legitimacy
    - .planning/phases/03-modals-palette-overlays/03-UI-SPEC.md Design System + Dialog anatomy scrim/radius
    - package.json (existing @base-ui/react, shadcn pin)
  </read_first>
  <behavior>
    - Same as Task 1 isolation asserts — all GREEN after install
    - dialog/command/input-group import as named exports without throwing
    - Dialog overlay class strings use black/60-level scrim and no product backdrop-blur; content uses rounded-none
    - No alert-dialog.tsx, card.tsx, sheet.tsx, drawer.tsx, popover.tsx, select.tsx under ui/
    - cmdk present in package-lock after add
  </behavior>
  <action>
    Install and lock primitives (per D-01, D-03, D-24, OVL-01/02).

    1. Run exactly: npx shadcn@4.13.1 add dialog command -y (official registry only). Expect dialog.tsx, command.tsx, input-group.tsx and npm install of cmdk. Do NOT add alert-dialog, sheet, drawer, popover, card, select, or --all.
    2. Immediately override registry defaults to Mist Sky / UI-SPEC (D-00a, D-03): Dialog Overlay → bg-black/60 (or equivalent ~60% black), strip product backdrop-blur; Dialog Content/Footer → rounded-none (not rounded-xl); footer linear border-t border-border (not rounded-b muted wash as product look); title sizing compatible with text-sm font-semibold when used; keep Base UI imports — never introduce product @radix-ui/react-dialog imports in dialog.tsx.
    3. CommandDialog / Command defaults: rounded-none; preserve ability for top-ish placement (pt-24 feel per D-09) via className on consumer — do not force centered-only if registry uses top-1/3; list max height consumer-owned. No product blur on Command dialog overlay.
    4. Add import-only tests mirroring button.import.test.ts: dialog.import.test.ts, command.import.test.ts, input-group.import.test.ts — export callable/object components; optional source read asserts for rounded-none / black/60 / no backdrop-blur on dialog overlay if classes live in the module.
    5. Verify package-lock includes cmdk; npm ls cmdk @base-ui/react. cmdk is OK per Package Legitimacy Audit (required peer of official command).
    6. Do not restyle ShareModal/Import/Load/Submit/Palette/ConfigApp this plan. Do not edit preferencesCore. Do not import shadcn/tailwind into index.desktop.css.
    7. Run isolation + ui import tests until GREEN; run full npm test.

    Using official shadcn@4.13.1 base-nova only per D-24 (research + components.json lock).
  </action>
  <verify>
    <automated>npx vitest run src/lib/foundation.isolation.test.ts src/components/ui/ && npm test && test -f src/components/ui/dialog.tsx && test -f src/components/ui/command.tsx && test -f src/components/ui/input-group.tsx && npm ls cmdk</automated>
  </verify>
  <done>Dialog + Command + input-group installed with Mist Sky overrides; FND-03 green; import tests green; no registry dump.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local ui primitives | CLI install adds third-party code (cmdk + generated components) |
| UI primitives → product overlays | Later plans mount Dialog/Command; no secrets in primitives |
| Desktop CSS entry | Must not gain shadcn/tailwind import from this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | shadcn CLI install | medium | mitigate | Pin shadcn@4.13.1 only; official registry; FND-03 forbids dump primitives |
| T-03-02 | Tampering | dialog.tsx Base UI vs Radix | medium | mitigate | Assert dialog uses @base-ui/react; no product @radix-ui/react-dialog import |
| T-03-03 | Information Disclosure | this plan | low | accept | No OAuth/share/redaction paths touched |
| T-03-SC | Tampering | npm/cmdk install | high | mitigate | Package Legitimacy: cmdk OK; only official command peer; isolation + npm ls after install |
</threat_model>

<verification>
- foundation.isolation.test.ts green for expanded allowlist + required dialog/command/input-group + forbid dump
- ui import tests green
- npm test green
- No alert-dialog/card/sheet/drawer/popover/select under src/components/ui
</verification>

<success_criteria>
- OVL-01/02 primitives ready (Dialog + Command)
- D-24 FND-03 allowlist expanded without registry dump
- D-03 Mist Sky scrim/radius overrides on Dialog defaults
- Desktop CSS isolation untouched
</success_criteria>

<output>
Create `.planning/phases/03-modals-palette-overlays/03-01-SUMMARY.md` when done
</output>
