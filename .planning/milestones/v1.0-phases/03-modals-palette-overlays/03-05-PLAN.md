---
phase: 03-modals-palette-overlays
plan: 05
type: execute
wave: 3
depends_on:
  - "02"
  - "03"
  - "04"
files_modified:
  - src/ConfigApp.tsx
  - src/lib/phase03.overlays.test.ts
  - src/index.desktop.css
autonomous: true
requirements:
  - OVL-01
  - OVL-02
  - OVL-03
must_haves:
  truths:
    - "ConfigApp enforces single open product overlay — opening palette closes modals and opening a modal closes palette (D-16)"
    - "⌘K shortcut and toolbar/start openers use exclusivity helpers, not independent stacked flags (D-16, OVL-03)"
    - "phase03.overlays.test.ts source contracts cover Dialog/Command usage, no uiClasses btn language on overlays, Share redaction keywords, Submit scan+completeOAuthSubmit, Palette scorers+shouldFilter, exclusivity helper presence"
    - "No nested Dialog stacks introduced; dirty confirms stay window.confirm (D-15, D-02)"
    - "Dual builds pass; desktop still no shadcn/tailwind import (ISO-01)"
    - "If shared Dialog semantic classes break desktop paint, only a minimal CSS variable bridge is added — not full desktop Mist Sky restyle (D-21, D-22)"
    - "preferencesCore redaction tests still green; no OAuth code logging regressions"
  artifacts:
    - path: src/ConfigApp.tsx
      provides: "closeAllOverlays + exclusive open helpers"
    - path: src/lib/phase03.overlays.test.ts
      provides: "OVL-01/02/03 source contracts"
    - path: src/index.desktop.css
      provides: "Optional minimal semantic var bridge only if dual-build smoke requires it"
  key_links:
    - from: openPalette / share / import / load / submit openers
      to: closeAllOverlays then set one flag
      via: "D-16 host exclusivity"
    - from: phase03.overlays.test.ts
      to: overlay source files + ConfigApp
      via: "readFileSync contracts (Phase 2 pattern)"
    - from: dual build
      to: ISO-01 isolation
      via: "npm run build:web && npm run build"
---

<objective>
Enforce **single-open overlay exclusivity** in ConfigApp, add **phase03 source contracts**, and gate **dual builds** (optional minimal desktop semantic bridge only if needed) (per D-16, D-21–D-22, OVL-03).

Purpose: Prevent dual focus traps; lock OVL-01/02/03 with automated source tests; prove web+desktop still build.

Output: ConfigApp exclusivity helpers; phase03.overlays.test.ts; green dual builds.
</objective>

## Phase Goal

**As a** web user of GSD Pi Config, **I want** product modals and the ⌘K palette on shadcn Dialog/Command with solid focus and exclusive open, **so that** overlays match Mist Sky chrome without changing share, import, load, submit, or field-jump behavior.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-modals-palette-overlays/03-02-SUMMARY.md
@.planning/phases/03-modals-palette-overlays/03-03-SUMMARY.md
@.planning/phases/03-modals-palette-overlays/03-04-SUMMARY.md
@.planning/phases/03-modals-palette-overlays/03-CONTEXT.md
@.planning/phases/03-modals-palette-overlays/03-UI-SPEC.md
@.planning/phases/03-modals-palette-overlays/03-RESEARCH.md
@.planning/phases/03-modals-palette-overlays/03-PATTERNS.md
@.planning/phases/03-modals-palette-overlays/03-VALIDATION.md
@src/ConfigApp.tsx
@src/lib/phase02.surfaces.test.ts
@src/lib/foundation.isolation.test.ts
@src/components/ShareModal.tsx
@src/components/ImportPreferencesModal.tsx
@src/components/LoadPresetModal.tsx
@src/components/SubmitPresetModal.tsx
@src/components/Palette.tsx
@src/index.desktop.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ConfigApp exclusive overlay openers (D-16, OVL-03)</name>
  <files>src/ConfigApp.tsx</files>
  <read_first>
    - src/ConfigApp.tsx (paletteOpen, shareOpen, importPrefsOpen, loadPresetOpen, submitOpen setters; useShortcuts setPaletteOpen; toolbar/WebStartPanel openers; modal mounts ~654–689)
    - .planning/phases/03-modals-palette-overlays/03-RESEARCH.md Pattern 3 Host exclusivity + Pitfall 3
    - .planning/phases/03-modals-palette-overlays/03-PATTERNS.md ConfigApp section
  </read_first>
  <behavior>
    - closeAllOverlays (or equivalent) clears palette + share + import + load + submit flags
    - openPalette / openShare / openImport / openLoad / openSubmit (names flexible) each close others then open one
    - useShortcuts ⌘K path calls exclusive openPalette, not raw setPaletteOpen(true) while leaving modals open
    - Toolbar and WebStartPanel openers for import/load/share/submit use exclusive openers
    - onClose handlers may still set single flag false; no nested Dialog; window.confirm dirty paths unchanged
    - No full ConfigApp chrome restyle (WEB-04 Phase 4 out of scope)
  </behavior>
  <action>
    Wire host exclusivity only (per D-16, OVL-03, D-15).

    1. Add useCallback closeAllOverlays that sets paletteOpen, shareOpen, importPrefsOpen, loadPresetOpen, submitOpen all false.
    2. Add exclusive open helpers: before opening any one overlay, closeAllOverlays then set the target true. Apply to: palette (including shortcutCtx / useShortcuts path that currently setPaletteOpen(true)), sharePreset after content ready, import openers, load openers, submit openers (toolbar + WebStartPanel).
    3. Keep modal mount APIs { open, onClose } unchanged. Do not convert dirty window.confirm to Dialog/AlertDialog (D-02, D-15). Do not restyle sidebar/toolbar chrome (Phase 4).
    4. GalleryPage route-local preview does not need ConfigApp exclusivity.
    5. Do not edit preferencesCore or OAuth complete flow.

    Single open overlay per D-16.
  </action>
  <verify>
    <automated>npm test && grep -E 'closeAll|openPalette|setPaletteOpen\(false\)' src/ConfigApp.tsx | head -40 && grep -q 'paletteOpen' src/ConfigApp.tsx</automated>
  </verify>
  <done>ConfigApp closes other product overlays before opening one; shortcuts included.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: phase03.overlays source contracts + dual builds (OVL-01/02/03)</name>
  <files>src/lib/phase03.overlays.test.ts, src/index.desktop.css</files>
  <read_first>
    - src/lib/phase02.surfaces.test.ts (source-contract pattern)
    - src/lib/foundation.isolation.test.ts (desktop ISO asserts)
    - Overlay sources after Plans 02–04: ShareModal, ImportPreferencesModal, LoadPresetModal, SubmitPresetModal, Palette
    - src/ConfigApp.tsx (Task 1 exclusivity)
    - src/index.desktop.css (only if dual-build visual/class gap needs bridge)
    - .planning/phases/03-modals-palette-overlays/03-RESEARCH.md Validation Architecture + Open Question 1 resolution (shared first, bridge if needed)
    - .planning/phases/03-modals-palette-overlays/03-VALIDATION.md
  </read_first>
  <behavior>
    - phase03.overlays.test.ts: each overlay file imports dialog and/or command from @/components/ui; no uiClasses btn/btnPrimary/modalPanel import language on overlays
    - Share source still mentions key, token, secret, password
    - Submit still references scanForLeakedSecrets and exports completeOAuthSubmit
    - Palette still has scoreField, scoreSection, MAX_RESULTS = 50, shouldFilter
    - ConfigApp source shows exclusivity pattern (close-all or multi-flag clear before open)
    - Optional: no backdrop-blur on product overlay class strings
    - foundation isolation still green; preferencesCore tests green
    - npm run build:web && npm run build succeed
  </behavior>
  <action>
    Lock phase contracts and dual-build gate (per OVL-01/02/03, D-16, D-21–D-22, ISO-01).

    1. Create src/lib/phase03.overlays.test.ts mirroring phase02.surfaces.test.ts (Vitest node, readFileSync, no jsdom). OVERLAYS list: ShareModal, ImportPreferencesModal, LoadPresetModal, SubmitPresetModal, Palette. Assert Dialog/Command imports; forbid uiClasses btn/btnPrimary/modalPanel (and modalPanel) on those files; Share redaction keywords; Submit scanForLeakedSecrets + completeOAuthSubmit; Palette scorers + MAX_RESULTS + shouldFilter; ConfigApp exclusivity helper signals; optional no backdrop-blur on overlay sources.
    2. Run full npm test including foundation.isolation + preferencesCore + phase03.
    3. Run dual builds: npm run build:web && npm run build. If desktop build succeeds but shared Dialog classes would clearly lack tokens (only if evidence of broken semantic vars on desktop CSS), add **minimal** desktop CSS variable bridge mapping only overlay-needed semantics (--background, --foreground, --card, --popover, --primary, --border, --muted-foreground, --destructive, --ring, --input, --accent) onto existing --gsd-* — not full Mist Sky desktop restyle (D-21, D-22). Prefer no desktop CSS change if builds and isolation stay green without it (Open Question 1 locked: shared first, bridge only if needed).
    4. Never import shadcn/tailwind into index.desktop.css. Do not weaken preferencesCore. Do not add AlertDialog dump.
    5. Leave VALIDATION.md status updates optional; execution SUMMARY should note dual-build result.

    Phase gate: source contracts + dual builds + security tests green.
  </action>
  <verify>
    <automated>npx vitest run src/lib/phase03.overlays.test.ts src/lib/foundation.isolation.test.ts src/lib/preferencesCore.test.ts && npm test && npm run build:web && npm run build</automated>
  </verify>
  <done>phase03 contracts green; exclusivity covered; dual builds pass; desktop isolation holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host overlay state → Dialog traps | Dual open = dual focus traps (D-16) |
| Source tests → repo FS | Trusted local readFileSync only |
| Desktop CSS bridge | Presentation only; must not load shadcn stack |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-14 | Tampering | Dual focus traps / background actions | medium | mitigate | ConfigApp exclusivity D-16; modal Dialog default |
| T-03-15 | Information Disclosure | phase03 asserts Share/Submit | high | mitigate | Source tests require redaction keywords + scanForLeakedSecrets |
| T-03-16 | Information Disclosure | preferencesCore | high | mitigate | preferencesCore.test must stay green; no redaction edits |
| T-03-17 | Tampering | desktop CSS bridge scope | medium | mitigate | Minimal var bridge only; isolation forbids shadcn/tailwind on desktop |
| T-03-SC | Tampering | npm installs | low | accept | No package installs this plan |
</threat_model>

<verification>
- phase03.overlays.test.ts green
- foundation.isolation + preferencesCore + full npm test green
- npm run build:web && npm run build succeed
- ConfigApp exclusivity present in source
</verification>

<success_criteria>
- OVL-01, OVL-02, OVL-03 phase requirements locked by source contracts + exclusivity
- D-16 single open overlay
- ISO-01 desktop isolation preserved
- ASVS L1: redaction messaging protected; no OAuth code logging; no preferencesCore weaken
</success_criteria>

<output>
Create `.planning/phases/03-modals-palette-overlays/03-05-SUMMARY.md` when done
</output>
