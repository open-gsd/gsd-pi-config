---
phase: 03-modals-palette-overlays
plan: 04
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/components/Palette.tsx
autonomous: true
requirements:
  - OVL-02
  - OVL-03
must_haves:
  truths:
    - "Palette shell is shadcn Command inside Dialog (CommandDialog or equivalent) with top-ish pt-24 feel (D-09)"
    - "scoreField, scoreSection, and MAX_RESULTS = 50 remain authoritative ranking (D-10)"
    - "Command shouldFilter is false so cmdk does not re-rank pre-sorted results (D-10)"
    - "Active result rows use left primary edge 2–3px + soft wash, not only full accent fill (D-11)"
    - "Empty state is quiet No matches muted inline (D-12)"
    - "onNavigate(section, fieldPath?) then close is preserved; open resets query and cursor"
    - "No product backdrop-blur scrim; flat black/60 (D-03); no uiClasses modalPanel/btn on palette"
    - "Keyboard navigate/select/dismiss remains usable via Command/Dialog (OVL-02/03)"
  artifacts:
    - path: src/components/Palette.tsx
      provides: "Command-in-Dialog palette with existing scorers"
      contains: "shouldFilter"
  key_links:
    - from: scoreField/scoreSection useMemo
      to: CommandItem list
      via: "pre-sorted results map; shouldFilter false (D-10)"
    - from: pick/onSelect
      to: onNavigate + onClose
      via: "unchanged field-jump contract"
    - from: CommandDialog open
      to: Dialog focus trap/ESC
      via: "D-13 drop redundant window Escape"
---

<objective>
Restyle ⌘K **Palette** to Command-in-Dialog with Mist Sky list chrome while **keeping scoring/ranking and navigate contract** (per D-09–D-13, OVL-02, OVL-03).

Purpose: Keyboard field-jump stays familiar; only presentation and primitive focus change.

Output: Palette.tsx on Command/Dialog; shouldFilter false; scorers intact.
</objective>

## Phase Goal

**As a** web user of GSD Pi Config, **I want** product modals and the ⌘K palette on shadcn Dialog/Command with solid focus and exclusive open, **so that** overlays match Mist Sky chrome without changing share, import, load, submit, or field-jump behavior.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-modals-palette-overlays/03-01-SUMMARY.md
@.planning/phases/03-modals-palette-overlays/03-CONTEXT.md
@.planning/phases/03-modals-palette-overlays/03-UI-SPEC.md
@.planning/phases/03-modals-palette-overlays/03-RESEARCH.md
@.planning/phases/03-modals-palette-overlays/03-PATTERNS.md
@src/components/ui/command.tsx
@src/components/ui/dialog.tsx
@src/components/Palette.tsx
@src/lib/fields.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Command-in-Dialog shell + shouldFilter false (D-09, D-10, D-13)</name>
  <files>src/components/Palette.tsx</files>
  <read_first>
    - src/components/Palette.tsx (full — scoreField, scoreSection, MAX_RESULTS, keyboard handlers, open effect, row render)
    - src/components/ui/command.tsx (CommandDialog, Command, CommandInput, CommandList, CommandItem, CommandEmpty exports)
    - .planning/phases/03-modals-palette-overlays/03-RESEARCH.md Pattern 2 + Pitfall 2
    - .planning/phases/03-modals-palette-overlays/03-UI-SPEC.md Command palette anatomy + copywriting
    - .planning/phases/03-modals-palette-overlays/03-PATTERNS.md Palette section
  </read_first>
  <behavior>
    - scoreField and scoreSection function bodies and thresholds remain; MAX_RESULTS stays 50
    - Command root sets shouldFilter={false} (or shouldFilter={false} equivalent prop) so only pre-sorted results render
    - Shell uses CommandDialog or Dialog+Command with open/onOpenChange → onClose
    - Placeholder Jump to section or field…; empty No matches; footer kbd strip 12px muted with result count
    - No backdrop-blur on palette overlay classes; no modalPanel/btn/btnPrimary from uiClasses
    - Drop redundant window Escape when Dialog owns dismiss
  </behavior>
  <action>
    Replace hand-rolled palette chrome with Command-in-Dialog (per D-09, D-10, D-13, OVL-02).

    1. Keep props { open, onClose, onNavigate, sectionGroups? }. Keep pure scorers and results useMemo (sections + fields, sort by score, slice MAX_RESULTS = 50) — restyle list only (D-10). Do not change scorer scoring weights or MAX_RESULTS.
    2. Mount CommandDialog (or Dialog wrapping Command) with open/onOpenChange mapping to onClose. Top-ish placement: preserve pt-24 feel (D-09 / UI-SPEC default). max-w-xl, rounded-none, flat scrim black/60, no backdrop-blur (D-03). showCloseButton false preferred for palette.
    3. Command shouldFilter={false}. Controlled CommandInput bound to existing query state; placeholder Jump to section or field…. On open: clear query + cursor 0 and focus input (existing effect intent).
    4. Map pre-sorted results to CommandItem with stable value keys; onSelect → existing pick → onNavigate then onClose. Prefer cmdk selection for ↑↓/Enter when CommandItem covers it; remove duplicate hand-rolled arrow handlers only if cmdk fully replaces them without behavior loss.
    5. Remove fixed inset-0 hand scrim, modalPanel, uiClasses buttons, and window Escape listener if Dialog dismisses on Escape (D-13).
    6. Do not edit ConfigApp exclusivity yet (Plan 05). Do not touch modals or preferencesCore.

    Keep existing scoring/ranking per D-10 (cmdk must not re-filter).
  </action>
  <verify>
    <automated>npm test && grep -q 'MAX_RESULTS = 50' src/components/Palette.tsx && grep -q 'function scoreField' src/components/Palette.tsx && grep -q 'function scoreSection' src/components/Palette.tsx && grep -q 'shouldFilter' src/components/Palette.tsx && grep -E 'CommandDialog|from ["'']@/components/ui/command["'']' src/components/Palette.tsx && ! grep -E 'backdrop-blur' src/components/Palette.tsx && ! grep -E 'btnPrimary|modalPanel' src/components/Palette.tsx</automated>
  </verify>
  <done>Palette on Command/Dialog with shouldFilter false and scorers preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Linear active rows + empty/footer Mist Sky (D-11, D-12)</name>
  <files>src/components/Palette.tsx</files>
  <read_first>
    - src/components/Palette.tsx (post Task 1)
    - .planning/phases/03-modals-palette-overlays/03-UI-SPEC.md Palette row content + type rules
    - .planning/phases/02-web-chrome-standalone-pages/02-UI-SPEC.md choice-row grammar if needed for left-edge pattern
  </read_first>
  <behavior>
    - Active/selected row: border-l primary ~2–3px + bg-primary/10 (or accent-soft) — not only full solid accent bar
    - Section rows: group label 12 uppercase muted + title 14/600; meta section
    - Field rows: label 14/600 + optional hint; mono section · path meta
    - Empty: No matches text-xs muted centered
    - Footer: ↑↓ navigate · ↵ open · esc close + result count at 12px (no 10px type)
    - min-h-10 rows; rounded-none; no active scale press theater
  </behavior>
  <action>
    Finish Mist Sky list chrome on Palette (per D-11, D-12, OVL-02).

    1. Style CommandItem selected/active state with left primary edge 2–3px + soft primary wash (D-11). Remove full-only accent fill and active:scale press if still present.
    2. Row content layout per UI-SPEC section vs field. Truncate long labels; mono path meta 12 muted.
    3. CommandEmpty: No matches quiet centered. Footer kbd strip 12px muted with pluralized result count.
    4. List max-h-96 (discretion locked — no virtualization). Ensure onNavigate contract unchanged.
    5. Run npm test + greps for shouldFilter, scorers, no blur.

    Linear list + left accent per D-11; quiet empty per D-12.
  </action>
  <verify>
    <automated>npm test && grep -q 'shouldFilter' src/components/Palette.tsx && grep -q 'MAX_RESULTS = 50' src/components/Palette.tsx && grep -q 'No matches' src/components/Palette.tsx && grep -q 'onNavigate' src/components/Palette.tsx</automated>
  </verify>
  <done>Palette list chrome Mist Sky; empty/footer quiet; scoring still authoritative.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Palette → section navigation | Client-only field jump; no network |
| Command/cmdk dependency | Already installed Plan 01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-12 | Tampering | Palette ranking integrity | medium | mitigate | shouldFilter false + keep scoreField/scoreSection/MAX_RESULTS (D-10) |
| T-03-13 | Information Disclosure | Palette | low | accept | No secrets in field registry search UI |
| T-03-SC | Tampering | npm installs | low | accept | No new installs this plan |
</threat_model>

<verification>
- Command/Dialog imports present; shouldFilter present; scorers + MAX_RESULTS 50 present
- No backdrop-blur; no uiClasses button/modal panel language
- npm test green
</verification>

<success_criteria>
- OVL-02 Command palette restyled
- D-09–D-12 list + scoring contracts met
- OVL-03 Dialog-owned dismiss/focus for palette shell (host exclusivity in Plan 05)
</success_criteria>

<output>
Create `.planning/phases/03-modals-palette-overlays/03-04-SUMMARY.md` when done
</output>
