---
phase: 02-web-chrome-standalone-pages
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/components/ui/button.tsx
  - src/components/ui/input.tsx
  - src/components/ui/textarea.tsx
  - src/components/ui/input.import.test.ts
  - src/components/ui/textarea.import.test.ts
  - src/components/ui/button.import.test.ts
  - src/lib/foundation.isolation.test.ts
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - WEB-06
  - WEB-03
must_haves:
  truths:
    - "Official shadcn Input and Textarea exist under src/components/ui (Base UI only — never Radix)"
    - "FND-03 allowlist expanded for input/textarea (+ import tests); card/dialog/select/command still forbidden"
    - "Button default language is linear: radius none, default size hit target ≥40px"
    - "Button variants cover primary filled, outline secondary, soft destructive (not solid red fill)"
    - "buttonVariants export remains for Link/a styling without nested interactive"
    - "Import-only tests for Button, Input, Textarea pass without DOM render"
    - "No product page mount of Input/Textarea yet (Gallery/Wizard in later plans)"
    - "UI Considerations: primitive focus/disabled defaults present; no empty/loading product surfaces owned here"
  artifacts:
    - path: src/components/ui/input.tsx
      provides: "shadcn Input primitive"
    - path: src/components/ui/textarea.tsx
      provides: "shadcn Textarea primitive"
    - path: src/components/ui/button.tsx
      provides: "Linear Button language overrides for Phase 2"
      contains: "rounded-none"
    - path: src/components/ui/input.import.test.ts
      provides: "Import-only Input proof"
    - path: src/components/ui/textarea.import.test.ts
      provides: "Import-only Textarea proof"
  key_links:
    - from: components.json
      to: npx shadcn@4.13.1 add input textarea
      via: "locked base-nova / css → index.web.css"
    - from: button.tsx buttonVariants
      to: Phase 2 surfaces (plans 03–04)
      via: "WEB-06 single button system"
    - from: foundation.isolation UI_ALLOWLIST
      to: ui/*.tsx basenames
      via: "FND-03 expanded allowlist"
---

<objective>
Add **Input + Textarea** via pinned official shadcn CLI and lock **linear Button language** (radius 0, ≥40px, soft danger) so shell/pages can mount a single button/input system (per D-05–D-08, D-15, FND-03 expand).

Purpose: Primitives ready before WebShell/pages; expand FND-03 allowlist in the same wave as CLI add so isolation stays green.

Output: `input.tsx`, `textarea.tsx`, import tests, updated `button.tsx`, expanded isolation allowlist.
</objective>

## Phase Goal

**As a** web user of GSD Pi Config, **I want** shared chrome and standalone routes fully on Mist Sky shadcn with consistent buttons and quiet states, **so that** the site looks cohesive without changing product flows.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-web-chrome-standalone-pages/02-01-SUMMARY.md
@.planning/phases/02-web-chrome-standalone-pages/02-CONTEXT.md
@.planning/phases/02-web-chrome-standalone-pages/02-UI-SPEC.md
@.planning/phases/02-web-chrome-standalone-pages/02-RESEARCH.md
@.planning/phases/02-web-chrome-standalone-pages/02-PATTERNS.md
@.planning/phases/02-web-chrome-standalone-pages/02-VALIDATION.md
@.planning/phases/01-foundation-isolation-theme-bridge/01-03-SUMMARY.md
@components.json
@src/components/ui/button.tsx
@src/components/ui/button.import.test.ts
@src/lib/foundation.isolation.test.ts
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand FND-03 allowlist + add Input/Textarea + import tests (D-15)</name>
  <files>src/lib/foundation.isolation.test.ts, src/components/ui/input.tsx, src/components/ui/textarea.tsx, src/components/ui/input.import.test.ts, src/components/ui/textarea.import.test.ts, package.json, package-lock.json</files>
  <read_first>
    - components.json (style base-nova, css path, aliases)
    - src/lib/foundation.isolation.test.ts (UI_ALLOWLIST + forbidden dump list that currently blocks input)
    - src/components/ui/button.import.test.ts (import-only pattern)
    - .planning/phases/02-web-chrome-standalone-pages/02-RESEARCH.md Pattern 2 + Package Legitimacy Audit + Pitfall 3
    - package.json (existing @base-ui/react, shadcn pin)
  </read_first>
  <behavior>
    - Input and Textarea modules import without throw
    - Input/Textarea sources use @/lib/utils cn and no @radix-ui packages
    - UI_ALLOWLIST accepts button + input + textarea (+ their *.import.test.ts)
    - Forbidden dump list still blocks card, dialog, select, command (input/textarea/label no longer blanket-forbidden)
  </behavior>
  <action>
    Expand the primitive set for wizard/gallery fields (per D-15) without registry dump (FND-03).

    1. **Before or in the same commit as CLI add**, update `foundation.isolation.test.ts`:
       - Expand `UI_ALLOWLIST` to include `input.tsx`, `input.import.test.ts`, `textarea.tsx`, `textarea.import.test.ts` (and `label.tsx` only if you choose to add Label — prefer plain HTML labels per RESEARCH; skip Label primitive unless needed).
       - Change the dump forbid list so `input` / `textarea` are **allowed**; keep forbidding `card`, `dialog`, `select`, `command`.
    2. Run pinned official CLI only: `npx shadcn@4.13.1 add input textarea -y` (no `--all`, no third-party registries). Do not change components.json irreversible fields.
    3. Diff generated files: ensure imports use `@/lib/utils`; assert no `@radix-ui/` imports. If Input fails to resolve `@base-ui/react/input`, reinstall peer `@base-ui/react@^1.6.0` only (Phase 1 peer-gap pattern) — no new package names without legitimacy.
    4. Override registry defaults on generated Input/Textarea for linear grammar: effective square corners (`rounded-none` or `rounded-[var(--radius)]` with radius 0) and Input min height ≥40px (`min-h-10` / `h-10`) where defaults are `h-8` / `rounded-lg`.
    5. Add import-only tests mirroring `button.import.test.ts` (typeof export checks; no React render / no jsdom).
    6. Prefer plain `<label>` for wizard later — do not add Label unless required for shared styling.
    7. Run: isolation + new import tests + full `npm test`.

    Package legitimacy: CLI + existing deps only; SUS false-positive packages already human-approved in Phase 1 — no new [SUS] installs.
  </action>
  <verify>
    <automated>npx vitest run src/lib/foundation.isolation.test.ts src/components/ui/input.import.test.ts src/components/ui/textarea.import.test.ts src/components/ui/button.import.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ui/input.tsx and textarea.tsx exist
    - No @radix-ui imports in ui/*
    - UI_ALLOWLIST includes input and textarea files
    - Forbidden dump still blocks card/dialog/select/command
    - Import tests exit 0
    - foundation.isolation.test.ts exits 0
  </acceptance_criteria>
  <done>Input + Textarea installed under FND-03 allowlist with import-only proofs green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Button linear language overrides (D-05, D-06, D-07, D-08, D-23)</name>
  <files>src/components/ui/button.tsx, src/components/ui/button.import.test.ts</files>
  <read_first>
    - src/components/ui/button.tsx (current rounded-lg, h-8 defaults)
    - .planning/phases/02-web-chrome-standalone-pages/02-UI-SPEC.md Button language table
    - .planning/phases/02-web-chrome-standalone-pages/02-RESEARCH.md Pattern 3 CVA overrides table
    - src/components/ui/button.import.test.ts
  </read_first>
  <behavior>
    - buttonVariants({ variant: "default" }) class string includes rounded-none (or radius var that is 0) and min-h-10 or h-10 for default size
    - default / outline / destructive variants still resolve to non-empty class strings
    - destructive is soft (text/border or soft wash) — not a solid harsh red fill utility as the primary destructive treatment
  </behavior>
  <action>
    Lock WEB-06 button language on the shared primitive (per D-05, D-06, D-07, D-08, D-23).

    1. Edit `buttonVariants` base classes: replace `rounded-lg` with `rounded-none` (or `rounded-[var(--radius)]` with radius 0). Apply same radius discipline on size variants that hardcode rounded corners.
    2. Raise default size hit target to ≥40px (`h-10` / `min-h-10` + horizontal padding as needed). Adjust sm/lg if they would undercut page CTAs; row actions may use default with min-h-10.
    3. Primary (default): filled `bg-primary text-primary-foreground`; prefer hover via `--primary-hover` if Plan 01 defined it, else keep primary/80 approximation.
    4. Outline secondary: 1px border, transparent/surface fill (registry outline is fine with radius override).
    5. Destructive soft danger (D-07): keep soft wash/text treatment (registry soft destructive) **or** outline + destructive border/text — **not** solid red filled buttons. No destructive CTA required on Phase 2 routes; reserve language only.
    6. Keep exporting `Button` and `buttonVariants`. Do not mount Button on product routes this plan (Plans 03–04). Do not edit WebShell/pages.
    7. Extend button.import.test.ts lightly if useful (e.g. default class string contains rounded-none / min-h-10) without DOM render.
    8. Run button import + isolation + full `npm test`.

    Per D-08: product surfaces still use uiClasses until later plans; this plan only prepares the single system.
  </action>
  <verify>
    <automated>npx vitest run src/components/ui/button.import.test.ts src/lib/foundation.isolation.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - button.tsx base uses rounded-none (or equivalent radius-0)
    - default size is ≥40px height
    - buttonVariants still exports all required variant keys
    - button.import.test.ts exits 0
    - No product files import Button yet (optional grep: Gallery/Wizard still on uiClasses)
  </acceptance_criteria>
  <done>Linear Button language ready for shell/pages; primitives import-green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shadcn CLI → owned source under components/ui | Generated code becomes first-party; must stay Base UI |
| npm lockfile | Only existing/approved peers; no new [SLOP] packages |
| FND-03 allowlist | Prevents accidental full registry dump |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Tampering | shadcn add input/textarea | medium | mitigate | Pin CLI 4.13.1; official registry only; grep no @radix-ui; lockfile review |
| T-02-05 | Tampering | FND-03 allowlist bypass | medium | mitigate | Isolation tests still forbid card/dialog/select/command dump |
| T-02-06 | Information Disclosure | primitives | low | accept | No secret handling in Input/Textarea |
| T-02-SC | Tampering | npm/shadcn installs | high | mitigate | No new package names beyond Phase 1 set; peer reinstall only if required; legitimacy table already OK for existing deps |
</threat_model>

<verification>
- Import tests for button/input/textarea green
- foundation.isolation FND-03 allowlist green
- npm test green
</verification>

<success_criteria>
- Input + Textarea available for Wizard/Gallery (D-15)
- Button linear language matches UI-SPEC (D-05–D-07, D-23)
- FND-03 still blocks registry dump
</success_criteria>

<output>
Create `.planning/phases/02-web-chrome-standalone-pages/02-02-SUMMARY.md` when done
</output>
