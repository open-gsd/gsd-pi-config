---
phase: 02-web-chrome-standalone-pages
plan: 04
type: execute
wave: 4
depends_on:
  - "03"
files_modified:
  - src/pages/GalleryPage.tsx
  - src/pages/WizardPage.tsx
  - src/components/WebStartPanel.tsx
  - src/pages/OAuthCallbackPage.tsx
  - src/lib/phase02.surfaces.test.ts
autonomous: true
requirements:
  - WEB-02
  - WEB-03
  - WEB-05
  - WEB-06
  - WEB-07
must_haves:
  truths:
    - "Gallery is linear list rows with single search Input, quiet load/empty/error, Use preset primary + Preview outline"
    - "Gallery distinguishes catalog-empty vs filtered-empty copy per UI-SPEC"
    - "Wizard is single-page form with linear choice rows (left-edge active), Input+Textarea meta, bottom CTAs"
    - "WebStartPanel keeps 3 steps + 3 CTAs + Mist Sky kicker Git · Ship · Done"
    - "OAuth callback renders minimal status inside WebShell; quiet loading/error; no OAuth code logging"
    - "Phase 2 surface files do not import btn/btnPrimary/btnSegment/choiceBtn from uiClasses"
    - "Domain handlers preserved: fetchPresetIndex, use/preview preset, applyMode/ProfilePreset, completeOAuthSubmit, start CTAs"
    - "Loaded ConfigApp sidebar/toolbar not restyled (WEB-04 / D-24 out of scope)"
    - "UI Considerations: gallery load/empty/error/populated covered; wizard empty meta OK; OAuth load/error covered; start panel is empty-editor state"
  artifacts:
    - path: src/pages/GalleryPage.tsx
      provides: "Restyled gallery route (WEB-02, WEB-07)"
    - path: src/pages/WizardPage.tsx
      provides: "Restyled wizard route (WEB-03)"
    - path: src/components/WebStartPanel.tsx
      provides: "Restyled empty start panel"
    - path: src/pages/OAuthCallbackPage.tsx
      provides: "WebShell-wrapped OAuth status (WEB-05)"
    - path: src/lib/phase02.surfaces.test.ts
      provides: "WEB-06 surface source contracts"
  key_links:
    - from: GalleryPage CTAs
      to: Button / buttonVariants / Input
      via: "WEB-06 + D-09–D-12"
    - from: WizardPage choices
      to: linear choice row CSS
      via: "D-13 left-edge primary"
    - from: OAuthCallbackPage
      to: WebShell active=editor
      via: "D-19 wrap"
    - from: phase02.surfaces.test.ts
      to: Phase 2 surface sources
      via: "no uiClasses btn* / OAuth mentions WebShell"
---

<objective>
Restyle **standalone pages and empty start** onto Mist Sky shadcn — Gallery, Wizard, WebStartPanel, OAuth — and lock WEB-06 with a surface source contract test (per D-09–D-20, D-22, WEB-02/03/05/06/07).

Purpose: Complete Tokens → Shell → Pages cutover so all Phase 2 routes share one button language and quiet states without domain rewrites.

Output: Restyled page components; `phase02.surfaces.test.ts` green; dual builds green.
</objective>

## Phase Goal

**As a** web user of GSD Pi Config, **I want** shared chrome and standalone routes fully on Mist Sky shadcn with consistent buttons and quiet states, **so that** the site looks cohesive without changing product flows.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-web-chrome-standalone-pages/02-01-SUMMARY.md
@.planning/phases/02-web-chrome-standalone-pages/02-02-SUMMARY.md
@.planning/phases/02-web-chrome-standalone-pages/02-03-SUMMARY.md
@.planning/phases/02-web-chrome-standalone-pages/02-CONTEXT.md
@.planning/phases/02-web-chrome-standalone-pages/02-UI-SPEC.md
@.planning/phases/02-web-chrome-standalone-pages/02-RESEARCH.md
@.planning/phases/02-web-chrome-standalone-pages/02-PATTERNS.md
@.planning/phases/02-web-chrome-standalone-pages/02-VALIDATION.md
@src/pages/GalleryPage.tsx
@src/pages/WizardPage.tsx
@src/components/WebStartPanel.tsx
@src/pages/OAuthCallbackPage.tsx
@src/components/WebShell.tsx
@src/components/ui/button.tsx
@src/components/ui/input.tsx
@src/components/ui/textarea.tsx
@src/lib/uiClasses.ts
@src/App.web.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gallery linear list + Input search + quiet states (D-09–D-12, WEB-02, WEB-07)</name>
  <files>src/pages/GalleryPage.tsx</files>
  <read_first>
    - src/pages/GalleryPage.tsx (full — handlers, loadingSlug, ShareModal, filtered search)
    - src/components/ui/button.tsx (Button, buttonVariants)
    - src/components/ui/input.tsx
    - .planning/phases/02-web-chrome-standalone-pages/02-UI-SPEC.md GalleryPage + Copywriting Contract
    - .planning/phases/02-web-chrome-standalone-pages/02-RESEARCH.md domain handlers table + Gallery search example
  </read_first>
  <behavior>
    - fetchPresetIndex / fetchPresetMarkdown / setWebDraft / buildShareablePreset / ShareModal open paths unchanged in behavior
    - No imports of btn, btnPrimary, heading, prose from uiClasses (use semantic text classes)
    - Search uses shadcn Input type=search with accessible name
    - Row actions: primary Use preset + outline Preview
    - Loading copy Loading presets…; error role=alert soft danger; empty states per UI-SPEC split
  </behavior>
  <action>
    Restyle Gallery only (WEB-02, WEB-07, D-09–D-12, D-08) — presentation wiring; preserve domain handlers.

    1. Remove `uiClasses` imports (`btn`, `btnPrimary`, `heading`, `prose`). Use `text-foreground` / `text-muted-foreground` / `text-primary` / `text-destructive` and type scale from UI-SPEC (title text-xl font-semibold; body text-sm).
    2. Header CTAs: Create new preset → `Link` with `buttonVariants({ variant: "default" })` + min-h-10 rounded-none; Submit via PR → outline `buttonVariants` on external `<a>`. Never nest Button around Link.
    3. Search row: shadcn `Input type="search"` full width, placeholder “Search presets…”, `aria-label="Search presets"` (or visible label); controlled `query` state as today. Refresh list = outline `Button` (copy **Refresh list** per UI-SPEC; keep loadIndex handler). Wire controlled input carefully — if Base UI Input needs `onValueChange`, use that; verify typing still updates filter.
    4. List: keep **linear list** (border + divide-y), not card grid (D-09). Row pad p-4 / sm:p-5; title, description, quiet tags (text-xs py-1), author mono text-xs, actions shrink-0.
    5. Row actions: Use preset = `Button variant="default"` (loading: disabled + “Loading…” when loadingSlug matches); Preview = outline `Button`. Keep existing use/preview handlers and ShareModal.
    6. Quiet states (WEB-07, D-11): loading “Loading presets…” muted text only; error soft danger `role="alert"`; catalog empty vs filtered empty **distinct copy** per UI-SPEC Copywriting Contract (No presets found… vs No presets match your search…).
    7. Do not rewrite catalog fetch, redaction, or ShareModal system (Phase 3). Light token inheritance on ShareModal only if required for readability — no Dialog migration.
    8. Do not restyle ConfigApp. Run `npm test` after edits.

    Per D-08: only shadcn Button / buttonVariants on this surface.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - GalleryPage has no btn/btnPrimary imports from uiClasses
    - Imports Input and Button or buttonVariants from components/ui
    - type=search present; linear list (divide-y / border) not grid of cards
    - Use preset and Preview action labels present
    - Loading presets… and role=alert error path still present
    - npm test exits 0
  </acceptance_criteria>
  <done>Gallery route fully on shadcn Mist Sky linear list with quiet states and single search.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wizard choice rows + Input/Textarea + Start panel (D-13–D-18, WEB-03, WEB-06)</name>
  <files>src/pages/WizardPage.tsx, src/components/WebStartPanel.tsx</files>
  <read_first>
    - src/pages/WizardPage.tsx (full — create/skip handlers, mode/profile state)
    - src/components/WebStartPanel.tsx (full — STEPS, CTAs, kicker)
    - src/components/ui/input.tsx
    - src/components/ui/textarea.tsx
    - src/components/ui/button.tsx
    - .planning/phases/02-web-chrome-standalone-pages/02-UI-SPEC.md Wizard + WebStartPanel + copy
    - .planning/phases/02-web-chrome-standalone-pages/02-RESEARCH.md Pattern 5 choice rows
  </read_first>
  <behavior>
    - Wizard create still applyModePreset + applyProfilePreset + setWebDraft + meta + navigate
    - Skip blank still setWebDraft({}) + navigate
    - Start panel still exposes onUpload / onLoadPreset / Link to /new — no ConfigApp chrome edits
    - No choiceBtn / btn / btnPrimary imports on either file
    - Single-page wizard (no multi-step chrome)
  </behavior>
  <action>
    Restyle Wizard and empty start (WEB-03, D-13–D-18, D-08) — domain paths unchanged.

    **WizardPage**
    1. Drop all `uiClasses` imports. Keep WebShell active="new".
    2. Layout max-w-lg single page (D-14): title New preset; intro copy per UI-SPEC.
    3. Mode + profile: replace `choiceBtn*` tiles with **linear choice rows** (D-13) — native buttons; idle 1px border muted; active left border 2–3px primary + soft primary wash; min-h-12 p-4; capitalize labels. Mode may stay equal flex row pair; profile full-width stack.
    4. Meta: visible labels (not placeholder-only) + shadcn `Input` (title) + `Textarea` (description); optional fields stay optional (D-15). Placeholders per UI-SPEC.
    5. Bottom CTAs (D-16): primary Button “Open editor”; outline “Skip (blank)”; primary full-width on mobile (`w-full sm:w-auto`). Keep create/skip handler bodies identical in behavior.

    **WebStartPanel**
    6. Keep 3-step list + 3 CTAs (D-17). Kicker “Git · Ship · Done” = Mist Sky primary uppercase tracking (D-18) — use `text-primary`, not logo cyan utility. H1 text-2xl; body muted.
    7. Steps: linear left rule; step markers square/radius-0 (not neon cyan dots).
    8. CTAs: Import files = Button default; Load preset = outline Button; New preset = Link + buttonVariants outline. Footer “browse the preset gallery” primary link.
    9. Do **not** edit ConfigApp.tsx toolbar/sidebar (D-24). Only WebStartPanel presentation.
    10. Run `npm test`.

    Per D-08: shadcn Button language only on these surfaces.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - WizardPage has no choiceBtn/btn/btnPrimary imports; uses Input and Textarea
    - Open editor and Skip (blank) labels present
    - WebStartPanel has Git · Ship · Done kicker and three CTAs Import files / Load preset / New preset
    - WebStartPanel has no btn/btnPrimary imports
    - ConfigApp.tsx not modified this task
    - npm test exits 0
  </acceptance_criteria>
  <done>Wizard and start panel on Mist Sky linear grammar with shadcn inputs/buttons.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: OAuth WebShell wrap + phase02 surfaces gate + dual builds (D-19, D-20, WEB-05, WEB-06)</name>
  <files>src/pages/OAuthCallbackPage.tsx, src/lib/phase02.surfaces.test.ts</files>
  <read_first>
    - src/pages/OAuthCallbackPage.tsx (full — completeOAuthSubmit, navigate state prUrl)
    - src/components/WebShell.tsx (active prop union)
    - src/components/SubmitPresetModal.tsx (completeOAuthSubmit export only — do not rewrite OAuth exchange)
    - .planning/phases/02-web-chrome-standalone-pages/02-UI-SPEC.md OAuth contract + copy
    - .planning/phases/02-web-chrome-standalone-pages/02-PATTERNS.md phase02.surfaces.test.ts section
    - .planning/phases/02-web-chrome-standalone-pages/02-VALIDATION.md Wave 0 surfaces + dual build
  </read_first>
  <behavior>
    - OAuth still reads code from URLSearchParams; missing code sets Missing authorization code (or UI-SPEC equivalent)
    - completeOAuthSubmit(code) still called; success navigate replace with prUrl state
    - No console.log of code or tokens
    - OAuthCallbackPage source includes WebShell
    - phase02.surfaces asserts listed Phase 2 files do not import forbidden uiClasses button symbols
  </behavior>
  <action>
    Finish WEB-05 and WEB-06 gates (D-19, D-20, D-22).

    **OAuthCallbackPage**
    1. Wrap status UI in `<WebShell active="editor">` (D-19) — not bare full-viewport orphan.
    2. Quiet loading: “Completing sign-in…” muted centered text.
    3. Error: soft danger text `role="alert"` + Back to editor via Link or Button/link variant (D-20 quiet — no confetti/loud banners). Keep missing-code and catch error strings.
    4. Preserve useEffect OAuth exchange exactly: `completeOAuthSubmit(code)` → navigate with prUrl. **Do not log** authorization code, tokens, or full query string. Do not change SubmitPresetModal OAuth implementation.
    5. Drop gsd-* utility-only styling in favor of semantic tokens (bg-background, text-destructive, etc.).

    **phase02.surfaces.test.ts**
    6. Create node-env source contract test (readFileSync pattern like foundation.isolation):
       - Files: WebShell.tsx, ThemeToggle.tsx, WebStartPanel.tsx, GalleryPage.tsx, WizardPage.tsx, OAuthCallbackPage.tsx
       - Assert none import `btn`, `btnPrimary`, `btnSegment`, `choiceBtn` (and prefer no `segmentGroup` / `choiceBtnActive`) from uiClasses — use careful greps that do not false-positive comments.
       - Assert OAuthCallbackPage source contains `WebShell`
       - Optional: button.tsx contains rounded-none; index.web.css contains Mist Sky primary hex
    7. Keep `uiClasses.ts` file intact for Phase 3/4 consumers (D-22) — only Phase 2 surfaces stop importing button symbols.
    8. Run: `npx vitest run src/lib/phase02.surfaces.test.ts src/lib/foundation.isolation.test.ts && npm test && npm run build:web && npm run build` (ISO-01 desktop build still green).

    Security: OAuth UI wrap only; no code logging; no secret path regression.
  </action>
  <verify>
    <automated>npx vitest run src/lib/phase02.surfaces.test.ts src/lib/foundation.isolation.test.ts && npm test && npm run build:web && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - OAuthCallbackPage imports and renders WebShell
    - completeOAuthSubmit still used; no console logging of code
    - phase02.surfaces.test.ts exists and exits 0
    - Phase 2 surface files free of btn/btnPrimary/btnSegment/choiceBtn imports
    - npm test exits 0
    - npm run build:web and npm run build exit 0
  </acceptance_criteria>
  <done>OAuth in WebShell; WEB-06 surface contracts green; dual builds prove desktop isolation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OAuth callback query string → completeOAuthSubmit | Authorization code is sensitive; UI must not log or display unnecessarily |
| Gallery preview → ShareModal / buildShareablePreset | Must keep redaction path; no secret leakage in restyle |
| Web draft localStorage | Unchanged; presentation only |
| Desktop vs web build | Dual build gate preserves ISO-01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-OAuth | Information Disclosure | OAuthCallbackPage | high | mitigate | Do not console.log code/query; only call completeOAuthSubmit; quiet error strings; no new telemetry |
| T-02-10 | Information Disclosure | Gallery Preview / ShareModal | medium | mitigate | Keep buildShareablePreset + existing ShareModal; do not bypass redaction |
| T-02-11 | Tampering | Wizard draft write | low | mitigate | Preserve applyModePreset/applyProfilePreset; no schema changes |
| T-02-12 | Spoofing | External PR / gallery links | low | mitigate | Keep rel=noreferrer / noopener on external anchors |
| T-02-13 | Elevation | OAuth | low | accept | No authz model change; callback remains web-only exchange |
| T-02-SC | Tampering | npm | low | accept | No new package installs this plan |
</threat_model>

<verification>
- phase02.surfaces.test.ts + foundation.isolation + full npm test green
- Dual builds: build:web + build green
- Manual residual (not blocking this plan): theme matrix, gallery offline error, wizard create path, OAuth missing-code UI
</verification>

<success_criteria>
- WEB-02 / WEB-03 / WEB-05 pages restyled on shadcn
- WEB-06 no mixed button systems on Phase 2 surfaces
- WEB-07 quiet load/empty/error patterns on Gallery + OAuth
- OAuth inside WebShell (D-19); quiet success/error (D-20)
- Desktop build still green (ISO-01)
</success_criteria>

<output>
Create `.planning/phases/02-web-chrome-standalone-pages/02-04-SUMMARY.md` when done
</output>
