---
phase: 04-form-kit-adapters-editor-chrome
plan: 05
type: execute
wave: 5
depends_on:
  - "04"
files_modified:
  - src/lib/phase04.forms.test.ts
  - src/lib/foundation.isolation.test.ts
  - src/index.web.css
autonomous: true
requirements:
  - FRM-01
  - FRM-02
  - FRM-03
  - FRM-04
  - WEB-04
must_haves:
  truths:
    - "phase04.forms.test.ts locks FormControls exports, data-field-path/data-invalid, web ui primitive imports, no native multi listbox, ModelPicker sentinel, ModelChain filter(Boolean), TextField String(value), ConfigApp useDirty/enablement, web toolbar Button language, Sidebar left-edge language"
    - "Desktop FormControls legacy markers still present in source (role=switch track and/or native select branch)"
    - "FND-03 still requires Phase 4 primitives; dump peers still forbidden"
    - "Web bridge no longer requires .gsd-btn* for success after toolbar migration (locked RESEARCH Q2); desktop form tag chrome remains"
    - "Dual builds green: npm run build:web && npm run build"
    - "preferencesCore redaction/scan tests remain green"
    - "Optional residual web .gsd-btn CSS for library sections is Phase 5-tolerant; FormControls/ConfigApp web path must not reintroduce btn language"
  artifacts:
    - path: src/lib/phase04.forms.test.ts
      provides: "FRM-01/02/03/04 + WEB-04 source contracts"
    - path: src/lib/foundation.isolation.test.ts
      provides: "Updated FND-03 + bridge expectations post shell"
    - path: src/index.web.css
      provides: "Optional bridge class retirement when safe"
  key_links:
    - from: phase04.forms.test.ts
      to: FormControls.tsx + ConfigApp.tsx + Sidebar.tsx
      via: "readFileSync contracts (phase02/03 pattern)"
    - from: dual builds
      to: ISO compile gate
      via: "build:web + build"
    - from: preferencesCore.test.ts
      to: redaction security
      via: "no regression while UI rewired"
---

<objective>
Add **phase04 source contracts**, update **FND-03/bridge isolation** after shell migration, optionally retire unused web button-bridge CSS, and gate **dual builds** (per FRM-01–04, WEB-04, locked RESEARCH Q2/Q4).

Purpose: Automated locks so form kit + editor chrome cannot regress; prove web+desktop still build.

Output: `phase04.forms.test.ts`; updated isolation; green dual builds.
</objective>

## Phase Goal

**As a** web user of GSD Pi Config, **I want to** edit preferences with Mist Sky form controls and a restyled editor shell, **so that** the cloud editor looks cohesive without changing dirty, save, download, or domain behavior.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-form-kit-adapters-editor-chrome/04-01-SUMMARY.md
@.planning/phases/04-form-kit-adapters-editor-chrome/04-02-SUMMARY.md
@.planning/phases/04-form-kit-adapters-editor-chrome/04-03-SUMMARY.md
@.planning/phases/04-form-kit-adapters-editor-chrome/04-04-SUMMARY.md
@.planning/phases/04-form-kit-adapters-editor-chrome/04-RESEARCH.md
@.planning/phases/04-form-kit-adapters-editor-chrome/04-VALIDATION.md
@.planning/phases/04-form-kit-adapters-editor-chrome/04-PATTERNS.md
@src/lib/phase03.overlays.test.ts
@src/lib/phase02.surfaces.test.ts
@src/lib/foundation.isolation.test.ts
@src/components/FormControls.tsx
@src/ConfigApp.tsx
@src/components/Sidebar.tsx
@src/index.web.css
@src/index.desktop.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: phase04.forms source contracts (FRM-01–04, WEB-04)</name>
  <files>src/lib/phase04.forms.test.ts</files>
  <read_first>
    - src/lib/phase03.overlays.test.ts (readFileSync + describe pattern)
    - src/lib/phase02.surfaces.test.ts (surface contract style)
    - src/components/FormControls.tsx (exports + markers after Plans 02–03)
    - src/ConfigApp.tsx (useDirty, Button, enablement after Plan 04)
    - src/components/Sidebar.tsx (left-edge after Plan 04)
    - .planning/phases/04-form-kit-adapters-editor-chrome/04-RESEARCH.md Suggested phase04 source contracts + locked Q4
  </read_first>
  <behavior>
    - FormControls source contains export function for Field, Toggle, SelectField, LabeledSelectField, MultiSelectField, ComboField, TextField, NumberField, TagInput, SectionHeader, ModelPicker, ModelChain
    - data-field-path and data-invalid present
    - Imports or string refs to ui switch/select/checkbox/popover/input/button primitives for web path
    - No native multi listbox pattern (select with multiple attribute)
    - ModelPicker CUSTOM_SENTINEL / __custom__ and provider/model-id
    - ModelChain filter(Boolean) and + Add fallback
    - TextField String(value) coercion path
    - Desktop legacy markers: role=\"switch\" and/or native select still in file
    - ConfigApp: useDirty(, webWorkspaceReady, anyDirty; Button from ui/button
    - Sidebar: left-edge / primary active language; Unsaved changes
  </behavior>
  <action>
    Create phase04.forms.test.ts source contracts (per FRM-01–04, WEB-04, VALIDATION map).

    1. Mirror phase03 structure: node:fs readFileSync, vitest describe/it, root via fileURLToPath.
    2. Assert FormControls exports list, Field attributes, web primitive import paths (flexible @/ or relative), absence of native multi listbox pattern, ModelPicker sentinel + custom placeholder, ModelChain filter(Boolean) + Add fallback, TextField String(value), desktop legacy branch markers (locked Q4).
    3. Assert ConfigApp still uses useDirty(, keeps webWorkspaceReady and anyDirty in save enablement region, imports Button from ui/button for web chrome. Do not require ConfigApp to drop all uiClasses imports if desktop still needs btn* — assert web primary path uses Button (e.g. import present + Save/Download region not exclusively btnPrimary-only).
    4. Assert Sidebar has left-edge/primary active language and Unsaved changes dirty aria.
    5. Do not rewrite product code in this task except fixing false negatives if a prior plan left a contract gap (minimal).
    6. Run until GREEN with current tree after Plans 01–04.

    Source-level only — no DOM render.
  </action>
  <verify>
    <automated>npx vitest run src/lib/phase04.forms.test.ts && npm test -- src/lib/phase04.forms.test.ts src/lib/preferencesCore.test.ts src/lib/phase03.overlays.test.ts</automated>
  </verify>
  <done>phase04.forms.test.ts green locking FRM/WEB contracts; security suites still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Isolation bridge update + optional web btn bridge cleanup + dual builds</name>
  <files>src/lib/foundation.isolation.test.ts, src/index.web.css</files>
  <read_first>
    - src/lib/foundation.isolation.test.ts (bridge test requiring .gsd-btn*, FND-03 required Phase 4)
    - src/index.web.css (.gsd-btn rules)
    - src/ConfigApp.tsx and src/components/FormControls.tsx (confirm web no longer depends on gsd-btn)
    - rg usage of gsd-btn / btnPrimary on web surfaces
    - .planning/phases/04-form-kit-adapters-editor-chrome/04-RESEARCH.md locked Q2 bridge disposition
  </read_first>
  <behavior>
    - FND-03 still requires switch/select/checkbox/popover; forbids card/sheet/drawer/alert-dialog dump
    - Web isolation no longer fails if .gsd-btn* rules are removed; if residual rules remain for library sections, tests must not require them for Phase 4 success
    - Desktop CSS still has form tag chrome and .gsd-btn OK
    - Dual builds succeed
    - Full npm test green including phase04 + preferencesCore + foundation
  </behavior>
  <action>
    Close Phase 4 gates (per locked RESEARCH Q2, ISO, FND-03).

    1. Update foundation.isolation bridge test: web must NOT require .gsd-btn / .gsd-btn-primary / .gsd-btn-segment for success after ConfigApp web toolbar Button migration. Keep Mist Sky token asserts and color-gsd-accent → var(--primary). Desktop form tag chrome asserts remain.
    2. Optionally remove unused .gsd-btn* rules from index.web.css only if no remaining web product surface needs them (ApiKeys/libraries may still reference — if so, leave residual CSS and document in SUMMARY; Phase 5 full purge). Do not strip desktop CSS.
    3. Confirm FND-03 required Phase 4 primitives still enforced.
    4. Run: npm test ; npm run build:web && npm run build.
    5. Do not change preferencesCore, Tauri, or OAuth. Do not domain-edit sections.

    Dual-build gate is mandatory success criteria.
  </action>
  <verify>
    <automated>npm test && npm run build:web && npm run build</automated>
  </verify>
  <done>Isolation + phase04 contracts + full suite + dual builds green; bridge expectation aligned with web Button shell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source contracts → CI | Tests lock presentation contracts without executing secrets paths |
| Web CSS bridge retirement | Residual library chrome may still need bridge until Phase 5 |
| Dual build | Desktop must compile without web-only assumptions breaking |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-13 | Tampering | phase04 contracts incomplete | medium | mitigate | Explicit FRM/WEB asserts including enablement + sentinel + filter(Boolean) |
| T-04-14 | Information Disclosure | preferencesCore regression | high | mitigate | Keep preferencesCore.test.ts in gate; do not edit redaction code |
| T-04-15 | Denial of Service | dual build break | medium | mitigate | Mandatory build:web + build |
| T-04-SC | Tampering | no new installs | low | accept | No package installs this plan |
</threat_model>

<verification>
- npm test green (phase04 + foundation + preferencesCore + phase03 + full suite)
- npm run build:web && npm run build green
- FND-03 still forbids registry dump
</verification>

<success_criteria>
- All FRM-01–04 and WEB-04 locked by automated source contracts
- Dual platform builds green
- Bridge/isolation aligned with Phase 4 shell completion
- Security redaction tests green
</success_criteria>

<output>
Create `.planning/phases/04-form-kit-adapters-editor-chrome/04-05-SUMMARY.md` when done
</output>
