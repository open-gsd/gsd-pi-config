---
phase: 04-form-kit-adapters-editor-chrome
plan: 03
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - src/components/FormControls.tsx
autonomous: true
requirements:
  - FRM-03
must_haves:
  truths:
    - "ModelPicker and ModelChain remain exported from FormControls.tsx with same props (D-11)"
    - "ModelPicker keeps CUSTOM_SENTINEL / __custom__, provider groups, custom free-text provider/model-id path (D-09)"
    - "ModelChain keeps local rows state, filter(Boolean) commit, resync effect, always ≥1 row, reorder/add/remove semantics (D-10)"
    - "Web ModelPicker presentation uses Select groups (or compose) without changing emitted qualified model strings"
    - "Empty model catalog shows quiet inline No models available (D-12)"
    - "ModelChain visual only: linear rows, Primary/Fallback labels, + Add fallback link style, quiet ghost reorder/remove"
  artifacts:
    - path: src/components/FormControls.tsx
      provides: "Restyled ModelPicker + ModelChain presentation"
      exports: ["ModelPicker", "ModelChain"]
  key_links:
    - from: ModelPicker onChange
      to: section prefs model fields
      via: "qualified provider/model strings + custom path"
    - from: ModelChain commit filter(Boolean)
      to: parent chain array
      via: "trailing empty row local-only"
    - from: CUSTOM_SENTINEL
      to: free-text custom Input
      via: "D-09 product UX"
---

<objective>
Restyle **ModelPicker** and **ModelChain** presentation on web while freezing product semantics (per D-09–D-12, FRM-03).

Purpose: Domain pickers match Mist Sky form kit without losing custom model path or chain reorder/add/remove behavior.

Output: Updated ModelPicker/ModelChain branches inside `FormControls.tsx`.
</objective>

## Phase Goal

**As a** web user of GSD Pi Config, **I want to** edit preferences with Mist Sky form controls and a restyled editor shell, **so that** the cloud editor looks cohesive without changing dirty, save, download, or domain behavior.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-form-kit-adapters-editor-chrome/04-02-SUMMARY.md
@.planning/phases/04-form-kit-adapters-editor-chrome/04-CONTEXT.md
@.planning/phases/04-form-kit-adapters-editor-chrome/04-UI-SPEC.md
@.planning/phases/04-form-kit-adapters-editor-chrome/04-RESEARCH.md
@.planning/phases/04-form-kit-adapters-editor-chrome/04-PATTERNS.md
@src/components/FormControls.tsx
@src/components/ui/select.tsx
@src/components/ui/input.tsx
@src/components/ui/button.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ModelPicker web restyle — Select groups + custom path (D-09, D-11, D-12)</name>
  <files>src/components/FormControls.tsx</files>
  <read_first>
    - src/components/FormControls.tsx (ModelPicker: CUSTOM_SENTINEL, knownQualified, isCustom, optgroups, free-text field ~347–405)
    - src/components/ui/select.tsx (Group/Label/Item APIs)
    - .planning/phases/04-form-kit-adapters-editor-chrome/04-UI-SPEC.md Domain pickers ModelPicker
    - .planning/phases/04-form-kit-adapters-editor-chrome/04-RESEARCH.md locked Q3 Select-first
  </read_first>
  <behavior>
    - CUSTOM_SENTINEL remains __custom__
    - Selecting custom shows free-text Input placeholder provider/model-id
    - Catalog values remain provider/model strings; Default/empty maps like today
    - Empty catalog: quiet No models available (12px muted) when no models
    - Web uses Select with groups preferred; desktop may keep native select+optgroup
    - Export name ModelPicker and props unchanged
  </behavior>
  <action>
    Restyle ModelPicker presentation only (per D-09, D-11, D-12, FRM-03).

    1. Keep CUSTOM_SENTINEL, knownQualified Set, isCustom detection, and onChange value rules exactly.
    2. Web: prefer shadcn Select with provider groups (SelectGroup/SelectLabel/SelectItem). Include Default/empty and — Custom (provider/model) — option. When custom, render Input below for provider/model-id free text (same as today).
    3. Empty catalog: quiet inline No models available (D-12) — do not crash Select.
    4. Do not switch to Command search unless Select is unusable with catalog length (locked RESEARCH Q3: Select first).
    5. Desktop: keep native select + optgroup path.
    6. Do not edit ModelsSection or other sections. Do not change ModelChain in this task.

    Product UX preserved; presentation only.
  </action>
  <verify>
    <automated>npx tsc --noEmit && rg -n 'CUSTOM_SENTINEL|__custom__|provider/model-id|No models available|ModelPicker' src/components/FormControls.tsx | head -40</automated>
  </verify>
  <done>ModelPicker web Select groups + custom path; empty quiet message; semantics frozen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ModelChain web restyle — linear rows only (D-10, D-11)</name>
  <files>src/components/FormControls.tsx</files>
  <read_first>
    - src/components/FormControls.tsx (ModelChain: rows state, useEffect resync, commit filter(Boolean), Primary/Fallback, move/remove, + Add fallback ~420–537)
    - .planning/phases/04-form-kit-adapters-editor-chrome/04-UI-SPEC.md ModelChain contract
    - .planning/phases/04-form-kit-adapters-editor-chrome/04-RESEARCH.md Pitfall 4 ModelChain
  </read_first>
  <behavior>
    - Local rows may hold trailing empty string; parent receives filter(Boolean) only
    - Resync effect when external chain diverges from filtered local rows remains
    - Always ≥1 UI row; remove last disabled; primary index 0
    - Labels Primary / Fallback N; control + Add fallback (primary text link, not filled button)
    - Reorder/remove keep aria-labels; no drag-and-drop
    - Visual: linear rows, 12px uppercase muted labels, quiet ghost controls on web
  </behavior>
  <action>
    Restyle ModelChain visuals only (per D-10, D-11, FRM-03).

    1. DO NOT simplify or rewrite commit/filter/resync algorithm. Keep rows state, useEffect resync, commit(next) → onChange(next.filter(Boolean)), always ≥1 row, disable remove when single row.
    2. Web visual: linear stack; labels text-xs font-semibold uppercase text-muted-foreground; ModelPicker (or existing picker) per row; ↑↓ and × as quiet ghost Button controls with existing aria-labels (Move up/down, Remove); + Add fallback as text-xs text-primary link-style control (not filled primary Button).
    3. Drop loud gsd accent washes / active scale on web. Type scale 12/14 only on restyled web chrome.
    4. Desktop: keep functional markup; visual legacy OK.
    5. Do not invent drag-and-drop. Do not edit section files.

    Semantics frozen — visual only.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test && rg -n 'filter\(Boolean\)|\+ Add fallback|ModelChain|setRows|commit' src/components/FormControls.tsx | head -40</automated>
  </verify>
  <done>ModelChain linear Mist Sky rows; commit/resync/reorder semantics unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ModelPicker free text → prefs | User-supplied provider/model strings |
| ModelChain local rows → parent chain | Empty rows must not pollute saved chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Tampering | ModelChain commit | high | mitigate | Keep filter(Boolean) commit + resync; no algorithm rewrite |
| T-04-08 | Tampering | ModelPicker sentinel | medium | mitigate | CUSTOM_SENTINEL only for UI custom path; never persist sentinel as model id |
| T-04-09 | Information Disclosure | model ids display | low | accept | Model ids are config not secrets; no console logging |
| T-04-SC | Tampering | no new installs | low | accept | Uses existing primitives |
</threat_model>

<verification>
- tsc + npm test green
- CUSTOM_SENTINEL and filter(Boolean) still present
- ModelPicker/ModelChain still exported from FormControls
</verification>

<success_criteria>
- FRM-03 domain pickers restyled without product UX loss
- D-09–D-12 satisfied
- No section domain rewrites
</success_criteria>

<output>
Create `.planning/phases/04-form-kit-adapters-editor-chrome/04-03-SUMMARY.md` when done
</output>
