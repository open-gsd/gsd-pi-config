---
phase: 04-form-kit-adapters-editor-chrome
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/components/FormControls.tsx
autonomous: true
requirements:
  - FRM-01
  - FRM-02
must_haves:
  truths:
    - "Same FormControls export names and props remain (Field, Toggle, SelectField, LabeledSelectField, MultiSelectField, ComboField, TextField, NumberField, TagInput, SectionHeader)"
    - "Web presentation uses Switch/Select/Input/Popover/Checkbox via isWebPlatform branches; desktop keeps legacy form markup (D-01, D-02)"
    - "Field still sets data-field-path and data-invalid; registry validators still drive error text (FRM-01/02)"
    - "Toggle web maps checked/onChange to Switch checked/onCheckedChange with switch a11y (D-05)"
    - "SelectField/LabeledSelectField preserve option values; empty maps to undefined (D-06)"
    - "MultiSelect/Combo/TagInput use Popover+Checkbox/Input compose on web — no native multi listbox (D-07)"
    - "TextField keeps snowflake string coercion; NumberField uses Input type number (D-08)"
    - "Section editors are not domain-rewritten — they inherit via FormControls only (D-03, FRM-02)"
  artifacts:
    - path: src/components/FormControls.tsx
      provides: "Web Mist Sky form kit adapters + desktop legacy branches"
      exports: ["Field", "Toggle", "SelectField", "LabeledSelectField", "MultiSelectField", "ComboField", "TextField", "NumberField", "TagInput", "SectionHeader"]
  key_links:
    - from: Field data-field-path
      to: ConfigApp pendingFocus querySelector
      via: "palette field jump contract"
    - from: isWebPlatform()
      to: web Switch/Select/Popover vs desktop legacy tags
      via: "D-01/D-02 isolation"
    - from: sections/*Section.tsx
      to: FormControls exports
      via: "FRM-02 presentation inheritance only"
---

<objective>
Restyle **FormControls** web presentation for the full form kit (Field, Toggle→Switch, Selects, Multi/Combo/Tags, Text/Number, SectionHeader) while keeping export APIs and desktop legacy chrome (per D-01–D-08, FRM-01, FRM-02).

Purpose: All preference sections inherit Mist Sky form look without domain rewrites in *Section.tsx.

Output: Updated `FormControls.tsx` with platform-branched presentation.
</objective>

## Phase Goal

**As a** web user of GSD Pi Config, **I want to** edit preferences with Mist Sky form controls and a restyled editor shell, **so that** the cloud editor looks cohesive without changing dirty, save, download, or domain behavior.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-form-kit-adapters-editor-chrome/04-01-SUMMARY.md
@.planning/phases/04-form-kit-adapters-editor-chrome/04-CONTEXT.md
@.planning/phases/04-form-kit-adapters-editor-chrome/04-UI-SPEC.md
@.planning/phases/04-form-kit-adapters-editor-chrome/04-RESEARCH.md
@.planning/phases/04-form-kit-adapters-editor-chrome/04-PATTERNS.md
@src/components/FormControls.tsx
@src/platform/index.ts
@src/components/ui/switch.tsx
@src/components/ui/select.tsx
@src/components/ui/checkbox.tsx
@src/components/ui/popover.tsx
@src/components/ui/input.tsx
@src/components/ui/button.tsx
@src/lib/fields.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Web Field, Toggle, Selects, Text, Number, SectionHeader adapters (D-01, D-05, D-06, D-08)</name>
  <files>src/components/FormControls.tsx</files>
  <read_first>
    - src/components/FormControls.tsx (Field, Toggle, SelectField, LabeledSelectField, TextField, NumberField, SectionHeader implementations)
    - src/platform/index.ts (isWebPlatform)
    - src/components/ui/switch.tsx, select.tsx, input.tsx (props: checked/onCheckedChange, value/onValueChange)
    - .planning/phases/04-form-kit-adapters-editor-chrome/04-UI-SPEC.md Form control anatomy
    - .planning/phases/04-form-kit-adapters-editor-chrome/04-PATTERNS.md FormControls section + Pattern 1
    - .planning/phases/04-form-kit-adapters-editor-chrome/04-RESEARCH.md Open Questions locked empty-Select strategy
  </read_first>
  <behavior>
    - export function still present for Field, Toggle, SelectField, LabeledSelectField, TextField, NumberField, SectionHeader with unchanged prop shapes
    - Field always writes data-field-path when path set; data-invalid when validator error; empty value still skips validator
    - Web Toggle uses Switch; desktop keeps existing role=switch button track
    - Web SelectField/LabeledSelectField use Select; empty → undefined; option values preserved
    - Web TextField/NumberField use Input; TextField keeps String(value) coercion
    - Web SectionHeader title text-xl font-semibold; description text-sm muted
    - No ModelPicker/ModelChain changes in this task; no *Section.tsx edits
  </behavior>
  <action>
    Implement web presentation branches inside shared FormControls exports (per D-01, D-02, D-05, D-06, D-08, FRM-01).

    1. Import isWebPlatform from platform and Switch/Select/Input (and cn if needed) from ui primitives. Prefer @/ aliases matching project.
    2. Field: restyle web classes to linear Mist Sky (label 14/600, description 12 muted, soft destructive error, sm layout label left/control right, bottom border divider). MUST keep data-field-path={path}, data-invalid when error, getField validator wiring, hint ? behavior. Soft destructive border language on invalid. Do not remove path/value props.
    3. Toggle: if web, render Switch with checked + onCheckedChange={onChange} inside min-h-10 min-w-10 hit wrapper; className h-5 w-9. Else keep existing desktop track markup unchanged.
    4. SelectField / LabeledSelectField: if web, compose Select Root/Trigger/Value/Content/Item; map product undefined ↔ empty; if SelectItem rejects empty string, use internal sentinel that never reaches onChange prefs. Preserve generic T and option values exactly for non-empty. Desktop keeps native select.
    5. TextField: web uses Input type text with existing display coercion and onChange → undefined when empty. NumberField: web Input type number with min/max/placeholder props preserved.
    6. SectionHeader: web title text-xl font-semibold text-foreground; description text-sm text-muted-foreground; mb-4. Desktop may keep gsd-heading text-lg.
    7. Drop web active scale press theater on these controls. Do not reintroduce gsd-btn on web path. Do not edit sections, ConfigApp, useDirty, fields registry paths, or ModelPicker/ModelChain yet.

    Empty Select strategy per locked RESEARCH open question 1.
  </action>
  <verify>
    <automated>npx vitest run src/lib/preferencesCore.test.ts src/components/ui/ && npx tsc --noEmit && rg -n 'data-field-path|data-invalid|isWebPlatform|from \"@/components/ui/switch\"|from \"@/components/ui/select\"|from \"@/components/ui/input\"' src/components/FormControls.tsx | head -40</automated>
  </verify>
  <done>Core form kit web adapters live; Field path/invalid contracts intact; desktop legacy branches remain.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Web MultiSelect, ComboField, TagInput compose (D-07)</name>
  <files>src/components/FormControls.tsx</files>
  <read_first>
    - src/components/FormControls.tsx (MultiSelectField, ComboField, TagInput current behavior: summary, toggle, chips, Enter-add)
    - src/components/ui/popover.tsx, checkbox.tsx, input.tsx, button.tsx
    - .planning/phases/04-form-kit-adapters-editor-chrome/04-UI-SPEC.md MultiSelect/Combo/TagInput anatomy
    - .planning/phases/04-form-kit-adapters-editor-chrome/04-RESEARCH.md Pattern 3 MultiSelect + Pitfall 7
  </read_first>
  <behavior>
    - MultiSelectField API unchanged: values, onChange, options, placeholder
    - Web MultiSelect: Popover + Checkbox rows; no native multi listbox element; chips with named Remove {label} buttons; summary placeholder / joined / N selected
    - ComboField API unchanged: free text + options; empty → undefined; web prefer Input + Popover suggestions (or linear Input if datalist kept only when still linear)
    - TagInput: Enter adds unique; quiet bordered chips; named remove; web Input under chips
    - Desktop Multi/Combo/Tag keep prior markup paths
    - ESC/outside close owned by Popover on web (drop hand-rolled window listeners on web path)
  </behavior>
  <action>
    Compose multi/combo/tag web UX (per D-07, FRM-01, UI-SPEC).

    1. MultiSelectField web: Popover trigger (min-h-10, summary text as today); panel lists options with Checkbox checked/onCheckedChange wiring to existing toggle(value) add/remove; chips below with quiet 1px border and Remove {label} aria-labels. Optional left-edge/soft wash on checked rows. No native multi listbox. Checkbox list first — do not add Command filter unless option lists prove long (locked RESEARCH Q3 is for ModelPicker; Multi stays checkbox-first per UI-SPEC).
    2. ComboField web: Input + Popover (or Command) suggestions preferred; free text onChange; empty string → undefined. Keep options list behavior.
    3. TagInput web: quiet chips + Input; Enter adds unique values; remove buttons named Remove {value}; hover soft danger on remove. Drop accent-filled chip wash on web.
    4. Desktop branches: preserve existing MultiSelect panel/listeners, Combo datalist/input, TagInput behavior.
    5. Use Button ghost/outline for chip remove if buttonized — no btn/gsd-btn on web. Drop active scale on web triggers.
    6. Do not change ModelPicker/ModelChain (Plan 03). Do not edit *Section.tsx. Do not touch dirty/save handlers.

    No native multi listbox per D-07 / DESIGN anti-pattern.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test && rg -n 'Popover|Checkbox|MultiSelectField|TagInput|ComboField' src/components/FormControls.tsx | head -50 && ! rg -n 'select multiple' src/components/FormControls.tsx</automated>
  </verify>
  <done>Multi/Combo/Tag web compose live with chip remove UX; desktop paths preserved; no native multi listbox.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User form input → Field validators | Untrusted preference values display/validate in UI |
| FormControls → prefs state | onChange must not invent values or drop snowflake strings |
| Web vs desktop presentation | Isolation boundary — web shadcn must not force desktop Mist Sky success |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Tampering | Field path / data-field-path | medium | mitigate | Keep path attributes; no dangerouslySetInnerHTML; CSS.escape remains in ConfigApp |
| T-04-05 | Information Disclosure | TextField snowflake / secrets display | medium | mitigate | Keep string coercion; do not log field values; validators unchanged |
| T-04-06 | Elevation of Privilege | Select empty/sentinel | low | mitigate | Internal empty sentinel never emitted into prefs (locked Q1) |
| T-04-SC | Tampering | no new installs this plan | low | accept | Uses Plan 01 primitives only |
</threat_model>

<verification>
- tsc clean; npm test green
- FormControls still exports full form kit API
- data-field-path / data-invalid present
- No select-multiple native multi pattern
- preferencesCore tests still green
</verification>

<success_criteria>
- FRM-01 form kit restyled on web via stable API
- FRM-02 sections inherit without domain rewrites
- D-01/D-02 platform isolation holds
- D-05–D-08 control mapping implemented
</success_criteria>

<output>
Create `.planning/phases/04-form-kit-adapters-editor-chrome/04-02-SUMMARY.md` when done
</output>
