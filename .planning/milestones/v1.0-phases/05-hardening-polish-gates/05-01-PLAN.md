---
phase: 05-hardening-polish-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/CustomProvidersSection.tsx
  - src/components/sections/ApiKeysSection.tsx
autonomous: true
requirements:
  - ISO-05
  - ISO-04
must_haves:
  truths:
    - "CustomProvidersSection web residual CTAs use shadcn Button (default/outline/soft destructive) with min-h ≥40px — no uiClasses btn/btnPrimary/btnDanger imports (D-01)"
    - "ApiKeysSection web residual CTAs (Export env.sh, Show/Hide, Set/Edit/Save/Cancel/Clear/Dismiss) use Button + semantic banner tokens — no uiClasses btn language or bannerDanger (D-01, D-04)"
    - "ApiKeys dense text-[9–11px] action chips upgraded to Button size sm (≥40px hit) preferring a11y over compact cosmetics (D-10, D-12)"
    - "Semantic Mist Sky tokens replace gsd-* color utilities on both residual sections; residual cards use radius 0 (D-00a, D-04)"
    - "Domain handlers preserved: provider CRUD, exportEnv, setKey, clearKey, window.confirm clear-key path, masked reveal, useConfigBackend key APIs (D-00d / ISO-02 wiring only)"
    - "No Skills/Agents library restyle; no preferencesCore/backend edits; no new registry packages"
  artifacts:
    - path: src/components/sections/CustomProvidersSection.tsx
      provides: "Web residual Button language + semantic tokens for custom providers"
      contains: "Button"
    - path: src/components/sections/ApiKeysSection.tsx
      provides: "Web residual Button language + Input search + soft-danger banner"
      contains: "Button"
  key_links:
    - from: CustomProvidersSection / ApiKeysSection
      to: "@/components/ui/button"
      via: "residual CTA language (WEB-06 completion)"
    - from: ApiKeysSection
      to: "@/components/ui/input"
      via: "search / edit fields preferred over bare dense inputs"
    - from: residual destructive CTAs
      to: "soft outline + destructive tint"
      via: "no solid red fill (palette lock)"
---

<objective>
Purge residual **uiClasses button language** and migrate **gsd-\* color utilities** on the two highest-density web-visible residual sections: CustomProviders and ApiKeys (per D-01, D-04, D-10, D-12, ISO-05, ISO-04 hit-target floor).

Purpose: Complete WEB-06 residual CTAs so later CSS purge of web button-bridge rules is safe.

Output: CustomProvidersSection + ApiKeysSection on Button + semantic tokens; domain handlers untouched.
</objective>

## Phase Goal

**As a** web user of GSD Pi Config, **I want to** finish the redesign with cohesive Mist Sky residual chrome, stable domain behavior, and accessible controls, **so that** the milestone closes without product or IA changes.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/design/PALETTE.md
@.planning/phases/05-hardening-polish-gates/05-CONTEXT.md
@.planning/phases/05-hardening-polish-gates/05-UI-SPEC.md
@.planning/phases/05-hardening-polish-gates/05-RESEARCH.md
@.planning/phases/05-hardening-polish-gates/05-PATTERNS.md
@.planning/phases/05-hardening-polish-gates/05-VALIDATION.md
@src/components/ui/button.tsx
@src/components/ui/input.tsx
@src/components/WebStartPanel.tsx
@src/ConfigApp.tsx
@src/lib/uiClasses.ts
@src/components/sections/CustomProvidersSection.tsx
@src/components/sections/ApiKeysSection.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CustomProvidersSection — Button language + semantic tokens</name>
  <files>src/components/sections/CustomProvidersSection.tsx</files>
  <read_first>
    - src/components/sections/CustomProvidersSection.tsx (full residual inventory: uiClasses import, btn/btnPrimary/btnDanger buttons, gsd-* cards)
    - src/components/ui/button.tsx (default/sm min-h-10, destructive soft wash, focus-visible ring)
    - src/components/WebStartPanel.tsx (Button + semantic token analog)
    - .planning/phases/05-hardening-polish-gates/05-UI-SPEC.md residual inventory + button language + token map + copywriting
    - .planning/phases/05-hardening-polish-gates/05-PATTERNS.md CustomProvidersSection section
  </read_first>
  <behavior>
    - File does not import btn, btnPrimary, or btnDanger from uiClasses
    - Primary CTA is Button variant default size sm with label "+ Add provider"
    - Delete provider / Remove model use soft destructive Button (outline + destructive tint or Button destructive soft wash) — not solid red fill
    - Show/Hide and + Add model use Button outline size sm
    - Empty state uses rounded-none dashed border-border bg-card and existing product copy about no custom providers
    - Provider cards use rounded-none border-border bg-card; collision uses soft destructive border/wash
    - Labels/meta use text-xs muted/foreground semantic tokens — no text-gsd-* or text-[9–11px] on restyled residual
    - Provider CRUD bodies (add/update/rename/delete/model ops) and FormControls field wiring unchanged
  </behavior>
  <action>
    Restyle CustomProvidersSection presentation only (per D-01, D-04, D-00a, D-10, ISO-05).

    1. Remove uiClasses import of btn/btnPrimary/btnDanger entirely. Import Button from @/components/ui/button. Keep FormControls imports.
    2. Replace every native button using btn* class helpers with Button size sm: primary "+ Add provider" → variant default; Show/Hide and "+ Add model" → outline; Delete/Remove → soft destructive (outline + text-destructive border-destructive/40 hover:bg-destructive/10, or Button destructive soft wash). Preserve title="Delete provider" / existing labels from UI-SPEC copywriting.
    3. Migrate gsd-* color utilities per UI-SPEC token table: text-gsd-text → text-foreground; dim/secondary/muted → text-muted-foreground; accent text → text-primary; surfaces → bg-card/bg-background; borders → border-border; danger → destructive soft. Empty panel and cards: rounded-none (drop rounded-lg/md).
    4. Do not rewrite updateProvider/renameProvider/deleteProvider/addProvider/model CRUD logic, collision BUILTIN_IDS rules, or FormControls field paths/validators. Do not edit Skills/Agents libraries, preferencesCore, backends, sectionConfig, or desktop CSS.
    5. Optional: prefer shadcn Input for free-text password/model id only if it does not change controlled value wiring — FormControls fields already fine.
    6. Prefer a11y over compact cosmetics (D-12): all residual CTAs must hit Button min-h-10 floor (D-10). Remove active:scale press theater if present on residual controls.

    Surgical class/JSX only — ISO-02 domain freeze (D-00d).
  </action>
  <verify>
    <automated>npx vitest run src/lib/preferencesCore.test.ts && rg -n "from ['\"].*uiClasses" src/components/sections/CustomProvidersSection.tsx ; test $? -ne 0 && rg -n "btnPrimary|btnDanger|\\bbtn\\b" src/components/sections/CustomProvidersSection.tsx ; test $? -ne 0 && rg -n "Button" src/components/sections/CustomProvidersSection.tsx && rg -n "text-gsd-|bg-gsd-|border-gsd-" src/components/sections/CustomProvidersSection.tsx ; test $? -ne 0</automated>
  </verify>
  <done>CustomProviders residual web surface is Button + semantic tokens; no uiClasses btn imports; domain CRUD intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ApiKeysSection — Button language + Input search + banner tokens + ≥40px actions</name>
  <files>src/components/sections/ApiKeysSection.tsx</files>
  <read_first>
    - src/components/sections/ApiKeysSection.tsx (uiClasses import, Export CTA, bannerDanger, dense text-[10px] chips, gsd-* density)
    - src/components/ui/button.tsx and src/components/ui/input.tsx
    - src/ConfigApp.tsx web toolbar Button + quiet error banner pattern
    - .planning/phases/05-hardening-polish-gates/05-UI-SPEC.md ApiKeys residual + a11y A1–A7 + copywriting
    - .planning/phases/05-hardening-polish-gates/05-PATTERNS.md ApiKeysSection section
    - .planning/phases/05-hardening-polish-gates/05-RESEARCH.md Pattern 1 Button + soft danger banner
  </read_first>
  <behavior>
    - File does not import btn, btnPrimary, bannerDanger, or any uiClasses symbols
    - Export env.sh is Button default size sm
    - Error banner is role=alert soft-danger (border-destructive/30 bg-destructive/10 text-destructive text-xs rounded-none) with Dismiss Button outline
    - Dense py-1 / text-[10px] action chips (Show/Hide/Set/Edit/Save/Cancel/Clear) become Button size sm (≥40px)
    - Clear remains soft destructive and still uses existing window.confirm Delete key path
    - Search uses Input with accessible name (aria-label or equivalent) and placeholder Search keys...
    - Status badges / group labels use text-xs (12px) scale; no text-[9px]/[10px]/[11px] on restyled residual
    - exportEnv / setKey / clearKey / reveal toggle / CLI status logic bodies unchanged
    - No solid red destructive fills; no logo cyan primary
  </behavior>
  <action>
    Restyle ApiKeysSection presentation only (per D-01, D-04, D-09–D-12, ISO-04, ISO-05).

    1. Remove all uiClasses imports (btn, btnPrimary, bannerDanger). Import Button and Input from @/components/ui/*.
    2. Export env.sh → Button variant default size sm. Row actions → Button outline/default/soft-destructive per UI-SPEC button language table. Save when editing → default. Clear → soft destructive. Keep enablement predicates and onClick handlers byte-stable in behavior.
    3. Replace bannerDanger host with quiet soft-danger bar (role=alert preferred) + Dismiss Button outline. Map export success wash from gsd-accent-dim to primary soft wash (border-primary/30 bg-primary/10 text-primary text-xs rounded-none) — not logo cyan.
    4. Migrate all residual gsd-* color utilities and ad-hoc type sizes to semantic tokens + text-xs label/meta scale (UI-SPEC type cleanup). Cards/groups: rounded-none border-border bg-card. Links Open docs / Get key → text-primary hover:underline (or Button link/ghost). Mono env names: font-mono text-xs text-muted-foreground.
    5. Search field: prefer Input with aria-label Search keys (or existing accessible name). Password/edit fields labeled; preserve masked bullet reveal.
    6. Do not change exportEnv implementation, setKey/clearKey, window.confirm for clear, backend key storage, redaction modules, or CLI detection logic. Do not add AlertDialog. Do not restyle Skills/Agents libraries. Do not edit preferencesCore.

    Hit-target floor ≥40px is mandatory on residual CTAs even if rows get taller (D-10, D-12). Visible focus-visible comes from Button/Input ring (D-11).
  </action>
  <verify>
    <automated>npx vitest run src/lib/preferencesCore.test.ts && rg -n "from ['\"].*uiClasses" src/components/sections/ApiKeysSection.tsx ; test $? -ne 0 && rg -n "bannerDanger|btnPrimary|\\bbtn\\b" src/components/sections/ApiKeysSection.tsx ; test $? -ne 0 && rg -n "Button" src/components/sections/ApiKeysSection.tsx && rg -n "text-\\[9px\\]|text-\\[10px\\]|text-\\[11px\\]" src/components/sections/ApiKeysSection.tsx ; test $? -ne 0 && rg -n "role=\"alert\"" src/components/sections/ApiKeysSection.tsx && rg -n "exportEnv|clearKey|setKey|confirm" src/components/sections/ApiKeysSection.tsx</automated>
  </verify>
  <done>ApiKeys residual web surface is Button + Input + semantic tokens; ≥40px actions; domain key handlers and confirm path intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ApiKeys UI → ConfigBackend key store | Secret values enter/leave via existing setKey/clearKey/exportEnv only |
| CustomProviders UI → prefs document | Provider secrets shown/hidden via existing local state |
| Presentation layer → domain handlers | ClassName/JSX restyle must not alter redaction or key persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | ApiKeysSection exportEnv / reveal | high | mitigate | Do not rewrite exportEnv/setKey/clearKey; keep masked bullets + confirm clear; no console logging of key values |
| T-05-02 | Information Disclosure | CustomProviders password show/hide | medium | mitigate | Preserve showKey toggle behavior; presentation-only Button swap |
| T-05-03 | Tampering | Residual className edits | low | mitigate | Static class strings only; no user HTML interpolation into className |
| T-05-04 | Elevation of Privilege | N/A single-user tool | low | accept | No multi-tenant auth surface in residual sections |
| T-05-SC | Tampering | npm installs | high | mitigate | No package installs this plan — reuse existing Button/Input only |
</threat_model>

<verification>
- CustomProvidersSection and ApiKeysSection have zero uiClasses btn/bannerDanger imports
- Residual CTAs are Button size sm with semantic tokens and radius 0 cards
- preferencesCore tests still green (domain freeze)
- No Skills/Agents, sectionConfig, or backend file edits
</verification>

<success_criteria>
- D-01 web residual btn purge complete for CustomProviders + ApiKeys
- D-04 token migration complete on those two surfaces
- D-10/D-12 residual hit targets ≥40px on key/provider actions
- ISO-05 presentation cohesion advanced without IA/capability change
</success_criteria>

<output>
Create `.planning/phases/05-hardening-polish-gates/05-01-SUMMARY.md` when done
</output>
