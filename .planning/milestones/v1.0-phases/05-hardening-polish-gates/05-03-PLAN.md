---
phase: 05-hardening-polish-gates
plan: 03
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - src/lib/phase05.residual.test.ts
  - src/lib/foundation.isolation.test.ts
  - src/index.web.css
autonomous: true
requirements:
  - ISO-05
  - ISO-02
must_haves:
  truths:
    - "phase05.residual.test.ts forbids uiClasses btn symbol imports on residual web sections (CustomProviders, ApiKeys) and asserts web CSS has no button-bridge rules while desktop CSS still contains them (D-02, D-01)"
    - "WEB_HIDDEN_SECTIONS stability locked to skills-library + agents-library only (ISO-05 / D-03)"
    - "src/index.web.css deletes .gsd-btn family rules after grep proves zero web residual callers; desktop CSS untouched (D-02)"
    - "Web CSS keeps field-focus helper for palette jump and --color-gsd-* theme bridge as needed (S9 / foundation)"
    - "foundation.isolation replaces Phase 4 residual-may-remain tolerance with post-purge asserts: web free of button-bridge selectors; desktop still has them (ISO-01 hold)"
    - "preferencesCore suite still green (ISO-02)"
  artifacts:
    - path: src/lib/phase05.residual.test.ts
      provides: "ISO-05 residual purge + CSS isolation source contracts"
    - path: src/lib/foundation.isolation.test.ts
      provides: "Post-purge web/desktop button-bridge isolation asserts"
    - path: src/index.web.css
      provides: "Web CSS without .gsd-btn family; field-focus + token bridge retained"
  key_links:
    - from: phase05.residual.test.ts
      to: CustomProvidersSection + ApiKeysSection + index.web.css + index.desktop.css + sectionConfig
      via: "readFileSync source contracts (phase02 pattern)"
    - from: CSS purge gate
      to: "zero web residual btn callers"
      via: "grep before delete (D-02 ordered gate)"
    - from: foundation.isolation
      to: "ISO-01 desktop chrome retained"
      via: "desktop still contains button-bridge class"
---

<objective>
Add **phase05 residual source contracts**, **delete web button-bridge CSS** after zero web callers, and update **foundation.isolation** post-purge (per D-02, D-01, D-03, ISO-05, ISO-02 regression bar).

Purpose: Hard-lock residual purge so dead bridge CSS cannot return and desktop isolation still holds.

Output: phase05.residual.test.ts; cleaned index.web.css; updated foundation.isolation.test.ts.
</objective>

## Phase Goal

**As a** web user of GSD Pi Config, **I want to** finish the redesign with cohesive Mist Sky residual chrome, stable domain behavior, and accessible controls, **so that** the milestone closes without product or IA changes.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-hardening-polish-gates/05-01-SUMMARY.md
@.planning/phases/05-hardening-polish-gates/05-02-SUMMARY.md
@.planning/phases/05-hardening-polish-gates/05-RESEARCH.md
@.planning/phases/05-hardening-polish-gates/05-PATTERNS.md
@.planning/phases/05-hardening-polish-gates/05-VALIDATION.md
@src/lib/phase02.surfaces.test.ts
@src/lib/phase03.overlays.test.ts
@src/lib/phase04.forms.test.ts
@src/lib/foundation.isolation.test.ts
@src/index.web.css
@src/index.desktop.css
@src/lib/sectionConfig.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add phase05.residual.test.ts source contracts (expect RED on web button-bridge CSS until Task 2)</name>
  <files>src/lib/phase05.residual.test.ts</files>
  <read_first>
    - src/lib/phase02.surfaces.test.ts (import-forbidden pattern + readFileSync helpers)
    - src/lib/phase03.overlays.test.ts / phase04.forms.test.ts (contract style)
    - src/lib/foundation.isolation.test.ts (Phase 4 residual tolerance comments ~128–131)
    - .planning/phases/05-hardening-polish-gates/05-RESEARCH.md Pattern 4 residual test scaffold
    - .planning/phases/05-hardening-polish-gates/05-VALIDATION.md Wave 0 requirements
    - src/components/sections/CustomProvidersSection.tsx and ApiKeysSection.tsx (post Plan 01 state)
    - src/index.web.css and src/index.desktop.css (button-bridge selectors still present on web until Task 2)
  </read_first>
  <behavior>
    - CustomProvidersSection and ApiKeysSection do not import forbidden uiClasses symbols: btn, btnPrimary, btnDanger, btnSegment, btnSegmentActive
    - Web CSS source does not match button-bridge selectors of the form dot gsd-btn (word boundary)
    - Desktop CSS source still contains the button-bridge class marker
    - sectionConfig still references skills-library and agents-library as WEB_HIDDEN
    - ConfigApp is NOT forbidden from importing uiClasses btn symbols (desktop needs them)
    - Tests are source-level only (no DOM / no Playwright)
  </behavior>
  <action>
    Create residual contracts as the automated lock for D-01/D-02/ISO-05 (Claude discretion: recommended phase05.residual.test.ts).

    1. Add src/lib/phase05.residual.test.ts mirroring phase02.surfaces.test.ts structure: root via fileURLToPath, readFileSync helper, describe blocks.
    2. Assert residual web section files CustomProvidersSection.tsx and ApiKeysSection.tsx do not import forbidden btn symbols from uiClasses (use import-line regex like phase02 — avoid false positives on comments).
    3. Assert web CSS file has no button-bridge family selectors (negative match on the gsd button class root with word boundary). Assert desktop CSS still contains that class root (ISO-01).
    4. Assert sectionConfig.ts still encodes skills-library and agents-library hidden on web (ISO-05). Do not edit sectionConfig.
    5. Do not assert ConfigApp must drop uiClasses imports (open Q2 resolved: allowed for desktop).
    6. Run the new file alone — section import asserts should already be GREEN after Plans 01–02; web CSS absence assert should be RED until Task 2 deletes bridge rules. That RED is expected and required (TDD gate).

    No production code edits in this task except the new test file.
  </action>
  <verify>
    <automated>npx vitest run src/lib/phase05.residual.test.ts ; echo "expect partial RED on web CSS absence until Task 2"; test -f src/lib/phase05.residual.test.ts && rg -n "CustomProvidersSection|ApiKeysSection|index.web.css|index.desktop.css|WEB_HIDDEN|skills-library" src/lib/phase05.residual.test.ts</automated>
  </verify>
  <done>phase05.residual.test.ts exists with section import bans + CSS dual-entry asserts + WEB_HIDDEN freeze; CSS absence RED until purge.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Grep gate → delete web button-bridge CSS → update foundation.isolation post-purge</name>
  <files>src/index.web.css, src/lib/foundation.isolation.test.ts, src/lib/phase05.residual.test.ts</files>
  <read_first>
    - src/index.web.css (button-bridge rules ~290–375 and reduced-motion block; header comments Phase 2→4 bridge)
    - src/index.web.css field-focus selector and --color-gsd-* bridge (KEEP)
    - src/index.desktop.css (must remain with button-bridge — do not edit)
    - src/lib/foundation.isolation.test.ts web CSS describe + Phase 4 residual-may-remain comments
    - .planning/phases/05-hardening-polish-gates/05-UI-SPEC.md CSS purge gate keep/delete lists
    - .planning/phases/05-hardening-polish-gates/05-PATTERNS.md index.web.css section
  </read_first>
  <behavior>
    - Grep proves zero web residual section files import uiClasses btn symbols before CSS delete
    - index.web.css no longer defines button-bridge rules (root, primary, segment, active, reduced-motion orphans)
    - index.web.css still has [data-field-path] field-focus helper and Mist Sky tokens / color-gsd theme bridge as needed
    - index.desktop.css completely unchanged and still contains button-bridge class
    - foundation.isolation no longer tolerates residual web button-bridge presence; asserts absence on web and presence on desktop
    - phase05.residual.test.ts GREEN; preferencesCore GREEN; phase02–04 contracts still GREEN
  </behavior>
  <action>
    Ordered CSS purge + isolation update (per D-02 — never reverse order).

    1. Grep gate (must pass before delete): zero matches of uiClasses btn/btnPrimary/btnDanger/btnSegment imports in web-visible residual sections (CustomProviders, ApiKeys, and any other web section). ConfigApp may still import for desktop. Skills/Agents libraries may still import (desktop-only). Zero web-rendered className strings that apply the button-bridge class family on web-only paths.
    2. Delete from src/index.web.css only: root button-bridge rules and hover/active/disabled; primary variant; segment and segment-active variants; orphaned reduced-motion active rules for that family. Update header comments that still claim residual bridge stays until Phase 4/5 — state web is free of button-bridge chrome post Phase 5.
    3. KEEP on web: --color-gsd-* theme bridge, Mist Sky semantic tokens, radius 0, shadcn/tw-animate imports, and the data-field-path field-focus flash helper used by ConfigApp palette jump. Optional: delete unused nav-item/choice-btn rules from web CSS only if grep shows zero web class string callers — do not delete if any web path still emits them; never touch desktop CSS.
    4. Update foundation.isolation.test.ts: replace Phase 4 “residual may remain until Phase 5” tolerance with post-purge expectations — web CSS must NOT contain the button-bridge class root; desktop CSS must still contain it; keep existing Mist Sky token / FND-03 / desktop form chrome asserts.
    5. Run phase05.residual + foundation.isolation + preferencesCore + full npm test until GREEN.
    6. Do not edit preferencesCore behavior, backends, or uiClasses.ts exports (desktop still needs them).

    Supply-chain: no npm installs (T-05-SC n/a this plan beyond reuse).
  </action>
  <verify>
    <automated>rg -n "from ['\"].*uiClasses" src/components/sections/CustomProvidersSection.tsx src/components/sections/ApiKeysSection.tsx ; test $? -ne 0 && rg -n "\\.gsd-btn\\b" src/index.web.css ; test $? -ne 0 && rg -n "\\.gsd-btn\\b" src/index.desktop.css && rg -n "gsd-field-focus" src/index.web.css && npx vitest run src/lib/phase05.residual.test.ts src/lib/foundation.isolation.test.ts src/lib/preferencesCore.test.ts src/lib/phase02.surfaces.test.ts src/lib/phase03.overlays.test.ts src/lib/phase04.forms.test.ts && npm test</automated>
  </verify>
  <done>Web button-bridge CSS removed; residual + isolation contracts green; desktop CSS untouched; full unit suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web CSS entry → browser presentation | Deleting bridge rules affects all web class consumers |
| Desktop CSS entry | Must remain isolated (ISO-01) |
| Test contracts → CI | Source asserts become regression gates |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-08 | Tampering | index.web.css purge | medium | mitigate | Ordered grep gate before delete; keep field-focus; dual CSS asserts |
| T-05-09 | Denial of Service | Accidental delete of focus helper | low | mitigate | Explicit keep-list for field-focus + reduced-motion only for orphaned btn rules |
| T-05-10 | Information Disclosure | N/A CSS-only | low | accept | No secret handling in CSS |
| T-05-SC | Tampering | npm installs | high | mitigate | No installs this plan |
</threat_model>

<verification>
- phase05.residual.test.ts green
- foundation.isolation post-purge green
- web CSS free of button-bridge; desktop retains it
- field-focus retained on web
- npm test green (ISO-02 bar)
</verification>

<success_criteria>
- D-02 web button-bridge CSS deleted after zero callers
- D-01 residual section contracts locked
- ISO-01 desktop chrome still present in desktop CSS
- ISO-02 unit suite green including preferencesCore
</success_criteria>

<output>
Create `.planning/phases/05-hardening-polish-gates/05-03-SUMMARY.md` when done
</output>
